import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "<header> — шапка страницы",
  order: 2,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# \`<header>\` — шапка страницы

## 🎯 После этого урока ты сможешь
- Делать шапку сайта с логотипом и заголовком
- Понимать когда \`<header>\` нужен внутри других секций

## 📚 Теория

### Что такое \`<header>\`

\`<header>\` — это **вводная часть** страницы или раздела. Обычно содержит:
- Название (логотип, заголовок)
- Главное меню
- Краткое представление того что будет ниже

\`\`\`html
<header>
  <h1>Мой блог</h1>
  <p>Записки разработчика</p>
</header>
\`\`\`

### \`<header>\` бывает не один

На странице можно поставить **несколько \`<header>\`**:
- Один — глобальный, в самом верху страницы
- Внутри **каждого \`<article>\`** или \`<section>\` — свой локальный

\`\`\`html
<header>
  <h1>Мой блог</h1>
</header>

<article>
  <header>
    <h2>Название поста</h2>
    <p>Автор: Иван · 5 минут чтения</p>
  </header>
  <p>Текст поста...</p>
</article>
\`\`\`

Глобальный \`<header>\` — про весь сайт. Внутренний — про один пост.

### Не путать

- \`<header>\` (тег) ≠ \`<head>\` (тег)
- \`<head>\` — невидимая шапка документа (мета, title)
- \`<header>\` — **видимая** шапка страницы или раздела

## 🛠️ Задание

Сделай страничку:

- \`<header>\` с \`<h1>Кулинарный блог</h1>\` и \`<p>Простые рецепты на каждый день</p>\` внутри.
- После хедера: \`<p>Здесь будут посты.</p>\``,
  starterCode: `<!-- Шапка с заголовком и подзаголовком -->
`,
  solution: `<header>
  <h1>Кулинарный блог</h1>
  <p>Простые рецепты на каждый день</p>
</header>
<p>Здесь будут посты.</p>`,
  tests: [
    { kind: "exists", selector: "header", description: "Есть тег <header>" },
    { kind: "exists", selector: "header h1", description: "Внутри <header> есть <h1>" },
    { kind: "text", selector: "header h1", equals: "Кулинарный блог", description: '<h1> содержит "Кулинарный блог"' },
    { kind: "exists", selector: "header p", description: "Внутри <header> есть <p>" },
    { kind: "textContains", selector: "header p", contains: "Простые рецепты", description: 'В <header> есть подзаголовок про простые рецепты' },
  ],
  hints: [
    "<header> — это обёртка. Внутри ставишь <h1> и <p>. После закрывающего </header> идёт основной <p>.",
    "Шаблон: <header><h1>...</h1><p>...</p></header><p>...</p>",
    'Полное решение:\n```html\n<header>\n  <h1>Кулинарный блог</h1>\n  <p>Простые рецепты на каждый день</p>\n</header>\n<p>Здесь будут посты.</p>\n```',
  ],
  isAvailable: true,
};

export default lesson;
