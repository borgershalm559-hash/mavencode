import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Что такое форма",
  order: 8,
  type: "quiz",
  language: "html",
  xpReward: 50,
  content: `# Что такое форма

## 🎯 После этого урока ты поймёшь
- Что такое форма и зачем она нужна
- Как форма отправляет данные на сервер
- Какие основные части у формы

## 📚 Теория

### Форма — это способ собрать данные от пользователя

Любое поле где пользователь вводит данные, нажимает кнопку и эти данные куда-то улетают — это **форма**:
- Регистрация на сайте
- Поиск товара
- Комментарий к посту
- Загрузка файла
- Опрос

### Главный тег — \`<form>\`

\`<form>\` — это **обёртка** всех полей формы. Без неё поля вроде есть, но «они сами по себе» — данные никуда не отправляются.

\`\`\`html
<form action="/api/login" method="post">
  <input type="text" name="email">
  <input type="password" name="password">
  <button>Войти</button>
</form>
\`\`\`

Атрибуты \`<form>\`:
- **\`action\`** — куда отправить данные (URL сервера)
- **\`method\`** — как отправить: \`get\` (данные в URL) или \`post\` (данные в теле запроса)

### Из чего состоит форма

- **\`<input>\`** — поля ввода: текст, пароль, email, число, чекбокс, радио, файл и т.д.
- **\`<textarea>\`** — большое поле для длинного текста
- **\`<select>\`** — выпадающий список
- **\`<button>\`** — кнопка отправки/отмены
- **\`<label>\`** — подпись к полю (что туда вводить)

### Что происходит когда нажимаешь «Отправить»

1. Браузер собирает значения всех полей формы
2. Упаковывает их (по правилам \`method\`)
3. Отправляет на адрес \`action\`
4. Сервер получает данные, что-то делает (например создаёт пользователя)
5. Сервер возвращает ответ (новую страницу или ошибку)

В этом курсе мы фокусируемся на **разметке** формы. Что произойдёт после отправки — это уже работа JavaScript и сервера.

## 💡 Проверь себя`,
  starterCode: "",
  solution: "",
  tests: [
    {
      input: "Зачем нужен тег <form>?",
      description: "Чтобы было красиво | Чтобы группировать поля и отправлять данные | Чтобы изменить шрифт полей | Это устаревший тег",
      expected: "Чтобы группировать поля и отправлять данные",
    },
    {
      input: "Что делает атрибут action у <form>?",
      description: "Запускает действие в браузере | Указывает URL куда отправить данные | Меняет цвет кнопки | Включает анимацию",
      expected: "Указывает URL куда отправить данные",
    },
    {
      input: "Какой тег используется для текстового поля ввода?",
      description: "<text> | <input> | <field> | <data>",
      expected: "<input>",
    },
    {
      input: "Что произойдёт если поле <input> поставить ВНЕ <form>?",
      description: "Поле не отобразится | Поле отобразится, но данные никуда не отправятся при сабмите | Браузер выдаст ошибку | Поле станет неактивным",
      expected: "Поле отобразится, но данные никуда не отправятся при сабмите",
    },
    {
      input: "Какие два значения у атрибута method?",
      description: "send и receive | get и post | open и close | yes и no",
      expected: "get и post",
    },
  ],
  hints: [
    "Форма — это сбор данных от пользователя. Главные части: <form> (обёртка), <input>/<textarea>/<select> (поля), <button> (кнопка).",
    "<form> имеет два главных атрибута: action (куда отправить) и method (как отправить).",
    "Полные ответы:\n1. Чтобы группировать поля и отправлять\n2. URL куда отправить\n3. <input>\n4. Отобразится, но не отправится\n5. get и post",
  ],
  isAvailable: true,
};

export default lesson;
