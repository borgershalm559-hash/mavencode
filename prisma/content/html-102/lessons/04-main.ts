import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "<main> — основное содержимое",
  order: 4,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# \`<main>\` — основное содержимое

## 🎯 После этого урока ты сможешь
- Помечать главную часть страницы тегом \`<main>\`
- Понимать почему он один на странице

## 📚 Теория

### Что такое \`<main>\`

\`<main>\` — это **основное уникальное содержимое страницы**. То зачем пользователь сюда пришёл.

На странице блога — это сами посты. На странице курса — теория и редактор. На странице товара — фото, цена, описание.

\`\`\`html
<header>...</header>
<main>
  <h1>Заголовок поста</h1>
  <p>Текст поста...</p>
</main>
<footer>...</footer>
\`\`\`

### Только один \`<main>\` на странице

В отличие от \`<header>\` (которых может быть несколько), \`<main>\` **один-единственный** на всю страницу. У страницы одна главная цель.

### Что НЕ попадает в \`<main>\`

- **Шапка** сайта (логотип, главное меню) — это \`<header>\` снаружи
- **Подвал** (копирайт, контакты, ссылки на соцсети) — это \`<footer>\` снаружи
- **Боковая колонка** с виджетами «похожие статьи», «реклама» — это \`<aside>\` (учим в L06)

\`<main>\` содержит **только то что отличает эту страницу от других страниц того же сайта**. Шапка и подвал на всех страницах одинаковые — они НЕ в \`<main>\`.

### Зачем

Скринридеры делают «**Перейти к основному содержимому**» — пропустить шапку и меню. Они прыгают именно к \`<main>\`. Без \`<main>\` пользователь каждый раз слышит навигацию сначала.

## 🛠️ Задание

Сделай страницу со структурой:

- \`<header>\` с \`<h1>Блог</h1>\`
- \`<main>\` с \`<h2>Первый пост</h2>\` и \`<p>Текст поста.</p>\`
- \`<footer>\` с \`<p>© 2026</p>\``,
  starterCode: `<!-- header / main / footer -->
`,
  solution: `<header>
  <h1>Блог</h1>
</header>
<main>
  <h2>Первый пост</h2>
  <p>Текст поста.</p>
</main>
<footer>
  <p>© 2026</p>
</footer>`,
  tests: [
    { kind: "count", selector: "main", n: 1, description: "Ровно один <main>" },
    { kind: "exists", selector: "header h1", description: "В <header> есть <h1>" },
    { kind: "text", selector: "main h2", equals: "Первый пост", description: 'В <main> есть <h2> "Первый пост"' },
    { kind: "exists", selector: "footer p", description: "В <footer> есть параграф" },
    { kind: "textContains", selector: "footer", contains: "2026", description: 'В <footer> есть "2026"' },
  ],
  hints: [
    "Три блока подряд: <header>, <main>, <footer>. Каждый со своим содержимым внутри.",
    "Шаблон: <header>...</header><main>...</main><footer>...</footer>",
    'Полное решение:\n```html\n<header><h1>Блог</h1></header>\n<main>\n  <h2>Первый пост</h2>\n  <p>Текст поста.</p>\n</main>\n<footer><p>© 2026</p></footer>\n```',
  ],
  isAvailable: true,
};

export default lesson;
