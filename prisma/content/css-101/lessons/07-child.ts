import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Дочерний селектор (a > b)",
  order: 7,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Дочерний селектор (\`a > b\`)

## 🎯 После этого урока ты сможешь
- Стилизовать только прямых потомков
- Различать «дочерний» и «потомок»

## 📚 Теория

### «Дочерний» — это прямо внутри

В прошлом уроке селектор-**потомок** \`a b\` находил \`b\` на **любой глубине** внутри \`a\`. Селектор-**дочерний** \`a > b\` находит **только прямых детей**, не глубже.

\`\`\`html
<ul>
  <li>Прямой ребёнок</li>
  <li>Прямой ребёнок
    <ul>
      <li>Не прямой — внук</li>
    </ul>
  </li>
</ul>
\`\`\`

### Знак \`>\` — родитель и ребёнок

\`\`\`css
ul > li {
  color: red;
}
\`\`\`

Только **прямые** \`<li>\` родительского \`<ul>\` станут красными. Вложенный \`<li>\` (внук) — нет.

### Когда использовать

Нужно стилизовать **верхний уровень** иерархии и не трогать вложенные. Типичный пример — главное меню:

\`\`\`css
nav > ul > li {
  background: gray;
}
\`\`\`

«Главные пункты меню — серые, подменю не трогаем».

### \`a b\` vs \`a > b\`

| Селектор | Что находит |
|----------|-------------|
| \`ul li\`  | **Все** \`<li>\` внутри \`<ul>\` (на любой глубине) |
| \`ul > li\` | Только **прямые дети** \`<ul>\` (один уровень) |

## 🛠️ Задание

Сделай вложенный список:

\`\`\`html
<ul>
  <li>Главный 1</li>
  <li>Главный 2
    <ul>
      <li>Под-пункт</li>
    </ul>
  </li>
</ul>
\`\`\`

Стилизуй **только прямых детей внешнего \`<ul>\`** — цвет \`blue\`. Вложенный \`<li>\` (под-пункт) не должен изменить цвет.`,
  starterCode: `<!doctype html>
<html>
<head>
  <style>
    /* ul > li */
  </style>
</head>
<body>
  <!-- Вложенный список -->
</body>
</html>
`,
  solution: `<!doctype html>
<html>
<head>
  <style>
    ul > li { color: blue; }
  </style>
</head>
<body>
  <ul>
    <li>Главный 1</li>
    <li>Главный 2
      <ul>
        <li>Под-пункт</li>
      </ul>
    </li>
  </ul>
</body>
</html>`,
  tests: [
    { kind: "count", selector: "ul", n: 2, description: "Два <ul> (внешний и вложенный)" },
    { kind: "count", selector: "ul li", n: 3, description: "Три <li> всего" },
    { kind: "exists", selector: "ul > li > ul", description: "Внутри <li> есть вложенный <ul>" },
  ],
  hints: [
    "Селектор «ul > li» — только прямые дети ul, не внуки.",
    "Шаблон CSS: ul > li { color: blue; }",
    "Полное решение — см. в задании выше.",
  ],
  isAvailable: true,
};

export default lesson;
