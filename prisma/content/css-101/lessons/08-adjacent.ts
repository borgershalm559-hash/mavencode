import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Соседние селекторы (a + b, a ~ b)",
  order: 8,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Соседние селекторы (\`a + b\`, \`a ~ b\`)

## 🎯 После этого урока ты сможешь
- Стилизовать элемент **сразу после** другого
- Стилизовать **всех соседей** после другого

## 📚 Теория

### \`a + b\` — соседний (прямой следующий)

\`a + b\` находит **\`b\` который идёт сразу после \`a\`** на одном уровне:

\`\`\`html
<h1>Заголовок</h1>
<p>Сразу после h1 — этот p</p>
<p>Этот уже не "сразу после"</p>
\`\`\`

\`\`\`css
h1 + p { color: red; }
\`\`\`

Покрасит **только первый** \`<p>\` после \`<h1>\`. Второй — нет.

### \`a ~ b\` — общий брат

\`a ~ b\` находит **всех \`b\` после \`a\`** на одном уровне:

\`\`\`css
h1 ~ p { color: red; }
\`\`\`

Покрасит **все** \`<p>\` идущие после \`<h1>\` (но только на том же уровне иерархии).

### \`+\` vs \`~\`

| | Что находит |
|---|---|
| \`a + b\` | Один: **сразу следующий** \`b\` после \`a\` |
| \`a ~ b\` | **Все** \`b\` после \`a\` |

### Когда полезно

- \`h2 + p\` — первый абзац после подзаголовка часто хочется выделить (lead-абзац)
- \`label + input\` — стиль для инпута сразу после своей подписи

## 🛠️ Задание

Сделай разметку:

\`\`\`html
<h1>Заголовок</h1>
<p>Первый параграф</p>
<p>Второй параграф</p>
\`\`\`

Стилизуй **только первый** \`<p>\` после \`<h1>\` — цвет \`red\`. Второй параграф НЕ должен покраснеть.`,
  starterCode: `<!doctype html>
<html>
<head>
  <style>
    /* h1 + p */
  </style>
</head>
<body>
  <!-- h1 + два p -->
</body>
</html>
`,
  solution: `<!doctype html>
<html>
<head>
  <style>
    h1 + p { color: red; }
  </style>
</head>
<body>
  <h1>Заголовок</h1>
  <p>Первый параграф</p>
  <p>Второй параграф</p>
</body>
</html>`,
  tests: [
    { kind: "count", selector: "p", n: 2, description: "Два параграфа" },
    { kind: "exists", selector: "h1", description: "Есть <h1>" },
  ],
  hints: [
    "Селектор «h1 + p» — только первый p сразу после h1. Один знак плюс.",
    "Шаблон CSS: h1 + p { color: red; }",
    "Полное решение — см. в задании выше.",
  ],
  isAvailable: true,
};

export default lesson;
