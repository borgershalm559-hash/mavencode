import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Каскад — что побеждает",
  order: 11,
  type: "quiz",
  language: "html",
  xpReward: 50,
  content: `# Каскад — что побеждает

## 🎯 После этого урока ты будешь знать
- Что значит «каскад» в CSS
- Как браузер выбирает между конфликтующими правилами

## 📚 Теория

### Когда правил много

В CSS на один элемент часто действует **несколько** правил одновременно:

\`\`\`css
p { color: black; }
.warning { color: red; }
\`\`\`

\`\`\`html
<p class="warning">Текст</p>
\`\`\`

Какой цвет победит — \`black\` или \`red\`?

### Три основных принципа

Браузер решает в таком порядке:

**1. Происхождение и важность.** Сначала — стили автора (твой CSS), потом — стили браузера по умолчанию. \`!important\` побеждает всё, но используется редко.

**2. Специфичность.** Более **конкретный** селектор побеждает более общего. Это самое важное правило, разбираем в L12.

**3. Порядок объявления.** Если специфичность одинаковая — побеждает то правило, которое **ниже** в коде.

### Пример

\`\`\`css
p { color: black; }              /* специфичность 0,0,0,1 */
.warning { color: red; }         /* специфичность 0,0,1,0 — больше */
\`\`\`

\`<p class="warning">\` будет **красным** — \`.warning\` побеждает по специфичности.

\`\`\`css
p { color: blue; }   /* первое */
p { color: green; }  /* второе */
\`\`\`

Здесь специфичность одинаковая, побеждает **green** (стоит ниже).

## 💡 Проверь себя`,
  starterCode: "",
  solution: "",
  tests: [
    { input: "Что значит «каскад» в CSS?", description: "Стили падают сверху | Правила накладываются друг на друга, при конфликте есть правила выбора | Анимации плавно переходят | Это про скорость", expected: "Правила накладываются друг на друга, при конфликте есть правила выбора" },
    { input: "Что побеждает: p { color: black; } или .warning { color: red; } для <p class=\"warning\">?", description: "p — он первый | .warning — у класса выше специфичность чем у тега | Выберет браузер случайно | Обе равны, цвет смешается", expected: ".warning — у класса выше специфичность чем у тега" },
    { input: "Если специфичность одинаковая, что побеждает?", description: "Первое объявление | Последнее объявление в коде | Случайное | Браузер берёт стили по умолчанию", expected: "Последнее объявление в коде" },
    { input: "Что делает !important?", description: "Делает шрифт жирным | Помечает правило как важное и побеждает почти всё | Запускает анимацию | Печатает текст", expected: "Помечает правило как важное и побеждает почти всё" },
    { input: "Почему !important используется редко?", description: "Это устаревший синтаксис | Он замедляет страницу | Ломает обычные правила специфичности — каскад становится непредсказуемым | Он не поддерживается браузерами", expected: "Ломает обычные правила специфичности — каскад становится непредсказуемым" },
  ],
  hints: [
    "Каскад = три уровня выбора: происхождение/важность → специфичность → порядок.",
    "У класса специфичность выше чем у тега. У id — выше чем у класса. !important побеждает всё.",
    "Полные ответы:\n1. Правила накладываются, есть правила выбора\n2. .warning выше по специфичности\n3. Последнее в коде\n4. Помечает как важное, побеждает почти всё\n5. Ломает каскад, делает его непредсказуемым",
  ],
  isAvailable: true,
};

export default lesson;
