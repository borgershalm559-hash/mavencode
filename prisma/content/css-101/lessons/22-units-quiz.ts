import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Юниты viewport: vw, vh, ch и юнит fr",
  order: 22,
  type: "quiz",
  language: "html",
  xpReward: 50,
  content: `# Юниты viewport: \`vw\`, \`vh\`, \`ch\` и юнит \`fr\`

## 🎯 После этого урока ты будешь знать
- Что такое \`vw\`, \`vh\`, \`ch\`
- Где встречается юнит \`fr\`

## 📚 Теория

### \`vw\` и \`vh\` — относительно окна браузера

- **\`vw\`** (viewport width) — 1% **ширины окна** браузера. \`100vw\` = вся ширина.
- **\`vh\`** (viewport height) — 1% **высоты окна** браузера. \`100vh\` = вся высота.

\`\`\`css
.hero {
  height: 100vh;       /* во всю высоту экрана */
  width: 100vw;        /* во всю ширину */
}

.title {
  font-size: 5vw;      /* шрифт 5% ширины — масштабируется с экраном */
}
\`\`\`

Удобно для:
- Полноэкранных секций (hero блок)
- Адаптивных шрифтов (но осторожно — на маленьких экранах текст становится слишком мелким)

### \`ch\` — ширина символа «0»

**\`1ch\`** = ширина символа «0» в текущем шрифте. Удобно для **читабельной ширины** текста:

\`\`\`css
article {
  max-width: 65ch;     /* ~65 символов в строке — оптимально для чтения */
}
\`\`\`

Проверенный диапазон для комфортного чтения — **45-75 символов в строке**, поэтому \`65ch\` — частый выбор.

### \`fr\` — fraction (доля) для CSS Grid

\`fr\` появляется только в **CSS Grid** (учим в CSS 102). Это «доля свободного пространства»:

\`\`\`css
.layout {
  display: grid;
  grid-template-columns: 1fr 2fr 1fr;   /* три колонки в пропорции 1:2:1 */
}
\`\`\`

Запомни \`fr\` сейчас, чтобы потом не удивляться. Подробно — в курсе CSS 102.

## 💡 Проверь себя`,
  starterCode: "",
  solution: "",
  tests: [
    { input: "Что значит 100vh?", description: "100 пикселей высоты | 100% высоты окна браузера | 100% высоты экрана | 100% высоты документа", expected: "100% высоты окна браузера" },
    { input: "Чему равен 1ch?", description: "1 символу высоты | Ширине символа \"0\" в текущем шрифте | 1 пикселю | 1 строке", expected: "Ширине символа \"0\" в текущем шрифте" },
    { input: "Где используется юнит fr?", description: "В CSS Grid — для распределения свободного пространства | Для размера шрифта | Для прозрачности | Для границ", expected: "В CSS Grid — для распределения свободного пространства" },
    { input: "Какой юнит лучше для адаптивного hero-блока на всю высоту экрана?", description: "100% | 100vh | 1000px | 100rem", expected: "100vh" },
    { input: "Какой юнит ставить на max-width абзаца для комфортного чтения (~65 символов)?", description: "65px | 65vw | 65ch | 65rem", expected: "65ch" },
  ],
  hints: [
    "vw/vh — viewport (окно браузера). ch — ширина символа \"0\". fr — доля для grid.",
    "Полные ответы:\n1. 100% высоты окна\n2. Ширина символа \"0\"\n3. CSS Grid\n4. 100vh\n5. 65ch",
  ],
  isAvailable: true,
};

export default lesson;
