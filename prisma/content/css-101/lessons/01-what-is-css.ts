import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Что такое CSS",
  order: 1,
  type: "quiz",
  language: "html",
  xpReward: 50,
  content: `# Что такое CSS

## 🎯 После этого урока ты поймёшь
- Что делает CSS и чем отличается от HTML
- Как устроено CSS-правило
- Что такое селектор, свойство и значение

## 📚 Теория

### CSS отвечает за внешний вид

В HTML 101 ты строил **скелет** страницы: заголовки, параграфы, картинки, списки. Но всё выглядело голо — белый фон, чёрный текст, размеры по умолчанию.

**CSS** (Cascading Style Sheets — каскадные таблицы стилей) — это язык **оформления**. Он говорит браузеру:
- Какого цвета фон
- Каким шрифтом текст
- Где какой отступ
- Сколько пикселей высота
- Что подсвечивается на наведении

### Анатомия CSS-правила

\`\`\`css
h1 {
  color: red;
  font-size: 32px;
}
\`\`\`

Разберём:
- **\`h1\`** — **селектор** (что стилизуем). В этом случае — все теги \`<h1>\`.
- **\`{ ... }\`** — фигурные скобки, тело правила.
- **\`color: red;\`** — одно объявление: **свойство: значение;**
- **\`color\`** — что меняем (цвет текста)
- **\`red\`** — на что меняем (красный)
- **точка с запятой** в конце каждого объявления.

Можно поставить много объявлений в одном правиле — каждое со своим свойством и значением.

### Селекторов много

\`h1\` — это самый простой селектор, по тегу. В CSS их десятки видов:
- по классу (\`.button\`)
- по id (\`#header\`)
- по атрибуту (\`[type="text"]\`)
- по состоянию (\`a:hover\`)
- по позиции (\`li:first-child\`)

Все будем разбирать в курсе.

### Каскад

Слово **«Cascading»** в названии означает что правила **накладываются друг на друга**. Если на одном элементе работают несколько правил, они **объединяются**. Если конфликт — побеждает по правилам специфичности (учим в L11-12).

## 💡 Проверь себя`,
  starterCode: "",
  solution: "",
  tests: [
    { input: "Что отвечает за внешний вид страницы?", description: "HTML | CSS | JavaScript | Браузер сам", expected: "CSS" },
    { input: "В правиле h1 { color: red; } что такое 'h1'?", description: "Свойство | Значение | Селектор | Класс", expected: "Селектор" },
    { input: "В правиле h1 { color: red; } что такое 'color'?", description: "Селектор | Свойство | Значение | Тег", expected: "Свойство" },
    { input: "Что разделяет свойство и значение в CSS?", description: "Знак = | Двоеточие : | Равно == | Точка .", expected: "Двоеточие :" },
    { input: "Что значит 'Cascading' в CSS?", description: "Правила прыгают между страницами | Правила накладываются друг на друга, при конфликте побеждает более конкретное | CSS быстро каскадно рендерится | Это просто красивое слово", expected: "Правила накладываются друг на друга, при конфликте побеждает более конкретное" },
  ],
  hints: [
    "CSS = оформление. HTML = структура. CSS-правило = селектор + { свойство: значение; }",
    "Запомни шаблон: селектор { свойство: значение; }",
    "Полные ответы:\n1. CSS\n2. Селектор\n3. Свойство\n4. Двоеточие :\n5. Правила накладываются, побеждает более конкретное",
  ],
  isAvailable: true,
};

export default lesson;
