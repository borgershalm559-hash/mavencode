import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Ссылки <a href>",
  order: 11,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Ссылки \`<a href>\`

## 🎯 После этого урока ты сможешь
- Делать кликабельные ссылки
- Указывать куда ведёт ссылка
- Понимать что такое URL

## 🔍 Зачем это нужно
Интернет в первую очередь работает на **ссылках**. Любая страница ссылается на другие — это и делает «всемирную паутину». Ты переходишь по ссылке — открывается новая страница.

## 📚 Теория

### Ссылка — это тег \`<a>\`

\`<a>\` (от **anchor** — якорь) — это тег ссылки. Сам по себе \`<a>\` ничего не делает. Чтобы он стал кликабельной ссылкой, ему нужен атрибут **href** — путь куда вести.

\`\`\`html
<a href="https://google.com">Открыть Google</a>
\`\`\`

Что здесь:
- \`<a>\` — тег ссылки
- \`href="https://google.com"\` — атрибут с **URL** (адрес сайта куда ведёт)
- \`Открыть Google\` — текст который видит читатель и кликает
- \`</a>\` — закрывающий тег

### Что такое URL

**URL** — это полный адрес страницы в интернете. Например \`https://ya.ru\`, \`https://github.com/anthropics\`, \`https://wikipedia.org/wiki/HTML\`. Браузер по этому адресу находит и загружает страницу.

URL почти всегда начинается с \`https://\` или \`http://\`. Без этого префикса браузер не поймёт что это внешний адрес.

### Текст ссылки — что писать

В \`<a>...</a>\` пиши **осмысленный текст**, который описывает куда ведёт ссылка.

\`\`\`html
<!-- ✓ хорошо -->
<a href="https://wikipedia.org">Энциклопедия Wikipedia</a>

<!-- ✗ плохо -->
<a href="https://wikipedia.org">здесь</a>
<a href="https://wikipedia.org">кликни сюда</a>
\`\`\`

«Кликни сюда» — это плохо: вне контекста непонятно куда ссылка ведёт. Особенно для людей со скринридером, которые слышат список всех ссылок страницы — у них на экране будет «кликни сюда, кликни сюда, кликни сюда» — бесполезно.

## ⚠️ Частые ошибки

1. **Забыли \`href\`**: \`<a>Google</a>\` — это просто текст, не ссылка. Без \`href\` — некуда вести.
2. **Забыли \`https://\`** в URL: \`href="google.com"\` — браузер подумает что это **относительная** ссылка на твоём сайте, а не внешний адрес.
3. **Кавычки разные**: \`href="https://...'\` — открыли двойной, закрыли одинарной.

## 🛠️ Задание

Сделай параграф со ссылкой:

\`Подробнее на сайте Wikipedia.\`

Слово **Wikipedia** в этом тексте сделай ссылкой на \`https://wikipedia.org\`.`,
  starterCode: `<!-- Параграф со ссылкой ниже -->
`,
  solution: `<p>Подробнее на сайте <a href="https://wikipedia.org">Wikipedia</a>.</p>`,
  tests: [
    { kind: "exists", selector: "p", description: "Есть параграф" },
    { kind: "exists", selector: "p a", description: "Внутри параграфа есть ссылка <a>" },
    { kind: "attr", selector: "a", name: "href", equals: "https://wikipedia.org", description: 'Ссылка ведёт на https://wikipedia.org' },
    { kind: "text", selector: "a", equals: "Wikipedia", description: 'Текст ссылки — "Wikipedia"' },
  ],
  hints: [
    "Внутри <p> ставишь <a href=\"...\">текст</a> вокруг слова, которое должно стать ссылкой. Остальной текст параграфа остаётся как есть.",
    "Шаблон: <p>...текст до <a href=\"https://...\">слово</a> текст после.</p>",
    'Полное решение:\n```html\n<p>Подробнее на сайте <a href="https://wikipedia.org">Wikipedia</a>.</p>\n```',
  ],
  isAvailable: true,
};

export default lesson;
