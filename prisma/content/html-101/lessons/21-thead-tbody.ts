import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "<thead>, <tbody>, <th> — заголовки таблицы",
  order: 21,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# \`<thead>\`, \`<tbody>\`, \`<th>\` — заголовки таблицы

## 🎯 После этого урока ты сможешь
- Отделять заголовки таблицы от данных
- Использовать \`<th>\` вместо \`<td>\` для шапки

## 📚 Теория

### Зачем разделять заголовки и данные

В прошлом уроке у нас была таблица где первая строка — это «День, Время» (заголовки колонок), а остальные строки — данные. Браузер не понимает разницы — для него все строки одинаковые.

Чтобы пометить «вот это шапка» используют:
- **\`<thead>\`** — обёртка для строки/строк-заголовков
- **\`<tbody>\`** — обёртка для строк с данными
- **\`<th>\`** (от **table header** — заголовок таблицы) — ячейка-заголовок (вместо \`<td>\`)

\`\`\`html
<table>
  <thead>
    <tr>
      <th>День</th>
      <th>Время</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Пн</td>
      <td>10:00</td>
    </tr>
    <tr>
      <td>Вт</td>
      <td>11:00</td>
    </tr>
  </tbody>
</table>
\`\`\`

### Что меняется

- Браузер автоматически делает текст в \`<th>\` **жирным** и центрирует.
- Скринридер при чтении ячейки данных может **озвучить заголовок столбца** — «День: Пн, Время: 10:00». Без \`<th>\` он не знает что это заголовок.
- Поисковики лучше понимают структуру.

### Когда нужно

Если в таблице есть **заголовки колонок или строк** — используй \`<th>\`. Это почти всегда — расписание, прайс, сравнение.

Если таблица «голая» — без шапки — можно обойтись простым \`<tr><td>\` без \`<thead>\`. Но такие таблицы редкие.

## ⚠️ Частые ошибки

1. **Заголовки — обычные \`<td>\`**: \`<tr><td>День</td><td>Время</td></tr>\`. Работает но некорректно. Скринридер не поймёт что это шапка.
2. **\`<th>\` вне \`<thead>\`**: можно, но непривычно. Лучше держать заголовочную строку в \`<thead>\`, остальное — в \`<tbody>\`.
3. **Забыли \`<tbody>\`** обернуть данные. Браузер сам добавит, но лучше написать явно.

## 🛠️ Задание

Сделай ту же таблицу что в прошлом уроке, но с правильной структурой:

- \`<thead>\` со строкой \`<th>День</th><th>Время</th>\`
- \`<tbody>\` со строкой \`<td>Пн</td><td>10:00</td>\``,
  starterCode: `<!-- Таблица с thead/tbody/th -->
`,
  solution: `<table>
  <thead>
    <tr>
      <th>День</th>
      <th>Время</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Пн</td>
      <td>10:00</td>
    </tr>
  </tbody>
</table>`,
  tests: [
    { kind: "exists", selector: "table thead", description: "Есть <thead>" },
    { kind: "exists", selector: "table tbody", description: "Есть <tbody>" },
    { kind: "count", selector: "thead th", n: 2, description: "В <thead> две ячейки <th>" },
    { kind: "count", selector: "tbody td", n: 2, description: "В <tbody> две ячейки <td>" },
    { kind: "textContains", selector: "thead", contains: "День", description: 'В шапке есть "День"' },
    { kind: "textContains", selector: "tbody", contains: "10:00", description: 'В данных есть "10:00"' },
  ],
  hints: [
    "Сначала <thead> с одной <tr> и двумя <th>. Потом <tbody> с одной <tr> и двумя <td>. Всё внутри <table>.",
    "В <thead> используются <th>, в <tbody> — <td>. Это разные теги.",
    'Полное решение:\n```html\n<table>\n  <thead>\n    <tr><th>День</th><th>Время</th></tr>\n  </thead>\n  <tbody>\n    <tr><td>Пн</td><td>10:00</td></tr>\n  </tbody>\n</table>\n```',
  ],
  isAvailable: true,
};

export default lesson;
