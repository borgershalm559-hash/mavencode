import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Параграф <p>",
  order: 6,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Параграф \`<p>\`

## 🎯 После этого урока ты сможешь
- Создавать абзацы текста
- Писать несколько абзацев подряд

## 🔍 Зачем это нужно
В книге текст разбит на **абзацы** — между ними пустая строка для удобства чтения. На веб-странице абзац — это тег \`<p>\` (от слова **paragraph**).

## 📚 Теория

### Что такое \`<p>\`

\`<p>\` — обычный абзац обычного текста. Браузер автоматически делает между параграфами **отступ сверху и снизу**, поэтому текст хорошо читается.

\`\`\`html
<p>Это первый абзац. Может быть очень длинным.</p>
<p>А это уже второй абзац — отдельная мысль, отдельная коробка.</p>
\`\`\`

### Перенос строки внутри \`<p>\`

Если ты в коде нажмёшь Enter и продолжишь писать — браузер **не** перенесёт строку. Все пробелы и переносы внутри HTML «схлопываются» в один пробел:

\`\`\`html
<p>Этот текст
   написан
       на нескольких строках</p>
\`\`\`

Браузер покажет это как одну строку: \`Этот текст написан на нескольких строках\`. Если нужен реальный перенос — используем \`<br>\` (учим в следующем уроке).

### Несколько параграфов = несколько \`<p>\`

\`\`\`html
<p>Первый абзац.</p>
<p>Второй абзац.</p>
<p>Третий абзац.</p>
\`\`\`

Не нужно один \`<p>\` с переносами — нужно три отдельных \`<p>\`.

## ⚠️ Частые ошибки

1. **Один большой \`<p>\` со всем текстом**. Правильно — каждая мысль в своём \`<p>\`.
2. **Не закрыть \`<p>\`**: \`<p>Текст<p>Ещё текст</p>\`. Браузер постарается, но лучше всегда закрывать: \`<p>Текст</p><p>Ещё текст</p>\`.

## 🛠️ Задание

Сделай страничку из заголовка и **двух** параграфов:
- \`<h1>\` — \`Привет\`
- \`<p>\` — \`Это моя первая страница.\`
- \`<p>\` — \`Я учу HTML.\``,
  starterCode: `<!-- Заголовок и два параграфа ниже -->
`,
  solution: `<h1>Привет</h1>
<p>Это моя первая страница.</p>
<p>Я учу HTML.</p>`,
  tests: [
    { kind: "exists", selector: "h1", description: "Есть <h1>" },
    { kind: "text", selector: "h1", equals: "Привет", description: '<h1> содержит "Привет"' },
    { kind: "count", selector: "p", n: 2, description: "Ровно два параграфа <p>" },
    { kind: "textContains", selector: "p", contains: "Это моя первая страница", description: 'Первый параграф содержит "Это моя первая страница"' },
  ],
  hints: [
    "Тебе нужно три тега подряд: один <h1> и два <p>.",
    "Шаблон <p>: <p>Какой-то текст.</p>. Каждый параграф — на своей строке.",
    'Полное решение:\n```html\n<h1>Привет</h1>\n<p>Это моя первая страница.</p>\n<p>Я учу HTML.</p>\n```',
  ],
  isAvailable: true,
};

export default lesson;
