import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Таблица: <table>, <tr>, <td>",
  order: 20,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Таблица: \`<table>\`, \`<tr>\`, \`<td>\`

## 🎯 После этого урока ты сможешь
- Делать простые таблицы из строк и колонок
- Понимать роль каждого тега

## 📚 Теория

### Таблица — это строки и ячейки

В HTML таблица строится из трёх обязательных тегов:

- **\`<table>\`** — внешняя обёртка всей таблицы
- **\`<tr>\`** (от **table row** — строка таблицы) — одна строка
- **\`<td>\`** (от **table data** — ячейка данных) — одна ячейка в строке

\`\`\`html
<table>
  <tr>
    <td>1A</td>
    <td>1B</td>
    <td>1C</td>
  </tr>
  <tr>
    <td>2A</td>
    <td>2B</td>
    <td>2C</td>
  </tr>
</table>
\`\`\`

Получится таблица 2×3 — две строки, три колонки. Браузер по умолчанию **не рисует границы** — таблица выглядит как простой текст. Для границ нужен CSS (учим в CSS 101).

### Сколько колонок?

Количество колонок определяется числом \`<td>\` **в одной \`<tr>\`**. Если в одной строке 3 ячейки, а в другой — 4, то у тебя «кривая» таблица.

\`\`\`html
<!-- ✓ правильно: одинаково 3 ячейки в каждой строке -->
<table>
  <tr><td>A</td><td>B</td><td>C</td></tr>
  <tr><td>1</td><td>2</td><td>3</td></tr>
</table>

<!-- ✗ криво: разное число ячеек -->
<table>
  <tr><td>A</td><td>B</td><td>C</td></tr>
  <tr><td>1</td><td>2</td></tr>           <!-- одной не хватает -->
</table>
\`\`\`

### Когда нужна таблица

**Таблица — для табличных данных**: расписание, прайс-лист, сравнение характеристик. Если у тебя в голове «строки и колонки» — это таблица.

**Не использовать таблицу для верстки страницы** (для расположения блоков). Это устаревший подход 2000-х. Для верстки есть CSS Grid и Flexbox (учим в CSS 102).

## ⚠️ Частые ошибки

1. **Забыли \`<tr>\`** и пишут сразу \`<td>\`: \`<table><td>A</td></table>\`. Без строки ячейка не отобразится правильно.
2. **Разное число ячеек** в строках. Таблица «съезжает».
3. **Используют \`<table>\` для верстки макета.** Для этого CSS — не таблицы.

## 🛠️ Задание

Сделай таблицу 2×2 с расписанием:

| День  | Время |
|-------|-------|
| Пн    | 10:00 |

То есть две строки, в каждой по две ячейки. Первая строка: \`День\` и \`Время\`. Вторая строка: \`Пн\` и \`10:00\`.`,
  starterCode: `<!-- Таблица 2x2 ниже -->
`,
  solution: `<table>
  <tr>
    <td>День</td>
    <td>Время</td>
  </tr>
  <tr>
    <td>Пн</td>
    <td>10:00</td>
  </tr>
</table>`,
  tests: [
    { kind: "exists", selector: "table", description: "Есть <table>" },
    { kind: "count", selector: "table tr", n: 2, description: "В таблице две строки <tr>" },
    { kind: "count", selector: "table td", n: 4, description: "В таблице ровно 4 ячейки <td> (2×2)" },
    { kind: "textContains", selector: "table", contains: "День", description: 'В таблице есть "День"' },
    { kind: "textContains", selector: "table", contains: "10:00", description: 'В таблице есть "10:00"' },
  ],
  hints: [
    "Структура: <table><tr><td>...</td><td>...</td></tr><tr><td>...</td><td>...</td></tr></table>. Две <tr>, в каждой по две <td>.",
    "Каждая <tr> — строка. Внутри неё ставишь нужное число <td> (по одной на каждую колонку).",
    'Полное решение:\n```html\n<table>\n  <tr><td>День</td><td>Время</td></tr>\n  <tr><td>Пн</td><td>10:00</td></tr>\n</table>\n```',
  ],
  isAvailable: true,
};

export default lesson;
