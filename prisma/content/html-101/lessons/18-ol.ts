import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Нумерованный список <ol>",
  order: 18,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Нумерованный список \`<ol>\`

## 🎯 После этого урока ты сможешь
- Делать пронумерованные списки
- Понимать когда \`<ol>\`, а когда \`<ul>\`

## 📚 Теория

### \`<ol>\` — список с порядком

\`<ol>\` (от **ordered list** — упорядоченный список) работает **точно так же как \`<ul>\`**, но с двумя отличиями:

1. Браузер ставит **цифры** (1, 2, 3...) вместо точек.
2. **Порядок имеет значение** — это шаги, рейтинг, очерёдность.

\`\`\`html
<ol>
  <li>Налить воду в кастрюлю</li>
  <li>Поставить на огонь</li>
  <li>Дождаться кипения</li>
</ol>
\`\`\`

Внутри — те же \`<li>\` что и в \`<ul>\`.

### Когда \`<ol>\`, когда \`<ul>\`

**Используй \`<ol>\` если порядок важен:**
- Шаги рецепта
- Инструкция по сборке
- Рейтинг (топ-10)
- Хронология событий

**Используй \`<ul>\` если порядок не важен:**
- Список ингредиентов («можно положить в любом порядке»)
- Список тегов
- Меню навигации
- Перечень функций

### Атрибуты \`<ol>\`

У \`<ol>\` есть пара полезных атрибутов:

- \`start="5"\` — начать нумерацию не с 1, а с 5
- \`reversed\` — нумеровать в обратном порядке (3, 2, 1)
- \`type="A"\` — буквы вместо цифр (\`A\`, \`B\`, \`C\` или \`a\`, \`b\` или \`I\`, \`II\`, \`III\`)

\`\`\`html
<ol type="A">
  <li>Первый</li>   <!-- A -->
  <li>Второй</li>   <!-- B -->
  <li>Третий</li>   <!-- C -->
</ol>
\`\`\`

В этом уроке достаточно простого \`<ol>\`.

## ⚠️ Частые ошибки

1. **Используют \`<ol>\`** там где порядок не важен. Список ингредиентов — это \`<ul>\`. Шаги — \`<ol>\`.
2. **Цифры вручную в \`<ul>\`**: \`<ul><li>1. Молоко</li><li>2. Хлеб</li></ul>\`. Не делай — для нумерации есть \`<ol>\`.

## 🛠️ Задание

Сделай шаги рецепта из трёх пунктов:

1. Нарезать овощи
2. Обжарить лук с морковью
3. Залить водой и варить 30 минут`,
  starterCode: `<!-- Нумерованный список из трёх шагов -->
`,
  solution: `<ol>
  <li>Нарезать овощи</li>
  <li>Обжарить лук с морковью</li>
  <li>Залить водой и варить 30 минут</li>
</ol>`,
  tests: [
    { kind: "exists", selector: "ol", description: "Есть <ol>" },
    { kind: "count", selector: "ol li", n: 3, description: "Внутри <ol> ровно три <li>" },
    { kind: "textContains", selector: "ol", contains: "Нарезать овощи", description: 'Первый шаг — "Нарезать овощи"' },
    { kind: "textContains", selector: "ol", contains: "Обжарить лук", description: 'Второй шаг — про лук' },
    { kind: "textContains", selector: "ol", contains: "Залить водой", description: 'Третий шаг — "Залить водой"' },
  ],
  hints: [
    "<ol> с тремя <li> внутри. Структура та же что у <ul>, просто другая буква.",
    "Шаблон: <ol><li>шаг 1</li><li>шаг 2</li><li>шаг 3</li></ol>.",
    'Полное решение:\n```html\n<ol>\n  <li>Нарезать овощи</li>\n  <li>Обжарить лук с морковью</li>\n  <li>Залить водой и варить 30 минут</li>\n</ol>\n```',
  ],
  isAvailable: true,
};

export default lesson;
