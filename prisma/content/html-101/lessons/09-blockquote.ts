import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Цитаты <blockquote>",
  order: 9,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Цитаты \`<blockquote>\`

## 🎯 После этого урока ты сможешь
- Оформлять блок-цитаты
- Понимать когда уместен этот тег

## 📚 Теория

### Что такое \`<blockquote>\`

\`<blockquote>\` (от **block quote** — блочная цитата) — это **длинная цитата из другого источника**. Браузер по умолчанию делает отступ слева, чтобы было видно «здесь не наш текст, это чьи-то слова».

\`\`\`html
<blockquote>
  <p>Я не боюсь компьютеров. Я боюсь их отсутствия.</p>
  <p>— Айзек Азимов</p>
</blockquote>
\`\`\`

Цитата визуально отделится — обычно отступом и иногда вертикальной чертой слева.

### Когда использовать

- Длинная цитата из книги, статьи, интервью.
- Слова другого человека, которые ты приводишь дословно.

Для **коротких цитат внутри предложения** есть отдельный тег \`<q>\`. Например:

\`\`\`html
<p>Эйнштейн говорил <q>Воображение важнее знания</q>.</p>
\`\`\`

Браузер автоматически добавит кавычки вокруг \`<q>\`. Но в этом курсе мы используем только \`<blockquote>\`.

## ⚠️ Частые ошибки

1. **Использовать \`<blockquote>\` для отступа.** «Хочу чтобы текст был с отступом слева» → \`<blockquote>\`. Нет — это смысловой тег для цитат. Для отступа есть CSS.
2. **Не указать автора.** Хорошая цитата всегда подписана — кто это сказал.
3. **Голый текст внутри \`<blockquote>\`** без \`<p>\`. Технически работает, но правильнее обернуть текст цитаты в \`<p>\`.

## 🛠️ Задание

Сделай страничку с цитатой Стива Джобса:

- \`<h1>\` — \`Цитата дня\`
- \`<blockquote>\` с двумя \`<p>\` внутри:
  - первый \`<p>\` — текст цитаты: \`Оставайтесь голодными. Оставайтесь безрассудными.\`
  - второй \`<p>\` — автор: \`— Стив Джобс\``,
  starterCode: `<!-- Заголовок и цитата ниже -->
`,
  solution: `<h1>Цитата дня</h1>
<blockquote>
  <p>Оставайтесь голодными. Оставайтесь безрассудными.</p>
  <p>— Стив Джобс</p>
</blockquote>`,
  tests: [
    { kind: "text", selector: "h1", equals: "Цитата дня", description: '<h1> содержит "Цитата дня"' },
    { kind: "exists", selector: "blockquote", description: "Есть тег <blockquote>" },
    { kind: "count", selector: "blockquote p", n: 2, description: "Внутри <blockquote> два параграфа" },
    { kind: "textContains", selector: "blockquote", contains: "Стив Джобс", description: "В цитате упоминается Стив Джобс" },
  ],
  hints: [
    "Шаблон: <blockquote><p>текст цитаты</p><p>— автор</p></blockquote>. Внутри blockquote — два параграфа.",
    "Не забудь закрыть </blockquote> в конце.",
    'Полное решение:\n```html\n<h1>Цитата дня</h1>\n<blockquote>\n  <p>Оставайтесь голодными. Оставайтесь безрассудными.</p>\n  <p>— Стив Джобс</p>\n</blockquote>\n```',
  ],
  isAvailable: true,
};

export default lesson;
