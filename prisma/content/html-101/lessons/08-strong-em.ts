import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Жирный <strong> и курсив <em>",
  order: 8,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Жирный \`<strong>\` и курсив \`<em>\`

## 🎯 После этого урока ты сможешь
- Выделять важные слова жирным
- Делать акценты курсивом
- Понимать смысловую разницу между ними

## 🔍 Зачем это нужно
В обычной речи мы интонацией выделяем **важные слова** или *слова на которых акцент*. На бумаге это видно через **жирный** и *курсив*. В HTML то же самое.

## 📚 Теория

### \`<strong>\` — важное

Тег \`<strong>\` означает **«это важно»** или **«предупреждение»**. Браузер показывает текст внутри жирным.

\`\`\`html
<p>Срок сдачи — <strong>пятница</strong>. Не пропусти!</p>
\`\`\`

Слово «пятница» становится жирным, потому что оно — самое важное в предложении.

### \`<em>\` — акцент

\`<em>\` (от **emphasis** — акцент) означает **«здесь акцент»**. Браузер показывает курсивом.

\`\`\`html
<p>Этот сорт растёт <em>только</em> на юге.</p>
\`\`\`

Слово «только» выделено акцентом — мы хотим обратить на него внимание читателя.

### А чем плох \`<b>\` и \`<i>\`?

Есть похожие теги \`<b>\` (bold — жирный) и \`<i>\` (italic — курсив). Они работают, но **не несут смысла** — это «просто визуальное оформление». \`<strong>\` и \`<em>\` отличаются тем что они **смысловые**: скринридер прочитает их с интонацией, поисковики поймут что это важные слова.

**Правило простое:** если хочешь выделить слово **по смыслу** — используй \`<strong>\` или \`<em>\`. Если просто хочешь жирный текст без смысла (например для дизайна) — это работа CSS.

### Совмещение

Можно сделать «жирный курсив» — вложив один в другой:

\`\`\`html
<p><strong><em>Очень важно!</em></strong></p>
\`\`\`

Получится жирный курсив. Но не злоупотребляй — выделение должно работать.

## ⚠️ Частые ошибки

1. **Выделять всё подряд** — теряется смысл. Если все слова важные, важных нет ни одного.
2. **Использовать вместо заголовка**: «давайте сделаем заголовок жирнее» → \`<p><strong>Глава 1</strong></p>\`. Нет, для заголовка есть \`<h1>\`–\`<h6>\`.
3. **Путать \`<strong>\` и \`<b>\`**. По умолчанию выглядят одинаково, но смысла разный — учись правильно с самого начала.

## 🛠️ Задание

Сделай параграф:

\`Не забудь пакет — это очень важно.\`

В нём слово \`очень важно\` выдели через \`<strong>\`, а \`пакет\` — через \`<em>\`.`,
  starterCode: `<!-- Параграф со словом "пакет" в <em> и фразой "очень важно" в <strong> -->
`,
  solution: `<p>Не забудь <em>пакет</em> — это <strong>очень важно</strong>.</p>`,
  tests: [
    { kind: "exists", selector: "p", description: "Есть параграф" },
    { kind: "text", selector: "p em", equals: "пакет", description: 'Слово "пакет" обёрнуто в <em>' },
    { kind: "text", selector: "p strong", equals: "очень важно", description: 'Фраза "очень важно" обёрнута в <strong>' },
  ],
  hints: [
    "Внутри <p> вокруг нужного слова ставишь <em>...</em> или <strong>...</strong>. Остальной текст параграфа остаётся как был.",
    "Шаблон: <p>Не забудь <em>пакет</em> — это <strong>...</strong>.</p>",
    'Полное решение:\n```html\n<p>Не забудь <em>пакет</em> — это <strong>очень важно</strong>.</p>\n```',
  ],
  isAvailable: true,
};

export default lesson;
