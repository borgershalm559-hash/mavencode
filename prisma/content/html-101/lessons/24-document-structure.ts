import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Структура HTML-документа",
  order: 24,
  type: "quiz",
  language: "html",
  xpReward: 50,
  content: `# Структура HTML-документа

## 🎯 После этого урока ты будешь знать
- Что такое \`<!doctype html>\`
- Зачем нужны \`<html>\`, \`<head>\`, \`<body>\`
- Чем отличается \`<head>\` от \`<body>\`

## 📚 Теория

### Полная структура

До этого урока мы писали «голые» теги без обёрток. Это работало в нашем превью, но **настоящая HTML-страница** имеет вот такую полную структуру:

\`\`\`html
<!doctype html>
<html lang="ru">
  <head>
    <title>Моя страница</title>
    <meta charset="utf-8">
  </head>
  <body>
    <h1>Привет</h1>
    <p>Содержимое страницы.</p>
  </body>
</html>
\`\`\`

Разберём по частям.

### \`<!doctype html>\` — «это HTML5»

Самая первая строка любого HTML-файла. Это **не тег** в обычном смысле — это **указание браузеру** «читай этот файл как HTML5» (современный стандарт). Без него старые браузеры могут переключиться в «странный режим» (quirks mode) с отличиями от стандарта.

Пишется один раз в начале, не закрывается.

### \`<html>\` — корневой тег

Внутри \`<html>\` живёт **вся страница**. Это «коробка всех коробок». У неё обычно есть атрибут \`lang="ru"\` — указывает что страница на русском (важно для скринридеров и SEO).

### \`<head>\` — мета-информация

\`<head>\` — это «паспорт страницы». В нём — то что **не показывается на самой странице**, но влияет на то как она работает:
- Название во вкладке (\`<title>\`)
- Кодировка (\`<meta charset>\`)
- Описание для поисковиков
- Подключение стилей и шрифтов
- Иконка во вкладке (favicon)

### \`<body>\` — то что видит читатель

\`<body>\` — это **сама страница**: всё что увидит человек, когда зайдёт. Заголовки, параграфы, картинки, кнопки — всё внутри \`<body>\`.

### Можно представить так

\`\`\`
<html>           ← вся страница как папка
  <head>         ← информация о папке (название, метки)
  </head>
  <body>         ← содержимое папки (что увидят люди)
  </body>
</html>
\`\`\`

## ⚠️ Частые ошибки

1. **Забывают \`<!doctype html>\`** — браузер может включить странный режим.
2. **Пишут заголовок страницы в \`<body>\`** вместо \`<head>\`. \`<title>\` идёт в \`<head>\`.
3. **Помещают \`<h1>\` в \`<head>\`** — нет, \`<h1>\` это видимый заголовок, его место в \`<body>\`.

## 💡 Проверь себя`,
  starterCode: "",
  solution: "",
  tests: [
    {
      input: "Что должна быть самой первой строкой HTML-файла?",
      description: "<html> | <doctype html> | <!doctype html> | <head>",
      expected: "<!doctype html>",
    },
    {
      input: "Какой тег содержит видимое содержимое страницы (заголовки, текст, картинки)?",
      description: "<head> | <body> | <html> | <main>",
      expected: "<body>",
    },
    {
      input: "Где должен быть тег <title>?",
      description: "В <head> | В <body> | В <html> вне head и body | В самой первой строке файла",
      expected: "В <head>",
    },
    {
      input: "Зачем нужен атрибут lang у <html>?",
      description: "Для красоты | Указывает язык страницы — важно для скринридеров и SEO | Меняет шрифт | Это устаревший атрибут",
      expected: "Указывает язык страницы — важно для скринридеров и SEO",
    },
    {
      input: "Что увидит пользователь во вкладке браузера?",
      description: "Содержимое <h1> | Содержимое <title> | Первый <p> на странице | URL страницы",
      expected: "Содержимое <title>",
    },
  ],
  hints: [
    "<head> = паспорт страницы (невидимое). <body> = сама страница (видимое).",
    "Структура файла: <!doctype html> → <html> → внутри сначала <head>, потом <body>.",
    "Полные ответы:\n1. <!doctype html>\n2. <body>\n3. В <head>\n4. Указывает язык — для скринридеров и SEO\n5. Содержимое <title>",
  ],
  isAvailable: true,
};

export default lesson;
