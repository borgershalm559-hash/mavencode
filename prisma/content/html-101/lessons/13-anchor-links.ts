import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Якорные ссылки внутри страницы",
  order: 13,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Якорные ссылки внутри страницы

## 🎯 После этого урока ты сможешь
- Делать ссылки «оглавление → к разделу»
- Использовать атрибут \`id\`
- Понимать что значит \`#\` в URL

## 📚 Теория

### Что такое якорная ссылка

Открой длинную статью на Wikipedia. Сверху обычно есть **оглавление**: «1. История, 2. Применение, 3. Источники». Кликаешь на пункт оглавления — страница **прокручивается вниз** к нужному разделу. Это и есть якорная ссылка.

Работает в два шага:
1. Тегу-цели даёшь **id** (имя якоря): \`<h2 id="history">История</h2>\`
2. В ссылке указываешь \`href="#history"\` — решётка плюс имя якоря.

\`\`\`html
<!-- В начале страницы -->
<a href="#history">К истории</a>

<!-- Где-то ниже -->
<h2 id="history">История</h2>
<p>Очень длинный текст...</p>
\`\`\`

При клике на ссылку браузер прокрутит страницу к \`<h2>\` с этим id.

### Что такое \`id\`

Атрибут **\`id\`** — это **уникальное имя** элемента на странице. Как имя файла на диске или ник в чате: один-единственный.

\`\`\`html
<h2 id="history">История</h2>
<h2 id="usage">Применение</h2>
<h2 id="sources">Источники</h2>
\`\`\`

Каждый \`id\` на странице должен быть **уникальным** — два элемента не могут иметь один и тот же id.

### Правила имён id

- Только латинские буквы, цифры, дефис, подчёркивание (\`history\`, \`section-1\`, \`my_box\`)
- Без пробелов
- Желательно начинать с буквы
- Желательно в нижнем регистре

\`\`\`html
<!-- ✓ хорошо -->
<h2 id="history">История</h2>
<h2 id="section-1">Раздел 1</h2>

<!-- ✗ плохо -->
<h2 id="История">История</h2>           <!-- кириллица — допустимо но не принято -->
<h2 id="section 1">Раздел 1</h2>          <!-- пробел внутри -->
\`\`\`

## ⚠️ Частые ошибки

1. **Забыли \`#\` в href**: \`href="history"\` вместо \`href="#history"\`. Без решётки браузер подумает что это путь к файлу \`history\` и пойдёт его искать.
2. **Два одинаковых id** на странице. Якорь будет работать только до первого — это баг.
3. **Опечатка в id**: в ссылке \`href="#histroy"\`, а в \`<h2>\` стоит \`id="history"\`. Не сработает — буква в букву должно совпадать.

## 🛠️ Задание

Сделай мини-оглавление:

\`\`\`
[ссылка] К рецепту

<h1>Рецепт борща</h1>
<h2 id="ingredients">Ингредиенты</h2>
\`\`\`

В начале — параграф со ссылкой \`href="#ingredients"\` и текстом \`К ингредиентам\`. Затем \`<h1>\` со словом \`Рецепт борща\`. Затем \`<h2>\` с \`id="ingredients"\` и текстом \`Ингредиенты\`.`,
  starterCode: `<!-- Ссылка-якорь, заголовок и подзаголовок ниже -->
`,
  solution: `<p><a href="#ingredients">К ингредиентам</a></p>
<h1>Рецепт борща</h1>
<h2 id="ingredients">Ингредиенты</h2>`,
  tests: [
    { kind: "attr", selector: "a", name: "href", equals: "#ingredients", description: 'Ссылка href="#ingredients"' },
    { kind: "text", selector: "a", equals: "К ингредиентам", description: 'Текст ссылки — "К ингредиентам"' },
    { kind: "text", selector: "h1", equals: "Рецепт борща", description: '<h1> содержит "Рецепт борща"' },
    { kind: "attr", selector: "h2", name: "id", equals: "ingredients", description: 'У <h2> стоит id="ingredients"' },
    { kind: "text", selector: "h2", equals: "Ингредиенты", description: '<h2> содержит "Ингредиенты"' },
  ],
  hints: [
    "Якорная ссылка: href=\"#что-то\". Цель: тег с id=\"что-то\". Имя должно совпадать буква в букву.",
    "Шаблон ссылки: <a href=\"#ingredients\">К ингредиентам</a>. Шаблон цели: <h2 id=\"ingredients\">Ингредиенты</h2>.",
    'Полное решение:\n```html\n<p><a href="#ingredients">К ингредиентам</a></p>\n<h1>Рецепт борща</h1>\n<h2 id="ingredients">Ингредиенты</h2>\n```',
  ],
  isAvailable: true,
};

export default lesson;
