import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Как браузер читает HTML",
  order: 2,
  type: "quiz",
  language: "html",
  xpReward: 50,
  content: `# Как браузер читает HTML

## 🎯 После этого урока ты поймёшь
- Что такое HTML-тег и почему они идут парами
- Как теги вкладываются друг в друга
- Что произойдёт если забыть закрыть тег

## 📚 Теория

### Тег — это коробка с содержимым

Возьми коробку. У неё есть **крышка снизу** (когда коробка закрыта) и **крышка сверху** (открытая). Между ними — **содержимое**.

В HTML то же самое. Каждый тег — это **пара**: открывающий и закрывающий. Между ними — содержимое тега.

\`\`\`html
<h1>Привет</h1>
\`\`\`

Здесь:
- \`<h1>\` — **открывающий** тег (открыли коробку)
- \`Привет\` — **содержимое** (что лежит в коробке)
- \`</h1>\` — **закрывающий** тег (закрыли коробку — обрати внимание на слэш \`/\`)

### Браузер читает сверху вниз

Браузер открывает HTML-файл и читает его **строка за строкой**, сверху вниз. Когда видит открывающий тег — запоминает «открыли коробку», добавляет на страницу элемент. Когда видит закрывающий — закрывает коробку.

Если ты напишешь:

\`\`\`html
<h1>Заголовок</h1>
<p>Параграф</p>
\`\`\`

Браузер сначала покажет заголовок, потом ниже — параграф. **Порядок важен**: что выше в файле — то выше на странице.

### Вложенность — коробки внутри коробок

Один тег может лежать **внутри** другого. Это как когда ты кладёшь маленькую коробку в большую.

\`\`\`html
<p>Я ем <strong>очень</strong> большой бутерброд</p>
\`\`\`

Здесь \`<strong>\` (жирный текст) лежит **внутри** \`<p>\` (параграф). Маленькая коробка внутри большой.

**Правило вложенности:** что открыли последним — то закрываешь первым.

\`\`\`html
<p>Я ем <strong>очень</strong> большой бутерброд</p>
   ^  открыли P
            ^ открыли STRONG
                       ^ закрыли STRONG (последним открыли — первым закрыли)
                                                     ^ закрыли P
\`\`\`

### Что будет если забыть закрыть тег?

Браузер постарается «догадаться», но результат может сломаться. Часть страницы может оказаться внутри другого тега, цвета могут «протечь», верстка поломается. **Закрывай теги всегда.**

### Самозакрывающиеся (одиночные) теги

Есть несколько тегов, у которых **нет содержимого** — они стоят сами по себе. Например \`<br>\` (перенос строки) или \`<img>\` (картинка). Их не нужно закрывать.

\`\`\`html
<p>Первая строка<br>Вторая строка</p>
\`\`\`

Таких тегов мало, мы их разберём отдельно.

## ⚠️ Самые частые ошибки

1. **Забыли слэш в закрывающем теге**: \`<h1>Привет<h1>\` вместо \`<h1>Привет</h1>\`. Без слэша браузер думает что ты открыл ещё один \`<h1>\`.
2. **Перепутали порядок закрытия**: \`<p>Текст <strong>жирный</p></strong>\` — закрыли \`</p>\` раньше \`</strong>\`. Это как если бы маленькая коробка торчала из большой когда ты её закрываешь.
3. **Смешали регистр**: \`<H1>Привет</h1>\` — открыли заглавными, закрыли маленькими. По стандарту имена тегов **в нижнем регистре** всегда.

## 💡 Проверь себя`,
  starterCode: "",
  solution: "",
  tests: [
    {
      input: "Что такое закрывающий тег?",
      description: "Тег со слэшем перед именем — </h1> | Тег с пробелом перед именем | Тег с заглавной буквой | Это одно и то же что открывающий",
      expected: "Тег со слэшем перед именем — </h1>",
    },
    {
      input: "В каком порядке закрываются вложенные теги?",
      description: "В любом порядке | Что открыли последним — закрываешь первым | Сначала закрываешь все большие | Сначала закрываешь все маленькие",
      expected: "Что открыли последним — закрываешь первым",
    },
    {
      input: "Какая запись правильная?",
      description: "<p>Текст <strong>жирный</p></strong> | <p>Текст <strong>жирный</strong></p> | <P>Текст <strong>жирный</strong></p> | <p>Текст <strong>жирный</strong>",
      expected: "<p>Текст <strong>жирный</strong></p>",
    },
    {
      input: "В каком регистре по стандарту пишутся имена тегов?",
      description: "В верхнем (<H1>) | В нижнем (<h1>) | Любой регистр допустим | В заглавной первой буквой (<H1>)",
      expected: "В нижнем (<h1>)",
    },
    {
      input: "В каком порядке браузер показывает элементы?",
      description: "Случайно | Сверху вниз — как написано в файле | Сначала картинки, потом текст | По алфавиту",
      expected: "Сверху вниз — как написано в файле",
    },
  ],
  hints: [
    "Думай о тегах как о коробках. Открыл коробку — закрой её. Если коробку положил в большую — сначала закрой маленькую.",
    "Закрывающий тег отличается от открывающего только символом / перед именем: <h1> vs </h1>.",
    "Полные ответы:\n1. Тег со слэшем — </h1>\n2. Что открыли последним — закрываешь первым\n3. <p>Текст <strong>жирный</strong></p>\n4. Нижний регистр\n5. Сверху вниз — как написано в файле",
  ],
  isAvailable: true,
};

export default lesson;
