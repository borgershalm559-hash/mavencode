import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Маркированный список <ul>",
  order: 17,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Маркированный список \`<ul>\`

## 🎯 После этого урока ты сможешь
- Делать списки с маркерами (точками)
- Понимать пару \`<ul>\` + \`<li>\`

## 📚 Теория

### Что такое маркированный список

Список покупок, перечень фильмов, состав блюда — всё это **маркированные списки** (с точками или другими маркерами слева). В HTML они делаются парой тегов:

- \`<ul>\` (от **unordered list** — неупорядоченный список) — внешняя обёртка
- \`<li>\` (от **list item** — элемент списка) — каждый пункт

\`\`\`html
<ul>
  <li>Молоко</li>
  <li>Хлеб</li>
  <li>Яйца</li>
</ul>
\`\`\`

Браузер автоматически:
- Поставит маркер (точку) перед каждым пунктом
- Сделает отступ слева
- Разделит пункты переносами строк

### Зачем оборачивать в \`<ul>\`

Тег \`<ul>\` нужен чтобы **группировать** пункты. Браузеру и скринридеру важно знать «вот тут начинается список из 3 пунктов» — а не три случайных строки.

Без \`<ul>\` элементы \`<li>\` будут «болтаться» — это считается ошибкой разметки.

### Внутри \`<li>\` можно что угодно

Содержимое одного пункта может быть длинным текстом, ссылкой, картинкой:

\`\`\`html
<ul>
  <li>Простой пункт</li>
  <li><strong>Важно:</strong> закрывайте холодильник</li>
  <li>Подробнее на <a href="https://example.com">сайте</a></li>
</ul>
\`\`\`

## ⚠️ Частые ошибки

1. **\`<li>\` без \`<ul>\`**: браузер покажет, но это ошибка разметки.
2. **Используют \`<br>\`** для «списка»: \`<p>Молоко<br>Хлеб<br>Яйца</p>\`. Это **не список**, а просто параграф с переносами. Скринридер не поймёт что это пункты.
3. **Внутри \`<ul>\` пишут не \`<li>\`**: \`<ul><p>Пункт</p></ul>\`. Внутри \`<ul>\` могут быть **только** \`<li>\` (а уже внутри \`<li>\` — что угодно).

## 🛠️ Задание

Сделай список покупок из трёх пунктов:

\`\`\`
- Молоко
- Хлеб
- Яйца
\`\`\``,
  starterCode: `<!-- Маркированный список из трёх пунктов -->
`,
  solution: `<ul>
  <li>Молоко</li>
  <li>Хлеб</li>
  <li>Яйца</li>
</ul>`,
  tests: [
    { kind: "exists", selector: "ul", description: "Есть <ul>" },
    { kind: "count", selector: "ul li", n: 3, description: "Внутри <ul> ровно три <li>" },
    { kind: "textContains", selector: "ul", contains: "Молоко", description: 'В списке есть "Молоко"' },
    { kind: "textContains", selector: "ul", contains: "Хлеб", description: 'В списке есть "Хлеб"' },
    { kind: "textContains", selector: "ul", contains: "Яйца", description: 'В списке есть "Яйца"' },
  ],
  hints: [
    "<ul> — внешняя обёртка. Внутри — три <li>, каждый со своим текстом.",
    "Шаблон одного пункта: <li>текст</li>. Все три — внутри <ul>...</ul>.",
    'Полное решение:\n```html\n<ul>\n  <li>Молоко</li>\n  <li>Хлеб</li>\n  <li>Яйца</li>\n</ul>\n```',
  ],
  isAvailable: true,
};

export default lesson;
