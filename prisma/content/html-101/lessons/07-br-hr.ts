import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Перенос строки <br> и разделитель <hr>",
  order: 7,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Перенос строки \`<br>\` и разделитель \`<hr>\`

## 🎯 После этого урока ты сможешь
- Делать переносы строки внутри одного абзаца
- Ставить горизонтальную линию-разделитель
- Понимать что такое самозакрывающиеся теги

## 📚 Теория

### Самозакрывающиеся теги

Помнишь, в L02 мы говорили — большинство тегов идут парой (открывающий + закрывающий). Но есть **исключения**: теги без содержимого. Их пишут один раз, без закрывающей пары. Их называют **самозакрывающиеся** или **одиночные**.

В этом уроке два таких тега: \`<br>\` и \`<hr>\`.

### \`<br>\` — перенос строки

\`<br>\` (от **break** — разрыв) — это просто **перенос строки**. Как когда ты в Word нажимаешь Enter в середине абзаца.

\`\`\`html
<p>Первая строка<br>Вторая строка</p>
\`\`\`

В одном параграфе будут две строки одна под другой — \`<br>\` сделал перенос.

**Когда использовать**: для адресов, стихов, коротких списков прямо в тексте. **Не использовать** чтобы делать пустые строки между абзацами — для этого есть отдельные \`<p>\`.

### \`<hr>\` — горизонтальная линия

\`<hr>\` (от **horizontal rule** — горизонтальная черта) — это **разделительная линия** на всю ширину страницы. Используется чтобы визуально отделить одну смысловую часть от другой.

\`\`\`html
<h1>Главная страница</h1>
<p>Какой-то текст.</p>
<hr>
<h2>Контакты</h2>
<p>Пишите на почту.</p>
\`\`\`

Между параграфом и заголовком "Контакты" появится тонкая горизонтальная линия.

### Два варианта записи

Самозакрывающиеся теги можно писать двумя способами:

\`\`\`html
<br>          <!-- HTML5 — современный, рекомендуемый -->
<br />        <!-- XHTML/XML — со слэшем перед закрывающей скобкой -->
\`\`\`

Оба работают. Современный стандарт — без слэша. Пиши как удобнее, но чаще встретишь \`<br>\` без слэша.

## ⚠️ Частые ошибки

1. **Закрывают \`<br>\`** как обычный тег: \`<br></br>\`. Не нужно — у \`<br>\` нет содержимого, закрывать нечего.
2. **Используют \`<br><br>\`** вместо параграфов. Не делай так — для пустой строки между мыслями есть отдельные \`<p>\`.
3. **Путают \`<br>\` и \`<hr>\`**: \`<br>\` — перенос строки (невидимый), \`<hr>\` — линия (видимая горизонтальная черта).

## 🛠️ Задание

Сделай страничку:
- \`<h1>\` — \`Адрес\`
- \`<p>\` со строками через \`<br>\`: \`Москва\`, перенос, \`Тверская улица, 7\`
- \`<hr>\`
- \`<p>\` — \`Время работы: 9:00–21:00\``,
  starterCode: `<!-- Здесь твоя верстка -->
`,
  solution: `<h1>Адрес</h1>
<p>Москва<br>Тверская улица, 7</p>
<hr>
<p>Время работы: 9:00–21:00</p>`,
  tests: [
    { kind: "text", selector: "h1", equals: "Адрес", description: 'Есть <h1> со словом "Адрес"' },
    { kind: "exists", selector: "p br", description: "Внутри <p> есть тег <br>" },
    { kind: "exists", selector: "hr", description: "На странице есть <hr>" },
    { kind: "count", selector: "p", n: 2, description: "Два параграфа <p>" },
  ],
  hints: [
    "<br> ставится прямо внутри текста параграфа в месте, где нужен перенос. <hr> — отдельной строкой между параграфами.",
    "Параграф с переносом: <p>Москва<br>Тверская улица, 7</p>. Линия: <hr> (без закрывающего тега).",
    'Полное решение:\n```html\n<h1>Адрес</h1>\n<p>Москва<br>Тверская улица, 7</p>\n<hr>\n<p>Время работы: 9:00–21:00</p>\n```',
  ],
  isAvailable: true,
};

export default lesson;
