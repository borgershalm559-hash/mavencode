import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Иерархия заголовков <h1>–<h6>",
  order: 5,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Иерархия заголовков \`<h1>\`–\`<h6>\`

## 🎯 После этого урока ты сможешь
- Использовать заголовки разных уровней
- Выстраивать иерархию документа

## 🔍 Зачем это нужно
В книге есть **название книги** (одно), **названия глав** (много), **подразделы внутри главы**. В HTML то же самое:

- \`<h1>\` — название всей страницы (как название книги)
- \`<h2>\` — главы или большие разделы
- \`<h3>\` — подразделы внутри главы
- ...и так до \`<h6>\` (самый маленький подзаголовок)

## 📚 Теория

### Шесть уровней заголовков

В HTML **шесть** заголовочных тегов: от \`<h1>\` (самый важный) до \`<h6>\` (самый незаметный).

\`\`\`html
<h1>Название статьи</h1>
<h2>Глава 1</h2>
<h3>Раздел 1.1</h3>
<h3>Раздел 1.2</h3>
<h2>Глава 2</h2>
<h3>Раздел 2.1</h3>
\`\`\`

Браузер автоматически делает каждый следующий уровень мельче и менее жирным.

### Правила хорошего тона

1. **\`<h1>\` на странице один.** У страницы одно главное название, как у книги одна обложка.
2. **Не пропускай уровни.** Сразу после \`<h1>\` идёт \`<h2>\`, потом \`<h3>\`. Не надо прыгать с \`<h1>\` на \`<h4>\` — это ломает «оглавление» страницы. Это важно для тех, кто пользуется скринридером (программой которая читает страницы вслух).
3. **Не используй \`<h1>\`–\`<h6>\` чтобы «сделать текст большим».** Для размера есть CSS. Заголовки нужны чтобы показать **смысловую важность**.

### Зачем это всё

- **Скринридеры** строят «оглавление» страницы по заголовкам и читают его слепым пользователям. Если иерархия рваная — навигация ломается.
- **Поисковики** (Google, Яндекс) понимают о чём страница по заголовкам.
- **Читатель** видит структуру и не теряется.

## 🧪 Примеры

\`\`\`html
<h1>Кулинарный блог</h1>           <!-- название всего блога -->
  <h2>Супы</h2>                    <!-- раздел -->
    <h3>Борщ</h3>                  <!-- подраздел -->
    <h3>Уха</h3>
  <h2>Десерты</h2>
    <h3>Тирамису</h3>
\`\`\`

(Отступы здесь только для тебя — в реальном HTML их можно не делать, на отображение не влияют.)

## ⚠️ Частые ошибки

1. **Несколько \`<h1>\` на странице** — обычно ошибка. Должен быть один.
2. **Прыжок через уровни**: после \`<h2>\` сразу \`<h4>\`. Используй \`<h3>\`.
3. **Заголовок ради размера**: «надо чтобы было пожирнее, обернём в \`<h2>\`». Нет, для жирного есть \`<strong>\` (учим в L08).

## 🛠️ Задание

Сделай заголовочную структуру для статьи о Москве:

- \`<h1>\` — \`Москва\`
- \`<h2>\` — \`История\`
- \`<h2>\` — \`Достопримечательности\`

Три тега, по порядку, в указанной иерархии.`,
  starterCode: `<!-- Заголовок страницы: Москва -->
<!-- Раздел: История -->
<!-- Раздел: Достопримечательности -->
`,
  solution: `<h1>Москва</h1>
<h2>История</h2>
<h2>Достопримечательности</h2>`,
  tests: [
    { kind: "count", selector: "h1", n: 1, description: "Ровно один <h1> на странице" },
    { kind: "text", selector: "h1", equals: "Москва", description: '<h1> содержит "Москва"' },
    { kind: "count", selector: "h2", n: 2, description: "Ровно два <h2> на странице" },
    { kind: "textContains", selector: "body", contains: "История", description: 'На странице есть слово "История"' },
    { kind: "textContains", selector: "body", contains: "Достопримечательности", description: 'На странице есть слово "Достопримечательности"' },
  ],
  hints: [
    "Тебе нужны три заголовка: один <h1> и два <h2>. Каждый — отдельной строкой.",
    "Шаблон одной строки: <h1>текст</h1>. Сделай по этому шаблону три строки с правильным уровнем и текстом.",
    'Полное решение:\n```html\n<h1>Москва</h1>\n<h2>История</h2>\n<h2>Достопримечательности</h2>\n```',
  ],
  isAvailable: true,
};

export default lesson;
