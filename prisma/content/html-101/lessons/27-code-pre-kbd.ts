import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Технический текст: <code>, <pre>, <kbd>",
  order: 27,
  type: "code",
  language: "html",
  xpReward: 70,
  content: `# Технический текст: \`<code>\`, \`<pre>\`, \`<kbd>\`

## 🎯 После этого урока ты сможешь
- Оформлять код в тексте
- Сохранять переносы и пробелы
- Указывать сочетания клавиш

## 📚 Теория

### \`<code>\` — фрагмент кода в строке

Если в обычном предложении ты упоминаешь имя функции, тег, команду — заверни в \`<code>\`. Браузер покажет моноширинным шрифтом (как в редакторе кода).

\`\`\`html
<p>Функция <code>print()</code> выводит текст на экран.</p>
\`\`\`

«print()» будет с другим шрифтом и обычно с серым фоном.

### \`<pre>\` — сохранить форматирование

Помнишь, в L06 мы обсуждали что внутри \`<p>\` все пробелы и переносы «схлопываются» в один пробел? Тег \`<pre>\` (от **preformatted** — предформатированный) **сохраняет** все пробелы и переносы как есть.

\`\`\`html
<pre>
function hello() {
  console.log("Привет");
}
</pre>
\`\`\`

Текст внутри \`<pre>\` будет точно как ты написал — с отступами и переносами строк, моноширинным шрифтом.

### \`<pre><code>\` — стандартный паттерн для блоков кода

Самый частый вариант — обернуть код в **обоих** тегах:

\`\`\`html
<pre><code>function hello() {
  console.log("Привет");
}</code></pre>
\`\`\`

Зачем:
- \`<pre>\` сохраняет форматирование
- \`<code>\` помечает «это код» для поисковиков и подсветки синтаксиса

### \`<kbd>\` — клавиши клавиатуры

\`<kbd>\` (от **keyboard** — клавиатура) показывает **клавишу или сочетание клавиш**.

\`\`\`html
<p>Чтобы скопировать — нажми <kbd>Ctrl</kbd>+<kbd>C</kbd>.</p>
\`\`\`

Браузер по умолчанию делает рамочку вокруг \`<kbd>\` — выглядит как настоящая клавиша.

### Когда что

| Тег     | Зачем                                |
|---------|---------------------------------------|
| \`<code>\` | Имя функции, команды, тега в строке |
| \`<pre>\`  | Сохранить пробелы и переносы         |
| \`<pre><code>\` | Блок кода (несколько строк)    |
| \`<kbd>\`  | Клавиша или сочетание                |

## 🛠️ Задание

Сделай параграф:

\`Чтобы вызвать функцию [print], нажми [Ctrl]+[Enter].\`

Слово \`print\` обёрнуть в \`<code>\`. Слова \`Ctrl\` и \`Enter\` — каждое в свой \`<kbd>\`.`,
  starterCode: `<!-- Параграф с code и kbd -->
`,
  solution: `<p>Чтобы вызвать функцию <code>print</code>, нажми <kbd>Ctrl</kbd>+<kbd>Enter</kbd>.</p>`,
  tests: [
    { kind: "exists", selector: "p", description: "Есть параграф" },
    { kind: "text", selector: "p code", equals: "print", description: '<code>print</code> внутри параграфа' },
    { kind: "count", selector: "p kbd", n: 2, description: "Два тега <kbd>" },
    { kind: "textContains", selector: "p", contains: "Ctrl", description: 'В параграфе есть "Ctrl"' },
    { kind: "textContains", selector: "p", contains: "Enter", description: 'В параграфе есть "Enter"' },
  ],
  hints: [
    "Внутри <p>: текст до <code>print</code> текст между <kbd>Ctrl</kbd>+<kbd>Enter</kbd> и точка.",
    "Шаблон: <p>Чтобы вызвать функцию <code>print</code>, нажми <kbd>Ctrl</kbd>+<kbd>Enter</kbd>.</p>",
    'Полное решение:\n```html\n<p>Чтобы вызвать функцию <code>print</code>, нажми <kbd>Ctrl</kbd>+<kbd>Enter</kbd>.</p>\n```',
  ],
  isAvailable: true,
};

export default lesson;
