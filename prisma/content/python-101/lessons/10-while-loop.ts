import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Цикл while: пока истинно",
  order: 10,
  type: "code",
  language: "python",
  xpReward: 130,
  content: `# Цикл \`while\`: пока истинно

## 🎯 Что научишься делать
- Использовать цикл \`while\` для повторения действий
- Понимать, почему внутри тела цикла необходимо изменять состояние
- Использовать \`break\` для досрочного выхода из цикла
- Распознавать бесконечные циклы и знать, как их избежать
- Строить счётчики и накапливать результат

## 🔍 Зачем это нужно
Реальные задачи часто требуют повторять действия: перебирать строки файла, ждать ответа от сервера, повторять попытку подключения, считать деньги в кошельке. Без циклов пришлось бы писать одни и те же строки кода снова и снова. Цикл \`while\` — один из двух главных инструментов повторения в Python. Он особенно удобен, когда не знаешь заранее, сколько раз нужно повторить действие.

## 📚 Теория

### Метафора: чайник на плите

Представь, что ты поставил чайник на плиту. Ты проверяешь: «Чайник закипел? Нет? Продолжаю ждать. Закипел? Выключаю плиту».

Цикл \`while\` работает точно так же:
- Проверяет условие перед каждым витком.
- Если условие истинно — выполняет тело цикла.
- Снова проверяет условие.
- Повторяет, пока условие не станет ложным.

\`\`\`python
water_boiled = False
count = 0

while not water_boiled:
    count = count + 1
    print(f"Ожидание... виток {count}")
    if count >= 3:
        water_boiled = True

print("Чайник закипел!")
\`\`\`

### Синтаксис while

\`\`\`python
while условие:
    тело цикла
\`\`\`

Разберём детально:
- \`while\` — ключевое слово, начинает цикл.
- \`условие\` — любое выражение, которое возвращает \`True\` или \`False\`.
- \`:\` — двоеточие обязательно, как в \`if\`.
- Тело цикла — строки с отступом 4 пробела.

\`\`\`python
n = 5

while n > 0:           # условие: пока n больше нуля
    print(n)           # выводим текущее n
    n = n - 1          # уменьшаем n на 1 — изменяем состояние!

print("Старт!")        # выполнится после окончания цикла
# Вывод: 5, 4, 3, 2, 1, Старт!
\`\`\`

### Критически важно: изменять состояние внутри цикла

Посмотри на эту строку:
\`\`\`python
n = n - 1
\`\`\`

Это **обязательная часть** любого цикла \`while\`. Если не изменять переменную, от которой зависит условие, условие никогда не станет ложным — и цикл будет крутиться вечно.

\`\`\`python
n = 5

while n > 0:      # Бесконечный цикл!
    print(n)      # n всегда 5, условие n > 0 всегда True
                  # строка n = n - 1 забыта
\`\`\`

Это называется **бесконечный цикл** — программа зависает. В реальной консоли его можно прервать нажатием **Ctrl+C**.

### Счётчик — классический паттерн

\`\`\`python
counter = 0

while counter < 5:
    print(f"Итерация {counter}")
    counter = counter + 1    # или counter += 1 — краткая запись

# Вывод: Итерация 0, Итерация 1, ..., Итерация 4
\`\`\`

Здесь \`counter\` — счётчик: начинается с 0, увеличивается на 1 каждый виток, цикл завершается когда счётчик достигает 5.

### Оператор break — досрочный выход

Иногда нужно выйти из цикла до того, как условие станет ложным. Для этого есть \`break\`:

\`\`\`python
secret = "python"
attempts = 0

while attempts < 3:
    guess = input("Введи пароль: ")
    if guess == secret:
        print("Верно!")
        break            # выходим из цикла немедленно
    attempts = attempts + 1

print("Конец")
\`\`\`

\`break\` полезен, когда условие выхода сложнее, чем одна проверка в заголовке цикла.

### Накопление результата в цикле

Цикл \`while\` часто используют для наполнения списка или накопления суммы:

\`\`\`python
def collect_positive(limit):
    result = []
    n = 1
    while n <= limit:
        result.append(n)    # добавляем n в список
        n = n + 1
    return result

print(collect_positive(4))  # [1, 2, 3, 4]
\`\`\`

Паттерн: создаём пустой список \`[]\`, добавляем элементы через \`append\`, возвращаем готовый список.

## 🧪 Примеры

\`\`\`python
# Сумма от 1 до n
def sum_up_to(n):
    total = 0
    i = 1
    while i <= n:
        total = total + i
        i = i + 1
    return total

print(sum_up_to(5))    # 15 (1+2+3+4+5)
print(sum_up_to(10))   # 55
\`\`\`

\`\`\`python
# Нахождение первого чётного числа больше n
def next_even(n):
    candidate = n + 1
    while candidate % 2 != 0:
        candidate = candidate + 1
    return candidate

print(next_even(7))    # 8
print(next_even(10))   # 12
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Забыть изменять переменную условия.** Если \`n\` не уменьшается внутри \`while n > 0:\` — цикл бесконечный. Всегда проверяй: что изменится после каждого витка?

2. **Изменить переменную до входа в цикл вместо внутри.** Некоторые начинающие пишут \`n = n - 1\` перед \`while\`, а не внутри. Условие проверится один раз с уменьшенным n, а потом снова с неизменным.

3. **Неправильное начальное значение.** Если нужно считать от 1 до 5, но начать с 0, цикл выполнится 6 раз (0, 1, 2, 3, 4, 5), а не 5. Следи за начальными значениями.

4. **break внутри if без отступа.** \`break\` должен быть внутри тела цикла с правильным отступом. Если отступ неверный — Python выдаст \`IndentationError\` или \`break\` выполнится не там, где ожидалось.

5. **Использовать while там, где нужен for.** Если заранее известно количество итераций или перебираешь последовательность — \`for\` удобнее и безопаснее (о нём в следующем уроке).

## 💡 Мини-тест самопроверки

1. Сколько раз выполнится тело цикла?
\`\`\`python
x = 3
while x > 0:
    print(x)
    x = x - 1
\`\`\`
<details><summary>Ответ</summary>3 раза — при x=3, x=2, x=1. Когда x становится 0, условие x > 0 становится False и цикл завершается.</details>

2. Что произойдёт, если убрать строку \`x = x - 1\`?
<details><summary>Ответ</summary>Бесконечный цикл. x всегда будет 3, условие x > 0 всегда True, цикл никогда не завершится.</details>

3. Для чего нужен \`break\`?
<details><summary>Ответ</summary>Для досрочного выхода из цикла — до того, как условие в заголовке стало ложным.</details>

## 🛠️ Задание

Напиши функцию \`count_down(n)\`, которая принимает число \`n\` и возвращает **список** чисел от \`n\` до \`1\` включительно (в порядке убывания).

Примеры:
- \`count_down(3)\` → \`[3, 2, 1]\`
- \`count_down(1)\` → \`[1]\`
- \`count_down(0)\` → \`[]\`

Используй цикл \`while\` и метод \`append\` для наполнения списка.`,

  starterCode: `def count_down(n):
    result = []
    # Используй while для добавления чисел от n до 1
    # Добавляй числа через result.append(...)
    # Не забудь изменять n на каждом витке
    return result`,

  solution: `def count_down(n):
    result = []
    while n > 0:
        result.append(n)
        n = n - 1
    return result`,

  tests: [
    { input: "count_down(3)",  expected: "[3, 2, 1]",       description: "Отсчёт от 3" },
    { input: "count_down(1)",  expected: "[1]",             description: "Один элемент" },
    { input: "count_down(0)",  expected: "[]",              description: "Ноль → пустой список" },
    { input: "count_down(5)",  expected: "[5, 4, 3, 2, 1]", description: "Отсчёт от 5" },
  ],

  hints: [
    "Создай пустой список `result = []`. Условие цикла: `while n > 0:`. Внутри тела — добавь текущее `n` в список через `result.append(n)`, затем уменьши `n` на единицу: `n = n - 1`. Верни `result` после цикла.",
    "Структура решения:\n```python\nresult = []\nwhile n > 0:\n    result.append(n)  # сначала добавляем\n    n = n - 1         # потом уменьшаем\nreturn result\n```\nПорядок важен: сначала добавить текущее значение, потом изменить.",
    "Полное решение:\n```python\ndef count_down(n):\n    result = []\n    while n > 0:\n        result.append(n)\n        n = n - 1\n    return result\n```",
  ],

  isAvailable: true,
};

export default lesson;
