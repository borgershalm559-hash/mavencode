import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Найди баг: сломанная обработка данных",
  order: 16,
  type: "fix-bug",
  language: "python",
  xpReward: 150,
  content: `# Найди баг: сломанная обработка данных

## 🎯 Что научишься делать
- Замечать ошибку off-by-one в \`range()\`: разница между \`len(list)\` и \`len(list) + 1\`
- Различать \`=\` (присваивание) и \`==\` (сравнение) — особенно в условиях
- Видеть, когда числовая операция применяется некорректно

## 🔍 Зачем это нужно
Три бага из этого урока встречаются в реальном коде постоянно. Off-by-one — классика жанра: цикл делает на одну итерацию больше, чем нужно, и падает с \`IndexError\` на последнем шаге. Использование \`==\` вместо \`=\` в ключевом месте превращает исправление ошибки в ничто — переменная не обновляется. И неправильная математическая операция (умножение вместо вычитания) ломает всю логику расчёта.

## 📚 Теория

### Баг 1: off-by-one в range()

Это одна из самых распространённых ошибок при работе со списками через индексы. \`range(n)\` генерирует числа от 0 до n-1 включительно. Если список имеет длину \`n\`, его индексы — от \`0\` до \`n-1\`.

\`\`\`python
numbers = [10, 20, 30]   # длина 3, индексы: 0, 1, 2

# Неправильно — range(len + 1) даёт лишний индекс:
for i in range(len(numbers) + 1):   # 0, 1, 2, 3 — но индекса 3 нет!
    print(numbers[i])               # IndexError на последней итерации

# Правильно:
for i in range(len(numbers)):       # 0, 1, 2 — ровно столько, сколько элементов
    print(numbers[i])
\`\`\`

Обрати внимание: в большинстве случаев лучше обходить список напрямую:

\`\`\`python
for item in numbers:    # вообще без индексов — проще и безопаснее
    print(item)
\`\`\`

Индексы нужны, только когда тебе важна позиция элемента.

### Баг 2: неправильная операция

Математическая ошибка — неправильный оператор или неправильные операнды. Например, умножение вместо вычитания:

\`\`\`python
total = 100
discount = 10

# Неправильно — умножаем скидку на 10 вместо простого вычитания:
total = total - discount * 10   # 100 - 100 = 0 (скидка 10 превращается в 100!)

# Правильно — просто вычитаем скидку:
total = total - discount        # 100 - 10 = 90
\`\`\`

Такой баг сложно заметить по одному взгляду — нужно проверять вычисления на конкретных примерах.

### Баг 3: == вместо = в присваивании

Сравнение \`==\` и присваивание \`=\` — визуально похожи, но делают совершенно разное. Эта ошибка особенно коварна: код **не падает**, просто тихо работает неправильно.

\`\`\`python
total = -5

# Неправильно — это сравнение, результат True/False нигде не сохраняется:
total == 0    # просто вычисляет False и выбрасывает результат

# Правильно — это присваивание, значение сохраняется:
total = 0     # теперь total действительно равен 0
\`\`\`

Типичный сценарий: код должен сбросить переменную в 0 при определённом условии, но из-за \`==\` переменная остаётся прежней — и функция возвращает отрицательное число.

## 🧪 Примеры — аналогичные ошибки в другом контексте

\`\`\`python
# Off-by-one в разных сценариях
items = ["a", "b", "c"]

# Ошибочно:
for i in range(1, len(items) + 1):   # 1, 2, 3 — индекс 3 не существует
    print(items[i])

# Верно:
for i in range(len(items)):          # 0, 1, 2 — все существующие индексы
    print(items[i])
\`\`\`

\`\`\`python
# == вместо = в сбросе переменной
count = 5
count == 0   # ничего не происходит — это просто сравнение (False)
print(count) # 5 — не изменилось!

count = 0    # теперь переменная сброшена
print(count) # 0
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Писать \`range(len(list) + 1)\` по аналогии с нумерацией от 1 до N.** В Python нумерация с нуля, поэтому \`range(len(list))\` даёт ровно нужные индексы: от \`0\` до \`len-1\`.

2. **Использовать \`==\` вместо \`=\` для обнуления переменной.** IDE и Python не предупредят тебя об этом — результат выражения просто отбрасывается. Всегда проверяй, не потерялся ли один знак равенства.

3. **Не проверять граничные случаи.** Функция может работать правильно для «среднего» случая, но ломаться на пустом списке, нуле или отрицательном числе. Тесты с граничными значениями — твоя защита.

4. **Доверять коду, который «не падает».** Отсутствие ошибки не означает правильности. \`total == 0\` — не ошибка с точки зрения Python, но логический баг. Тесты нужны именно для таких случаев.

## 💡 Мини-тест самопроверки

1. Что не так с \`range(len([10, 20, 30]) + 1)\`?
<details><summary>Ответ</summary>Это даст \`range(4)\` — числа 0, 1, 2, 3. Но список имеет только индексы 0, 1, 2. На итерации с индексом 3 будет \`IndexError\`. Правильно: \`range(len([10, 20, 30]))\` → \`range(3)\`.</details>

2. Что произойдёт при выполнении \`x = 5; x == 0\`?
<details><summary>Ответ</summary>Вычислится выражение \`False\` (5 не равно 0), но результат нигде не сохранится. Переменная \`x\` останется равной 5.</details>

3. Какой результат даст правильная версия \`calculate_total([10], 50)\`?
<details><summary>Ответ</summary>\`0\` — сумма заказов равна 10, скидка 50 больше суммы, итог был бы отрицательным, но функция ограничивает результат нулём снизу.</details>

## 🛠️ Задание

В функции \`calculate_total(orders, discount)\` спрятаны три бага. Функция должна:
1. Посчитать сумму всех заказов из списка
2. Вычесть скидку (если она задана)
3. Вернуть \`0\` если итог отрицательный (скидка больше суммы)

Найди и исправь все три ошибки. Комментарии указывают, в каких строках они находятся.`,

  starterCode: `def calculate_total(orders, discount=0):
    # ⚠️ Эта функция содержит 3 бага
    total = 0
    for i in range(len(orders) + 1):  # Баг 1
        total = total + orders[i]
    if discount > 0:
        total = total - discount * 10  # Баг 2
    if total < 0:
        total == 0  # Баг 3
    return total`,

  solution: `def calculate_total(orders, discount=0):
    total = 0
    for i in range(len(orders)):
        total = total + orders[i]
    if discount > 0:
        total = total - discount
    if total < 0:
        total = 0
    return total`,

  tests: [
    { input: "calculate_total([10, 20, 30])",  expected: "60",  description: "Без скидки" },
    { input: "calculate_total([100, 50], 30)", expected: "120", description: "Со скидкой" },
    { input: "calculate_total([], 0)",          expected: "0",   description: "Пусто" },
    { input: "calculate_total([10], 50)",       expected: "0",   description: "Скидка больше суммы — результат 0" },
  ],

  hints: [
    "Баг 1: посмотри на `range(len(orders) + 1)`. Список из N элементов имеет индексы от 0 до N-1. Что даёт `range(N+1)`? Последний индекс будет N — а его не существует. Убери `+ 1`.",
    "Баг 2: `total = total - discount * 10` — скидку нужно просто вычесть, без умножения на 10. Исправь на `total = total - discount`. Баг 3: `total == 0` — это сравнение, а не присваивание. Нужно `total = 0` (один знак равенства).",
    "Полное исправленное решение:\n```python\ndef calculate_total(orders, discount=0):\n    total = 0\n    for i in range(len(orders)):\n        total = total + orders[i]\n    if discount > 0:\n        total = total - discount\n    if total < 0:\n        total = 0\n    return total\n```",
  ],

  isAvailable: true,
};

export default lesson;
