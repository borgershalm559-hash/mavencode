import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Первая программа: Hello, World!",
  order: 2,
  type: "code",
  language: "python",
  xpReward: 100,
  content: `# Первая программа: Hello, World!

## 🎯 Что научишься делать
- Использовать функцию \`print()\` для вывода текста на экран
- Работать со строками: кавычки, одинарные и двойные
- Писать комментарии в коде с помощью \`#\`
- Хранить текст в переменной и выводить её

## 🔍 Зачем это нужно
«Hello, World!» — это традиция. Каждый программист, на каком бы языке ни начинал, первым делом пишет именно эту программу. Это как первый шаг: маленький, но настоящий. После этого урока у тебя будет рабочая программа на Python — не какой-то учебный пример, а настоящий код, который можно запустить.

## 📚 Теория

### Функция \`print()\` — твой главный инструмент вывода

Представь, что \`print()\` — это громкоговоритель. Ты кладёшь туда текст, и он выводит его на экран. Всё просто.

\`\`\`python
print("Привет, мир!")
\`\`\`

Что здесь происходит:
- \`print\` — имя встроенной функции Python. Функция — это готовый инструмент, который умеет что-то делать.
- Круглые скобки \`()\` — сюда передаётся то, что нужно вывести.
- \`"Привет, мир!"\` — это **строка** (текстовые данные). Строки всегда берутся в кавычки.

### Строки: одинарные и двойные кавычки

В Python строку можно заключить как в одинарные, так и в двойные кавычки — результат одинаковый:

\`\`\`python
print("Привет")   # работает
print('Привет')   # тоже работает, абсолютно то же самое
\`\`\`

Когда это важно? Когда сама строка содержит кавычки:

\`\`\`python
print("Он сказал: 'Привет'")   # двойные снаружи, одинарные внутри — ОК
print('Он сказал: "Привет"')   # одинарные снаружи, двойные внутри — ОК
\`\`\`

Правило простое: **кавычка открытия и закрытия должны совпадать**. Если начал с \`"\` — заканчивай \`"\`.

### Комментарии: заметки для людей

Знак \`#\` (решётка) начинает **комментарий** — строку, которую Python полностью игнорирует. Комментарии нужны тебе и другим программистам, не компьютеру:

\`\`\`python
# Это комментарий — Python его не видит
print("Привет")   # Комментарий может быть и в конце строки с кодом
\`\`\`

Хорошие комментарии объясняют **зачем**, а не **что**. «Выводим приветствие» — плохой комментарий (и так видно из кода). «Приветствие показывается только при первом входе» — хороший.

### Переменные: коробки с названиями

**Переменная** — это коробка с именем, в которую можно положить данные. Думай о ней как о наклейке на коробке: ты пишешь имя и кладёшь туда что-то.

\`\`\`python
message = "Привет, мир!"   # Кладём строку в переменную message
print(message)              # Выводим содержимое переменной
\`\`\`

Что здесь происходит построчно:
1. \`message = "Привет, мир!"\` — создаём переменную с именем \`message\` и сохраняем в ней строку \`"Привет, мир!"\`. Знак \`=\` — это **присваивание** (не проверка равенства!).
2. \`print(message)\` — передаём переменную \`message\` в функцию \`print()\`. Python берёт значение из коробки и выводит его.

Переменная и строка в кавычках дают одинаковый результат на экране, но переменная удобнее когда значение используется несколько раз или может меняться.

## 🧪 Примеры

\`\`\`python
# Простой вывод строки напрямую
print("Привет, мир!")
# Вывод: Привет, мир!

# То же самое через переменную
greeting = "Привет, мир!"
print(greeting)
# Вывод: Привет, мир!

# Несколько вызовов print — каждый на новой строке
print("Строка первая")
print("Строка вторая")
# Вывод:
# Строка первая
# Строка вторая

# Одинарные и двойные кавычки — одно и то же
print('Одинарные кавычки тоже работают')
# Вывод: Одинарные кавычки тоже работают
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Забыть кавычки.** \`print(Привет)\` — Python подумает, что \`Привет\` — это имя переменной, а не текст, и выдаст ошибку \`NameError: name 'Привет' is not defined\`. Текст всегда в кавычках.

2. **Опечатка в названии функции.** \`Print("Привет")\` или \`PRINT("Привет")\` не работают — Python чувствителен к регистру. Только строчными: \`print()\`.

3. **Незакрытая скобка.** \`print("Привет"\` без закрывающей \`)\` — Python ждёт продолжения и ругается \`SyntaxError\`.

4. **Смешивание кавычек.** \`print("Привет')\` — открыли двойной, закрыли одинарной. Ошибка \`SyntaxError\`.

5. **Пробел перед \`print\`.** Если в начале строки есть лишний пробел — \`  print("Привет")\` — Python решит, что это вложенный блок кода и выдаст \`IndentationError\`. На первом уровне пробелов быть не должно.

## 💡 Мини-тест самопроверки

1. Что выведет \`print("Hello")\`? (Ответ: \`Hello\`)
2. Чем отличается \`print("текст")\` от \`print('текст')\`? (Ответ: ничем, оба работают одинаково)
3. Что делает символ \`#\` в коде? (Ответ: начинает комментарий — Python эту строку игнорирует)

## 🛠️ Задание

Создай переменную \`message\` со значением \`"Привет, мир!"\` и выведи её на экран через \`print()\`.

Тест проверит значение переменной \`message\` — убедись, что текст совпадает точно, включая запятую и восклицательный знак.`,

  starterCode: `# Создай переменную message со значением "Привет, мир!"
# Затем выведи её через print()

message = "___"
print(message)
`,

  solution: `message = "Привет, мир!"`,

  tests: [
    {
      input: "message",
      expected: "Привет, мир!",
      description: "Переменная message содержит правильный текст",
    },
  ],

  hints: [
    "Функция print() выводит то, что ты передаёшь в скобки. Текст нужно брать в кавычки — одинарные или двойные. Переменная создаётся так: имя = значение.",
    "Попробуй так: `message = \"Привет, мир!\"` на первой строке, затем `print(message)`. Знак `=` означает присваивание — кладём значение в переменную.",
    "Полное решение:\n```python\nmessage = \"Привет, мир!\"\nprint(message)\n```",
  ],

  isAvailable: true,
};

export default lesson;
