import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Функции: многоразовые блоки кода",
  order: 17,
  type: "code",
  language: "python",
  xpReward: 150,
  content: `# Функции: многоразовые блоки кода

## 🎯 Что научишься делать
- Объявлять функции с помощью \`def\`
- Передавать параметры и получать возвращаемое значение через \`return\`
- Понимать разницу между параметром и аргументом
- Объяснить, зачем вообще нужны функции

## 🔍 Зачем это нужно
До этого момента весь твой код выполнялся сверху вниз — одной простыней. Но представь, что тебе нужно посчитать факториал в пяти разных местах программы. Без функций придётся копировать одни и те же строки снова и снова. Изменишь логику в одном месте — забудешь изменить в остальных. Функции решают эту проблему: ты пишешь логику один раз, а вызываешь сколько угодно раз.

## 📚 Теория

### Метафора: кофемашина

Представь кофемашину. Ты кладёшь зерно (аргумент), нажимаешь кнопку (вызов функции), и через несколько секунд получаешь кофе (возвращаемое значение). Тебе не нужно знать, как работает перколяция или какое давление насоса оптимально. Ты просто передаёшь входные данные и получаешь результат.

Функция в Python — это точно такая же кофемашина. Снаружи виден только «интерфейс»: что подаём, что получаем. Что происходит внутри — детали реализации.

### Синтаксис: как объявить функцию

\`\`\`python
def greet(name):
    message = "Привет, " + name + "!"
    return message
\`\`\`

Разберём каждый элемент:

- **\`def\`** — ключевое слово, с которого начинается объявление любой функции. Сокращение от «define» — определить.
- **\`greet\`** — имя функции. Называй функцию глаголом или глагольной группой: \`calculate\`, \`get_name\`, \`is_valid\`. Имя должно описывать, что функция *делает*.
- **\`(name)\`** — список параметров в круглых скобках. Здесь: один параметр \`name\`. Параметров может быть несколько (через запятую), или ноль (тогда скобки пустые: \`()\`).
- **\`:\`** — двоеточие после скобок обязательно. Это знакомо по \`if\` и циклам — так Python отмечает начало блока.
- **Тело функции** — всё, что написано с отступом (4 пробела). Тело выполняется только при вызове функции.
- **\`return message\`** — возвращает значение из функции. Как только Python встречает \`return\`, выполнение функции прекращается, и результат передаётся туда, где функцию вызвали.

### Параметры vs аргументы

Это разные понятия, которые часто путают:

- **Параметр** — имя, которое ты пишешь в скобках при объявлении (\`def\`). Это как ярлык на ячейке памяти внутри функции.
- **Аргумент** — конкретное значение, которое ты передаёшь при вызове функции.

\`\`\`python
def square(x):       # x — это ПАРАМЕТР (имя)
    return x * x

result = square(5)   # 5 — это АРГУМЕНТ (значение)
\`\`\`

Проще запомнить так: параметры — в определении (def), аргументы — при вызове.

### Как вызвать функцию

Объявление функции само по себе ничего не запускает. Функция выполняется только когда ты её **вызываешь** — пишешь имя функции с круглыми скобками:

\`\`\`python
def greet(name):
    return "Привет, " + name + "!"

# Вызов:
result = greet("Алиса")   # передаём аргумент "Алиса"
print(result)              # Привет, Алиса!

# Можно передать другой аргумент — та же функция, другой результат:
print(greet("Борис"))      # Привет, Борис!
print(greet("Вера"))       # Привет, Вера!
\`\`\`

Обрати внимание: код внутри функции выполняется три раза — по одному разу на каждый вызов. Функция переиспользуется.

### return: что он делает

\`return\` делает две вещи сразу:
1. Завершает выполнение функции — код после \`return\` не выполняется.
2. Передаёт значение обратно в место вызова.

\`\`\`python
def absolute_value(n):
    if n < 0:
        return -n      # выходим здесь, если n отрицательное
    return n           # выходим здесь в остальных случаях

print(absolute_value(-5))   # 5
print(absolute_value(3))    # 3
\`\`\`

Функция может иметь несколько \`return\` в разных ветках условия — как в примере выше. Python остановится на первом встреченном.

### Функция без return возвращает None

Если функция не содержит \`return\` (или \`return\` без значения), Python неявно возвращает специальное значение \`None\` — «ничего»:

\`\`\`python
def say_hello():
    print("Привет!")   # просто выводит на экран, ничего не возвращает

result = say_hello()   # Привет! — напечатается
print(result)          # None — потому что функция ничего не вернула
\`\`\`

Это важно помнить: если ты написал функцию, но забыл \`return\`, а потом пытаешься использовать её результат в вычислениях — получишь неожиданный \`None\` и трудноуловимую ошибку.

### Docstring: документация прямо в коде

В Python принято документировать функции через **docstring** — специальную строку в тройных кавычках сразу после \`def\`:

\`\`\`python
def circle_area(radius):
    """Возвращает площадь круга с заданным радиусом.

    Использует приближение π ≈ 3.14.
    """
    return 3.14 * radius ** 2
\`\`\`

Docstring не влияет на выполнение кода. Но IDE и инструменты документации умеют его читать и показывать в подсказках. В Python 101 можно писать их по желанию, а в продакшн-коде — обязательно.

### Зачем функции: три главные причины

**1. DRY — Don't Repeat Yourself (не повторяйся)**

Без функций:
\`\`\`python
# Считаем площадь трёх кругов
area1 = 3.14 * 5 ** 2
area2 = 3.14 * 10 ** 2
area3 = 3.14 * 7 ** 2
\`\`\`

С функцией:
\`\`\`python
def circle_area(r):
    return 3.14 * r ** 2

area1 = circle_area(5)
area2 = circle_area(10)
area3 = circle_area(7)
\`\`\`

Если нужно поменять формулу (например, использовать \`math.pi\` вместо \`3.14\`) — достаточно изменить одно место вместо трёх.

**2. Читаемость**

Функция с понятным именем — это самодокументирующийся код. Сравни:
\`\`\`python
# Без функции — что происходит?
if n > 0 and n % 2 == 0 and n <= 100:
    ...

# С функцией — сразу понятно
def is_valid_even_in_range(n):
    return n > 0 and n % 2 == 0 and n <= 100

if is_valid_even_in_range(n):
    ...
\`\`\`

**3. Тестируемость**

Функции легко тестировать: подаёшь конкретные входные данные, проверяешь конкретный результат. Весь этот курс именно так и устроен — каждое задание тестирует твою функцию с набором входных значений.

## 🧪 Примеры

\`\`\`python
# Функция с двумя параметрами
def rectangle_area(width, height):
    return width * height

print(rectangle_area(5, 3))    # 15
print(rectangle_area(10, 4))   # 40
\`\`\`

\`\`\`python
# Функция с условием внутри
def max_of_two(a, b):
    if a >= b:
        return a
    return b

print(max_of_two(7, 3))    # 7
print(max_of_two(2, 9))    # 9
print(max_of_two(5, 5))    # 5
\`\`\`

\`\`\`python
# Функция без параметров
def get_greeting():
    return "Добро пожаловать в Python!"

message = get_greeting()
print(message)    # Добро пожаловать в Python!
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Забыть \`return\`.** Функция работает, но возвращает \`None\` вместо результата. Всегда проверяй: если функция должна что-то вычислить — там должен быть \`return\`.

2. **Вызвать функцию до объявления.** В Python функцию нужно объявить (\`def ...\`) до того, как её вызываешь. Если поменять местами вызов и объявление — получишь \`NameError\`.

3. **Забыть скобки при вызове.** \`greet\` — это ссылка на объект функции, \`greet()\` — вызов. Без скобок функция не выполнится.

4. **Передать неправильное количество аргументов.** Если функция объявлена с двумя параметрами, а ты передаёшь один — получишь \`TypeError: missing 1 required positional argument\`.

5. **Смешать \`print\` и \`return\`.** \`print\` выводит на экран, но не возвращает значение. Если задание требует \`return\`, не заменяй его \`print\` — тесты не пройдут.

## 💡 Мини-тест самопроверки

1. Что вернёт функция, если в ней нет \`return\`? (Ответ: \`None\`)
2. Как называется значение, которое передаётся функции при вызове? (Ответ: аргумент)
3. Что произойдёт, если написать \`result = my_func\` вместо \`result = my_func()\`? (Ответ: \`result\` станет ссылкой на объект функции, а не результатом её выполнения)

## 🛠️ Задание

Напиши **две функции**:

**Функция 1:** \`factorial(n)\` — возвращает факториал числа \`n\`.

Факториал — это произведение всех целых чисел от 1 до \`n\`. Например:
- \`factorial(5)\` = 1 × 2 × 3 × 4 × 5 = 120
- \`factorial(0)\` = 1 (по математическому соглашению)

**Функция 2:** \`is_even(n)\` — возвращает \`True\`, если \`n\` чётное, и \`False\`, если нечётное.

Подсказка: число чётное, если остаток от деления на 2 равен нулю (\`n % 2 == 0\`).`,

  starterCode: `def factorial(n):
    # n! = 1 * 2 * 3 * ... * n
    # Для n=0 вернуть 1 (краевой случай)
    pass


def is_even(n):
    # Используй оператор % (остаток от деления)
    pass`,

  solution: `def factorial(n):
    if n == 0:
        return 1
    result = 1
    for i in range(1, n + 1):
        result = result * i
    return result


def is_even(n):
    return n % 2 == 0`,

  tests: [
    { input: "factorial(0)",  expected: "1",     description: "0! = 1 (краевой случай)" },
    { input: "factorial(5)",  expected: "120",   description: "5! = 120" },
    { input: "factorial(1)",  expected: "1",     description: "1! = 1" },
    { input: "is_even(4)",    expected: "True",  description: "4 чётное" },
    { input: "is_even(7)",    expected: "False", description: "7 нечётное" },
    { input: "is_even(0)",    expected: "True",  description: "0 считается чётным" },
  ],

  hints: [
    "Для \`factorial\`: начни с краевого случая \`if n == 0: return 1\`. Потом создай переменную \`result = 1\` и умножай её на каждое число от 1 до n включительно, используя \`for i in range(1, n + 1)\`. Для \`is_even\`: вернуй результат сравнения \`n % 2 == 0\` — это уже булево значение True или False.",
    "Скелет factorial:\n```python\ndef factorial(n):\n    if n == 0:\n        return 1\n    result = 1\n    for i in range(1, n + 1):\n        result = result * i\n    return result\n```\nДля is_even достаточно одной строки: \`return n % 2 == 0\`.",
    "Полное решение:\n```python\ndef factorial(n):\n    if n == 0:\n        return 1\n    result = 1\n    for i in range(1, n + 1):\n        result = result * i\n    return result\n\n\ndef is_even(n):\n    return n % 2 == 0\n```",
  ],

  isAvailable: true,
};

export default lesson;
