import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Область видимости: local vs global",
  order: 19,
  type: "quiz",
  language: "python",
  xpReward: 130,
  content: `# Область видимости: local vs global

## 🎯 Что научишься делать
- Понимать, что такое область видимости (scope)
- Различать локальные и глобальные переменные
- Объяснить, как Python ищет имена переменных
- Знать, когда (и почему редко) используется \`global\`

## 🔍 Зачем это нужно
Когда ты пишешь функции, возникает важный вопрос: если внутри функции создать переменную \`x\` — это та же самая \`x\`, что снаружи, или другая? Ответ влияет на всё: на то, как работает твой код, откуда берутся значения и почему иногда изменения «не работают». Понимание областей видимости — необходимый фундамент для написания предсказуемого, чистого кода.

## 📚 Теория

### Метафора: комнаты в доме

Представь дом с несколькими комнатами. В прихожей стоят общие тапочки — ими могут пользоваться все. Но у каждой комнаты есть свои вещи: в спальне — подушка, на кухне — сковорода. Вещи из одной комнаты невидимы из другой.

В Python:
- **Глобальная область** — прихожая. Переменные, объявленные на уровне модуля (вне всех функций).
- **Локальная область** — каждая отдельная комната. Переменные, объявленные внутри функции.

Функция может «зайти в прихожую» и взять тапочки (прочитать глобальную переменную). Но если она хочет поставить туда новые тапочки (изменить глобальную переменную), нужен специальный ключ — ключевое слово \`global\`.

### Локальные переменные

Переменная, созданная внутри функции, существует **только** пока функция выполняется. После завершения функции она исчезает.

\`\`\`python
def calculate():
    result = 42    # result — локальная переменная
    return result

calculate()
print(result)    # NameError: name 'result' is not defined
\`\`\`

Переменная \`result\` живёт только «в комнате» функции \`calculate\`. Снаружи она недоступна.

Это хорошо: каждая функция работает в своём изолированном пространстве. Переменные разных функций не мешают друг другу, даже если называются одинаково.

\`\`\`python
def func_a():
    value = 10   # локальная value в func_a
    return value

def func_b():
    value = 99   # локальная value в func_b — ДРУГАЯ переменная!
    return value

print(func_a())   # 10
print(func_b())   # 99
\`\`\`

Переменные \`value\` в \`func_a\` и \`func_b\` — совершенно разные объекты, просто с одинаковыми именами.

### Глобальные переменные

Переменная, объявленная вне функций — на уровне модуля — называется **глобальной**. Она видна везде в модуле:

\`\`\`python
greeting = "Привет"    # глобальная переменная

def say_hello(name):
    return greeting + ", " + name + "!"   # читаем глобальную — это разрешено

print(say_hello("Анна"))    # Привет, Анна!
print(say_hello("Борис"))   # Привет, Борис!
\`\`\`

Функция спокойно **читает** глобальную переменную. Python ищет имя сначала в локальной области, не находит — идёт искать в глобальной. Нашёл — использует.

### Попытка изменить глобальную переменную (сюрприз!)

А что если попробовать изменить глобальную переменную изнутри функции?

\`\`\`python
counter = 0    # глобальная

def increment():
    counter = counter + 1    # что произойдёт?
    return counter

increment()    # UnboundLocalError: local variable 'counter' referenced before assignment
\`\`\`

Получаем ошибку. Почему?

Когда Python видит \`counter = ...\` внутри функции, он **немедленно** решает: «это локальная переменная». И при попытке прочитать \`counter\` в правой части \`counter + 1\` — находит только локальную, которую ещё не определили. Отсюда ошибка.

Это не баг — это защита. Python не позволяет случайно изменить глобальное состояние из функции.

### Ключевое слово global

Если тебе действительно нужно изменить глобальную переменную изнутри функции — используй \`global\`:

\`\`\`python
counter = 0

def increment():
    global counter   # объявляем: counter — это глобальная переменная
    counter = counter + 1

increment()
increment()
print(counter)    # 2
\`\`\`

Объявление \`global counter\` говорит Python: «когда я пишу \`counter\` в этой функции — имею в виду глобальную переменную, а не создаю новую локальную».

### Почему global — плохая практика

Хотя \`global\` технически работает, в реальных проектах его используют редко и с осторожностью. Причина: **глобальное состояние делает код непредсказуемым**.

Представь функцию, которая изменяет глобальную переменную. Теперь результат функции зависит не только от её аргументов, но и от того, что происходило с глобальной переменной до вызова. Это называется **побочный эффект** — его труднее тестировать и отлаживать.

Чистый подход: **передавай данные через параметры, возвращай через return**:

\`\`\`python
# Плохо (использует global):
total = 0
def add_to_total(x):
    global total
    total = total + x

# Хорошо (чистая функция):
def add(current_total, x):
    return current_total + x

total = 0
total = add(total, 5)
total = add(total, 3)
print(total)    # 8
\`\`\`

Второй вариант легко тестировать: \`add(10, 5)\` всегда вернёт \`15\`, независимо от внешнего состояния.

### Shadowing: перекрытие имён

Когда внутри функции создаётся переменная с тем же именем, что и глобальная — локальная «перекрывает» (shadows) глобальную внутри этой функции:

\`\`\`python
x = "глобальная"

def demo():
    x = "локальная"    # эта x перекрывает глобальную внутри функции
    print(x)           # локальная

demo()
print(x)               # глобальная — глобальная не тронута
\`\`\`

Глобальная \`x\` при этом остаётся нетронутой. Функция создала свою собственную \`x\`, которая существует только в её области.

### Правило LEGB

Python ищет имена переменных по схеме **LEGB** (от внутреннего к внешнему):

- **L**ocal — локальная область функции
- **E**nclosing — область объемлющих функций (для вложенных функций, подробнее в Python 201)
- **G**lobal — уровень модуля
- **B**uilt-in — встроенные имена Python (\`print\`, \`len\`, \`range\` и т.д.)

Python идёт по этому списку слева направо и останавливается, когда нашёл имя. Это объясняет, почему локальная переменная «побеждает» глобальную — она ближе.

### Изменение изменяемых объектов

Есть тонкость: если глобальная переменная — изменяемый объект (например, список или словарь), функция может изменять **содержимое** этого объекта без \`global\`:

\`\`\`python
numbers = [1, 2, 3]    # глобальный список

def add_number(n):
    numbers.append(n)   # изменяем содержимое списка — это разрешено без global

add_number(4)
print(numbers)    # [1, 2, 3, 4]
\`\`\`

Это работает, потому что мы не переприсваиваем \`numbers\` — мы вызываем метод объекта. Переменная \`numbers\` по-прежнему указывает на тот же список, просто список теперь другой.

Но это тоже побочный эффект — в продакшн-коде лучше передавать список через параметр.

## 🧪 Примеры

\`\`\`python
# Пример: локальная переменная не влияет на глобальную
name = "Алиса"

def rename():
    name = "Борис"    # создаём локальную, не трогаем глобальную
    return name

print(rename())   # Борис
print(name)       # Алиса — глобальная не изменилась
\`\`\`

\`\`\`python
# Пример: чтение глобальной константы в функции (хорошая практика)
PI = 3.14159    # константа на уровне модуля (по соглашению UPPER_CASE)

def circle_area(radius):
    return PI * radius ** 2    # читаем PI — это нормально

print(circle_area(10))   # 314.159
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Ожидать, что функция изменит глобальную переменную без \`global\`.** \`counter = counter + 1\` внутри функции создаёт локальную переменную и сразу же падает с \`UnboundLocalError\`.

2. **Злоупотреблять \`global\`.** Если в коде много \`global\` — это сигнал, что архитектура плохая. Передавай значения через параметры и \`return\`.

3. **Думать, что одинаковое имя == одна переменная.** \`x\` в \`main()\` и \`x\` в \`helper()\` — совершенно разные переменные, просто с одинаковыми именами.

4. **Путать изменение значения переменной и изменение содержимого объекта.** \`numbers = []\` (переприсвоение) требует \`global\`. \`numbers.append(1)\` (изменение содержимого) — нет.

## 💡 Мини-тест самопроверки

Прочитай вопросы и попробуй ответить сам перед тем, как открыть ответ.

## 📝 Проверь себя

**Вопрос 1:** Что выведет этот код?
\`\`\`python
x = 10

def foo():
    x = 5
    return x

print(foo())
print(x)
\`\`\`

<details><summary>Ответ</summary>

Сначала выведет \`5\`, потом \`10\`.

Внутри \`foo()\` создаётся **локальная** переменная \`x = 5\`. Она не изменяет глобальную \`x = 10\`. После выхода из функции глобальная \`x\` остаётся равной \`10\`.

</details>

---

**Вопрос 2:** Что произойдёт при запуске этого кода?
\`\`\`python
score = 100

def bonus():
    score = score + 50
    return score

print(bonus())
\`\`\`

<details><summary>Ответ</summary>

Будет ошибка: \`UnboundLocalError: local variable 'score' referenced before assignment\`.

Как только Python видит \`score = ...\` внутри функции, он помечает \`score\` как **локальную** переменную. Но в правой части \`score + 50\` Python пытается прочитать локальную \`score\`, которую ещё не определили — отсюда ошибка.

Исправление: либо добавить \`global score\` в начало функции, либо (лучше) передать \`score\` параметром: \`def bonus(score): return score + 50\`.

</details>

---

**Вопрос 3:** Что выведет этот код?
\`\`\`python
greeting = "Привет"

def say(name):
    return greeting + ", " + name

print(say("Маша"))
print(greeting)
\`\`\`

<details><summary>Ответ</summary>

Выведет \`"Привет, Маша"\`, потом \`"Привет"\`.

Функция \`say\` **читает** глобальную переменную \`greeting\` — это разрешено без \`global\`. Чтение глобальных переменных безопасно; проблемы возникают только при попытке **изменить** их.

</details>

---

**Вопрос 4:** Что выведет этот код?
\`\`\`python
items = ["яблоко", "банан"]

def add_item(item):
    items.append(item)

add_item("вишня")
print(items)
\`\`\`

<details><summary>Ответ</summary>

Выведет \`['яблоко', 'банан', 'вишня']\`.

Функция **не переприсваивает** \`items\`, а вызывает метод \`append\` объекта, на который указывает \`items\`. Поскольку переприсвоения нет, \`global\` не нужен. Список изменился.

Это технически работает, но является побочным эффектом. В чистом коде лучше передавать список параметром и возвращать изменённый: \`def add_item(lst, item): return lst + [item]\`.

</details>

---

**Вопрос 5:** Что выведет этот код?
\`\`\`python
n = 1

def double():
    global n
    n = n * 2

double()
double()
print(n)
\`\`\`

<details><summary>Ответ</summary>

Выведет \`4\`.

Объявление \`global n\` позволяет функции изменять глобальную переменную \`n\`. После первого вызова \`n = 1 * 2 = 2\`. После второго вызова \`n = 2 * 2 = 4\`.

Обрати внимание: такой код трудно тестировать — результат \`double()\` зависит от текущего значения глобального \`n\`. Гораздо чище было бы: \`def double(n): return n * 2\`.

</details>`,

  starterCode: "",
  solution: "",
  tests: [],

  hints: [
    "Вспомни: Python ищет имя переменной сначала в **локальной** области функции, и только потом — в **глобальной**. Поэтому если внутри функции есть своя переменная с тем же именем — она «побеждает».",
    "Присваивание \`x = ...\` внутри функции **всегда** создаёт **локальную** переменную, если в начале функции не объявлено \`global x\`. Это объясняет, почему попытка написать \`x = x + 1\` без \`global\` приводит к \`UnboundLocalError\`.",
    "Резюме: читать глобальные переменные из функции — можно без \`global\`. Изменять — только с \`global\`. Но лучше вообще избегать этого: передавай значения через параметры и возвращай через \`return\`. Так код проще читать, тестировать и понимать.",
  ],

  isAvailable: true,
};

export default lesson;
