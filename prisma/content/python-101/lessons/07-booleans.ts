import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Тип bool и логические операторы",
  order: 7,
  type: "fill-blanks",
  language: "python",
  xpReward: 120,
  content: `# Тип \`bool\` и логические операторы

## 🎯 Что научишься делать
- Использовать булев тип \`bool\` — \`True\` и \`False\`
- Сравнивать значения с помощью операторов \`==\`, \`!=\`, \`<\`, \`<=\`, \`>\`, \`>=\`
- Комбинировать условия через \`and\`, \`or\`, \`not\`
- Понимать приоритет логических операторов
- Писать условия для реальных задач

## 🔍 Зачем это нужно
Программа, которая всегда делает одно и то же — это не очень полезная программа. Настоящие программы принимают решения: «если пользователь авторизован — покажи страницу, иначе — перенаправь на вход». «Если возраст >= 18 и есть гражданство — разреши голосовать». Всё это работает через логику \`True\` / \`False\`. Без булевого типа нет условий. Без условий нет программ.

## 📚 Теория

### Тип \`bool\`: выключатель на два положения

Представь обычный выключатель света. У него два состояния: **включён** или **выключен**. Никаких полутонов — только два варианта.

Тип \`bool\` (от «boolean» — в честь математика Джорджа Буля) работает точно так же. У него только два возможных значения:
- \`True\` — истина (выключатель включён)
- \`False\` — ложь (выключатель выключен)

\`\`\`python
is_logged_in = True    # пользователь вошёл — да
is_admin = False       # администратор? — нет
\`\`\`

**Внимание!** \`True\` и \`False\` пишутся **с заглавной буквы**. Это не опечатка и не стиль — это обязательно. \`true\` (с маленькой) — это \`NameError\`, потому что Python не знает такого имени.

\`\`\`python
x = True     # правильно
y = true     # NameError: name 'true' is not defined — ОШИБКА!
z = TRUE     # NameError — тоже ОШИБКА!
\`\`\`

### Операторы сравнения

Когда ты сравниваешь два значения, Python возвращает \`True\` или \`False\`. Это и есть булев результат.

| Оператор | Что делает | Пример | Результат |
|---|---|---|---|
| \`==\` | равно | \`5 == 5\` | \`True\` |
| \`!=\` | не равно | \`5 != 3\` | \`True\` |
| \`<\` | меньше | \`3 < 5\` | \`True\` |
| \`<=\` | меньше или равно | \`5 <= 5\` | \`True\` |
| \`>\` | больше | \`7 > 3\` | \`True\` |
| \`>=\` | больше или равно | \`18 >= 18\` | \`True\` |

\`\`\`python
age = 20
print(age >= 18)    # True — возраст подходит
print(age == 18)    # False — возраст не равен 18 ровно
print(age != 21)    # True — действительно не 21
\`\`\`

Запомни разницу: \`=\` — это присваивание («положи в переменную»), \`==\` — это сравнение («проверь, равны ли»). Путаница между ними — одна из самых частых ошибок новичков.

### Логические операторы: \`and\`, \`or\`, \`not\`

Что если нужно проверить **несколько условий сразу**? Для этого есть логические операторы.

#### \`and\` — оба условия должны быть истинными

\`\`\`python
age = 20
is_citizen = True

can_vote = age >= 18 and is_citizen == True
print(can_vote)   # True — и возраст подходит, и гражданство есть
\`\`\`

Представь, что ты на секретном объекте. \`and\` — это двое охранников на двух дверях: чтобы пройти, нужно убедить **обоих**. Если хоть одна дверь закрыта — проход заблокирован.

| Левое | Правое | Результат |
|---|---|---|
| True | True | **True** |
| True | False | False |
| False | True | False |
| False | False | False |

#### \`or\` — хотя бы одно условие истинно

\`\`\`python
is_vip = False
has_ticket = True

can_enter = is_vip or has_ticket
print(can_enter)   # True — билет есть, этого достаточно
\`\`\`

\`or\` — это охранник у одной двери: достаточно убедить хоть одного из двух вариантов.

| Левое | Правое | Результат |
|---|---|---|
| True | True | True |
| True | False | True |
| False | True | True |
| False | False | **False** |

#### \`not\` — инвертирует значение

\`\`\`python
is_banned = False
can_post = not is_banned
print(can_post)   # True — не забанен, значит может
\`\`\`

\`not\` переворачивает: \`True\` становится \`False\`, \`False\` становится \`True\`.

### Приоритет операторов

Как и в математике, операторы выполняются в определённом порядке:

1. Сначала операторы сравнения (\`==\`, \`!=\`, \`<\`, \`<=\`, \`>\`, \`>=\`)
2. Потом \`not\`
3. Потом \`and\`
4. Последним \`or\`

\`\`\`python
# Без скобок:
True or False and False   # = True or (False and False) = True or False = True

# Со скобками — явный контроль:
(True or False) and False  # = True and False = False
\`\`\`

Когда сомневаешься — используй скобки. Они не замедляют код и делают намерения очевидными.

### Булевы значения в условиях

Тип \`bool\` — это строительный материал для условий (\`if\`), которые ты изучишь в следующем уроке. Уже сейчас важно понять: условие — это просто выражение, которое Python вычисляет и получает \`True\` или \`False\`.

\`\`\`python
def can_vote(age, is_citizen):
    return age >= 18 and is_citizen == True

# Функция возвращает bool — это и есть ответ
result = can_vote(20, True)
print(result)   # True
\`\`\`

## 🧪 Примеры

\`\`\`python
# Проверка возраста для просмотра фильма
age = 16
is_adult = age >= 18
print(is_adult)   # False
\`\`\`

\`\`\`python
# Комбинирование условий
temperature = 25
is_sunny = True

good_for_walk = temperature > 15 and is_sunny
print(good_for_walk)   # True — тепло и солнечно
\`\`\`

\`\`\`python
# not — инверсия
is_weekend = False
should_work = not is_weekend
print(should_work)   # True — не выходной, значит работаем
\`\`\`

\`\`\`python
# Граничные случаи имеют значение!
print(18 >= 18)   # True — «больше или равно» включает само значение
print(17 >= 18)   # False — 17 меньше 18
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Писать \`true\` или \`false\` с маленькой буквы.** Python чувствителен к регистру. Только \`True\` и \`False\` — строго с заглавной. \`true\` вызовет \`NameError\`.

2. **Путать \`=\` и \`==\`.** \`age = 18\` — присвоить 18. \`age == 18\` — проверить, равно ли 18. Внутри условия всегда \`==\`, никогда \`=\`.

3. **Забывать о приоритете \`and\` над \`or\`.** \`a or b and c\` вычисляется как \`a or (b and c)\`, а не \`(a or b) and c\`. Если нужен второй вариант — ставь скобки.

4. **Не использовать скобки в сложных условиях.** Даже если приоритет правильный — скобки делают код понятнее. \`(age >= 18) and (is_citizen == True)\` читается легче, чем \`age >= 18 and is_citizen == True\`.

5. **Сравнивать с \`True\` там, где не нужно.** \`if is_active == True:\` — избыточно, потому что \`is_active\` уже является булевым значением. Достаточно написать \`if is_active:\`. Но явное \`== True\` — это не ошибка, особенно для начинающих.

## 💡 Мини-тест самопроверки

1. Что вернёт \`5 > 3 and 10 < 7\`?
<details><summary>Ответ</summary>\`False\` — первое условие True, второе False, and требует оба True.</details>

2. Что вернёт \`not True\`?
<details><summary>Ответ</summary>\`False\` — not инвертирует значение.</details>

3. Чем \`=\` отличается от \`==\` в контексте булевой логики?
<details><summary>Ответ</summary>\`=\` присваивает значение переменной. \`==\` сравнивает два значения и возвращает \`True\` или \`False\`.</details>

## 🛠️ Задание

Заполни пропуски в функции \`can_vote(age, is_citizen)\`, чтобы она возвращала \`True\`, если человек имеет право голосовать: его возраст **не менее 18** И он является гражданином.

Стартовый код содержит пропуски \`___\` — замени их правильными операторами (\`>=\`, \`and\`, \`==\`).`,

  starterCode: `def can_vote(age, is_citizen):
    return age ___ 18 ___ is_citizen ___ True`,

  solution: `def can_vote(age, is_citizen):
    return age >= 18 and is_citizen == True`,

  tests: [
    { input: "can_vote(20, True)",  expected: "True",  description: "Совершеннолетний гражданин" },
    { input: "can_vote(15, True)",  expected: "False", description: "Несовершеннолетний гражданин" },
    { input: "can_vote(20, False)", expected: "False", description: "Негражданин" },
    { input: "can_vote(18, True)",  expected: "True",  description: "Граничный кейс: ровно 18" },
  ],

  hints: [
    "Тебе нужны три оператора для трёх пропусков. Первый пропуск — это оператор сравнения между `age` и `18`. Какой оператор означает «больше или равно»? Второй пропуск — логический оператор, который требует истинности ОБОИХ условий. Третий — сравнение `is_citizen` с `True`.",
    "`and` требует, чтобы оба условия были истинными. Оператор `>=` означает «больше или равно» и включает граничное значение 18. Для сравнения переменной с `True` используй `==`. Итого: `age >= 18 and is_citizen == True`.",
    "Полное решение:\n```python\ndef can_vote(age, is_citizen):\n    return age >= 18 and is_citizen == True\n```",
  ],

  isAvailable: true,
};

export default lesson;
