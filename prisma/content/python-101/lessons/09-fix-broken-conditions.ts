import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Найди баг: сломанные условия",
  order: 9,
  type: "fix-bug",
  language: "python",
  xpReward: 140,
  content: `# Найди баг: сломанные условия

## 🎯 Что научишься делать
- Различать \`=\`, \`==\` и \`>=\` — три разных оператора с разным смыслом
- Вызывать методы правильно: с круглыми скобками
- Замечать логические ловушки типа \`or True\`, которые делают проверку бессмысленной
- Читать чужой (или свой прошлый) код критически

## 🔍 Зачем это нужно
Умение находить баги — такой же важный навык, как умение их писать. Профессиональные разработчики тратят значительную часть времени на отладку, а не на написание нового кода. Три ошибки из этого урока встречаются в реальных проектах постоянно: неправильный оператор сравнения, забытые скобки у метода и логическая ловушка, которая делает проверку бессмысленной.

## 📚 Теория

### Три часто путаемых оператора: \`=\`, \`==\`, \`>=\`

\`\`\`python
x = 5       # ПРИСВАИВАНИЕ — кладёт значение 5 в переменную x
x == 5      # СРАВНЕНИЕ — проверяет, равно ли x числу 5, возвращает True/False
x >= 5      # СРАВНЕНИЕ — проверяет, больше ли x или равно 5
\`\`\`

Использовать \`=\` вместо \`>=\` в условии — это \`SyntaxError\`. Python даже не запустит такой код:

\`\`\`python
# Неправильно:
if len(password) = 8:    # SyntaxError — нельзя присваивать внутри if
    pass

# Правильно:
if len(password) >= 8:   # проверяем: длина не менее 8 символов
    pass
\`\`\`

### Методы нужно вызывать с круглыми скобками

Метод в Python — это функция, прикреплённая к объекту. Чтобы её вызвать, нужны круглые скобки \`()\`. Без скобок ты получишь не результат вызова, а сам объект-метод:

\`\`\`python
text = "hello123"

# Неправильно — isdigit без скобок:
if text[0].isdigit:        # Это всегда True! Ты проверяешь не результат,
    pass                   # а сам метод — он существует, значит truthy

# Правильно — isdigit() с скобками:
if text[0].isdigit():      # Теперь метод вызывается и возвращает True/False
    pass
\`\`\`

Это классическая ловушка: код не выдаёт ошибку, просто работает не так, как ожидается. \`isdigit\` без скобок — это не вызов, это ссылка на метод. Ссылка на метод — это объект, а любой объект в Python «истинен» в булевом контексте.

### Логическая ловушка: \`or True\`

Добавить \`or True\` к любому условию означает, что это условие **всегда истинно** — независимо от того, что стоит слева:

\`\`\`python
has_digit = False

# Неправильно:
if has_digit or True:      # True ИЛИ True → True
    return True            # Это выполнится всегда!

# Правильно:
if has_digit:              # проверяем только то, что нужно
    return True
\`\`\`

Такой баг иногда появляется при отладке («добавлю \`or True\`, чтобы проверить следующий блок»), а потом случайно остаётся в коде. Результат — функция проверки, которая вообще ничего не проверяет.

## 🧪 Примеры — аналогичные ошибки в другом контексте

\`\`\`python
# Баг 1: = вместо >= (SyntaxError)
# Плохо:
# if age = 18:
#     pass
# Хорошо:
if age >= 18:
    pass

# Баг 2: метод без скобок
# Плохо:
if "hello".upper:          # всегда True — проверяешь существование метода
    print("upper существует как метод")

# Хорошо:
if "hello".upper() == "HELLO":   # вызываешь метод и проверяешь результат
    print("правильно")

# Баг 3: or True делает условие бессмысленным
# Плохо:
x = False
if x or True:              # всегда True
    print("это всегда выполнится")

# Хорошо:
if x:
    print("это выполнится только когда x = True")
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Путать \`=\` и \`>=\` в условиях.** \`if len(s) = 8:\` — это всегда \`SyntaxError\`. Python не позволяет присваивание внутри условия (в отличие от некоторых других языков).

2. **Забывать скобки у методов.** \`ch.isdigit\` без скобок — это ссылка на метод, не его вызов. Результат всегда будет «истинным», что делает проверку бессмысленной.

3. **Оставлять отладочный \`or True\` в коде.** Логика «добавлю сейчас, уберу потом» работает против тебя. Убирай сразу.

4. **Не тестировать граничные случаи.** Функция проверки пароля с \`or True\` пройдёт тест \`check_password("abc123def")\`, но провалится на \`check_password("12")\` — если бы не \`or True\`, который скрывает проблему.

## 💡 Мини-тест самопроверки

1. Что вернёт \`"hello".upper\` (без скобок) в булевом контексте?
<details><summary>Ответ</summary>Всегда \`True\` — потому что \`"hello".upper\` — это объект-метод, а объекты в Python «истинны» в булевом контексте. Нужно \`"hello".upper()\` — с скобками — чтобы вызвать метод.</details>

2. Что не так с условием \`if x or True:\`?
<details><summary>Ответ</summary>Это условие всегда истинно, потому что \`что_угодно or True\` — это всегда \`True\`. Смысл проверки \`x\` полностью теряется.</details>

3. Почему \`if len(s) = 8:\` вызывает \`SyntaxError\`?
<details><summary>Ответ</summary>Потому что \`=\` — это оператор присваивания, а не сравнения. Внутри условия \`if\` нельзя использовать присваивание. Нужно \`==\` для равенства или \`>=\` для «больше или равно».</details>

## 🛠️ Задание

В функции \`check_password(password)\` спрятаны три бага. Функция должна возвращать \`True\`, если:
- длина пароля **не менее 8 символов**, И
- пароль содержит **хотя бы одну цифру**

Найди и исправь все три ошибки. Подсказки в комментариях указывают на строки с багами.`,

  starterCode: `def check_password(password):
    # ⚠️ В этой функции 3 бага — найди и исправь
    if len(password) = 8:  # Баг 1
        has_digit = False
        for ch in password:
            if ch.isdigit:  # Баг 2
                has_digit = True
        if has_digit or True:  # Баг 3
            return True
    return False`,

  solution: `def check_password(password):
    if len(password) >= 8:
        has_digit = False
        for ch in password:
            if ch.isdigit():
                has_digit = True
        if has_digit:
            return True
    return False`,

  tests: [
    { input: "check_password(\"abc123def\")", expected: "True",  description: "8+ символов, есть цифра" },
    { input: "check_password(\"short1\")",    expected: "False", description: "Менее 8 символов" },
    { input: "check_password(\"qwertyui\")",  expected: "False", description: "8+, но без цифр" },
    { input: "check_password(\"password1\")", expected: "True",  description: "9 символов, есть цифра" },
  ],

  hints: [
    "Баг 1 — в строке `if len(password) = 8:`. Знак `=` — это присваивание, Python выдаст SyntaxError. Нужен оператор сравнения: функция должна проверять, что длина НЕ МЕНЕЕ 8 символов. Какой оператор означает «больше или равно»?",
    "Баг 2 — в строке `if ch.isdigit:`. Метод вызывается без скобок — это означает ссылку на метод, а не его выполнение. Добавь `()`: `ch.isdigit()`. Баг 3 — в строке `if has_digit or True:`. Убери `or True` — оно делает условие всегда истинным.",
    "Полное исправленное решение:\n```python\ndef check_password(password):\n    if len(password) >= 8:\n        has_digit = False\n        for ch in password:\n            if ch.isdigit():\n                has_digit = True\n        if has_digit:\n            return True\n    return False\n```",
  ],

  isAvailable: true,
};

export default lesson;
