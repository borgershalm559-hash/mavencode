import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Строки: работа с текстом",
  order: 6,
  type: "code",
  language: "python",
  xpReward: 120,
  content: `# Строки: работа с текстом

## 🎯 Что научишься делать
- Собирать строки из кусочков — через склейку, повторение и f-строки
- Преобразовывать текст: регистр, пробелы, замена подстрок
- Измерять длину строки и понимать, что строки нельзя менять «на месте»
- Писать функции, которые аккуратно обрабатывают пользовательский ввод

## 🔍 Зачем это нужно
Компьютер говорит с человеком через текст. Имя пользователя в приветствии, тема письма, сообщение об ошибке, заголовок кнопки, строка в логе — всё это строки. Если числа — это про «сколько», то строки — это про «что написано».

Представь, что ты делаешь форму регистрации. Пользователь ввёл имя \`"  аня  "\` — с лишними пробелами и с маленькой буквы. Ты не можешь показать это в интерфейсе как есть: нужно убрать пробелы, сделать буквы заглавными, вставить имя в приветствие. Всё это — пять строчек кода, если ты знаешь базовые операции со строками. И ноль строчек, если не знаешь — программа просто не заработает.

## 📚 Теория

### Строка — это последовательность символов
**Строка (string, \`str\`)** — это текст в кавычках. Представь нитку с бусинами: каждая бусина — один символ, и все они нанизаны по порядку.

\`\`\`python
a = "Привет"      # двойные кавычки
b = 'Привет'      # одинарные — то же самое
c = """Привет"""  # тройные — для многострочного текста
\`\`\`

Почему два варианта кавычек? Чтобы удобно вставлять одни внутрь других: \`"Он сказал 'привет'"\` — никаких экранирований. Тройные кавычки нужны, когда текст занимает несколько строк — например, длинное письмо или SQL-запрос.

### Склейка (\`+\`) и повторение (\`*\`)
Две базовые операции, которые ты будешь использовать постоянно.

**Конкатенация** — это склейка двух строк через \`+\`:

\`\`\`python
first = "Python"
second = "101"
print(first + second)        # Python101
print(first + " " + second)  # Python 101 — пробел добавили вручную
\`\`\`

Обрати внимание: \`+\` не ставит пробел между словами. Если нужен пробел — добавь его явно, как отдельную строку \`" "\`.

**Повторение** — это \`*\` с числом. Строка повторяется столько раз, сколько попросили:

\`\`\`python
line = "-" * 20        # двадцать дефисов подряд
laugh = "ha" * 3       # hahaha
\`\`\`

### f-строки — главный инструмент
А теперь — самое важное. Склеивать строки через \`+\` можно, но когда переменных много, код превращается в кашу:

\`\`\`python
name = "Anna"
age = 25
# Старый способ — больно читать:
msg = "My name is " + name + " and I am " + str(age)
\`\`\`

Обрати внимание на \`str(age)\` — без него Python ругается, потому что \`+\` не умеет складывать строку с числом. Это раздражает.

**f-строка (f-string)** решает обе проблемы сразу. Ставишь букву \`f\` перед кавычками — и внутри можешь вставлять переменные в фигурных скобках \`{...}\`:

\`\`\`python
name = "Anna"
age = 25
msg = f"My name is {name} and I am {age}"
print(msg)  # My name is Anna and I am 25
\`\`\`

Что происходит построчно:
- \`f"..."\` — префикс \`f\` говорит Python: «внутри этой строки будут подстановки».
- \`{name}\` — Python берёт значение переменной \`name\` и вставляет его сюда.
- \`{age}\` — число автоматически превращается в текст, \`str()\` не нужен.

Внутри \`{}\` можно писать не только переменные, но и **любые выражения**:

\`\`\`python
a = 7
b = 3
print(f"Sum: {a + b}")                  # Sum: 10
print(f"Name in caps: {name.upper()}")  # Name in caps: ANNA
\`\`\`

### Полезные методы: \`.upper()\`, \`.lower()\`, \`.strip()\`, \`.replace()\`
**Метод** — это функция, которая «живёт» на объекте и вызывается через точку: \`text.upper()\`. У строк методов десятки, но четыре ты будешь использовать чаще всего.

| Метод | Что делает | Пример |
|---|---|---|
| \`.upper()\` | ВСЁ В ВЕРХНИЙ РЕГИСТР | \`"anna".upper()\` → \`"ANNA"\` |
| \`.lower()\` | всё в нижний регистр | \`"ANNA".lower()\` → \`"anna"\` |
| \`.strip()\` | убирает пробелы по краям | \`"  hi  ".strip()\` → \`"hi"\` |
| \`.replace(a, b)\` | меняет все \`a\` на \`b\` | \`"cat".replace("c", "r")\` → \`"rat"\` |

\`\`\`python
raw = "  Hello, World  "
print(raw.strip())                     # "Hello, World" — без крайних пробелов
print(raw.strip().lower())             # "hello, world" — методы можно цепочкой
print(raw.replace("World", "Python"))  # "  Hello, Python  "
\`\`\`

Обрати внимание на цепочку \`raw.strip().lower()\`: сначала убрали пробелы, потом результат перевели в нижний регистр. Так и читается — слева направо.

### Длина строки: \`len()\`
\`len()\` — это встроенная функция, которая возвращает **количество символов** в строке:

\`\`\`python
print(len("Python"))   # 6
print(len(""))         # 0 — пустая строка
print(len("   "))      # 3 — пробелы тоже считаются
\`\`\`

Полезно для проверок: «пароль короче 8 символов — не принимаем», «имя не пустое — можно сохранять».

### Неизменяемость строк
Короткий, но важный момент. Строки в Python **неизменяемы (immutable)**. Это значит: когда ты пишешь \`name.upper()\`, сама переменная \`name\` не меняется — создаётся **новая** строка, и её нужно куда-то присвоить:

\`\`\`python
name = "anna"
name.upper()           # вернёт "ANNA", но результат выбросится!
print(name)            # всё ещё "anna"

name = name.upper()    # вот теперь name стал "ANNA"
\`\`\`

Запомни как мантру: **методы строк возвращают новую строку, а не меняют старую**.

## 🧪 Примеры

\`\`\`python
# 1. Простое приветствие через f-строку
name = "Ivan"
greeting = f"Hello, {name}!"   # подставили имя внутрь шаблона
print(greeting)                 # Hello, Ivan!
\`\`\`

\`\`\`python
# 2. Очистка введённых данных
raw_name = "  anna  "
clean_name = raw_name.strip().upper()   # убрали пробелы, затем — в верхний регистр
print(clean_name)                        # ANNA
\`\`\`

\`\`\`python
# 3. Формирование приветствия
def greet(name, city):
    # strip() убирает пробелы по краям, upper() переводит в верхний регистр
    return f"Hello, {name.strip().upper()}, welcome to {city}!"

print(greet("ivan", "moscow"))     # Hello, IVAN, welcome to moscow!
print(greet("  anna  ", "kazan")) # Hello, ANNA, welcome to kazan!
\`\`\`

\`\`\`python
# 4. Разделитель и повтор
title = "REPORT"
line = "=" * len(title)   # длина разделителя = длине заголовка
print(line)               # ======
print(title)              # REPORT
print(line)               # ======
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Забыть префикс \`f\` перед кавычками.** Написал \`"Hello, {name}"\` без \`f\` — Python не подставит переменную, а выведет фигурные скобки буквально: \`Hello, {name}\`. Всегда проверяй: если внутри есть \`{}\` — перед кавычками должна стоять \`f\`.

2. **Ожидать, что метод изменит строку.** Код \`name.upper()\` сам по себе ничего не делает с переменной — он возвращает новую строку. Нужно писать \`name = name.upper()\` или использовать результат сразу в f-строке: \`f"{name.upper()}"\`.

3. **Путать \`+\` для чисел и для строк.** \`"5" + "3"\` даёт \`"53"\` (склейка), а не \`8\`. Если нужно сложить как числа — сначала преобразуй через \`int()\`: \`int("5") + int("3")\` вернёт \`8\`. Python строго различает строку \`"5"\` и число \`5\`.

4. **Забыть \`.strip()\` при обработке пользовательского ввода.** Пользователи часто случайно добавляют пробелы. Всегда очищай входные данные перед использованием.

## 💡 Мини-тест самопроверки

1. Что выведет \`print("abc" * 3)\`?
<details><summary>Ответ</summary>\`abcabcabc\` — строка повторяется три раза.</details>

2. Что вернёт \`"  Hello  ".strip().lower()\`?
<details><summary>Ответ</summary>\`"hello"\` — сначала убрали пробелы, потом перевели в нижний регистр.</details>

3. Есть переменная \`name = "bob"\`. Что выведет \`print(f"Hi, {name.upper()}!")\`?
<details><summary>Ответ</summary>\`Hi, BOB!\` — метод \`.upper()\` вызывается прямо внутри f-строки.</details>

## 🛠️ Задание
Напиши функцию \`greet(name, city)\`, которая возвращает строку вида \`"Hello, IVAN, welcome to moscow!"\` — то есть приветствие, где:
- имя обрезается от лишних пробелов через \`.strip()\`
- затем переводится в верхний регистр через \`.upper()\`
- город остаётся как есть

Используй f-строку. Возвращаемое значение должно быть строкой (\`str\`).`,

  starterCode: `def greet(name, city):
    # 1. Убери пробелы по краям имени с помощью .strip()
    # 2. Переведи имя в верхний регистр с помощью .upper()
    # 3. Город оставь как есть
    # 4. Собери f-строку: f"Hello, {...}, welcome to {...}!"
    # 5. Верни результат через return
    pass`,

  solution: `def greet(name, city):
    return f"Hello, {name.strip().upper()}, welcome to {city}!"`,

  tests: [
    { input: 'greet("ivan", "moscow")',    expected: "Hello, IVAN, welcome to moscow!",   description: "Базовый кейс" },
    { input: 'greet("  anna  ", "kazan")', expected: "Hello, ANNA, welcome to kazan!",    description: "С пробелами — strip работает" },
    { input: 'greet("pi", "london")',      expected: "Hello, PI, welcome to london!",     description: "Короткое имя" },
    { input: 'greet("MAX", "spb")',        expected: "Hello, MAX, welcome to spb!",       description: "Уже в верхнем регистре" },
  ],

  hints: [
    "F-строка позволяет вставлять переменные и вызывать методы прямо в шаблоне: `f\"Hello, {name}, welcome to {city}!\"`. Подумай, какие методы нужно применить к `name` перед подстановкой.",
    "Методы строк можно цепочкой: `name.strip()` убирает пробелы, `.upper()` переводит в верхний регистр. Оба вызова можно объединить: `name.strip().upper()`. Методы возвращают новую строку, поэтому результат можно использовать прямо в `{}`.",
    "Полное решение:\n```python\ndef greet(name, city):\n    return f\"Hello, {name.strip().upper()}, welcome to {city}!\"\n```",
  ],

  isAvailable: true,
};

export default lesson;
