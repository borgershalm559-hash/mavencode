import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Строки: работа с текстом",
  order: 6,
  type: "code",
  language: "python",
  xpReward: 120,
  content: `# Строки: работа с текстом

## 🎯 Что научишься делать
- Собирать строки из кусочков — через склейку, повторение и f-строки
- Преобразовывать текст: регистр, пробелы, замена подстрок
- Измерять длину строки и понимать, что строки нельзя менять «на месте»
- Писать аккуратные приветствия, сообщения и шаблоны писем

## 🔍 Зачем это нужно
Компьютер говорит с человеком через текст. Имя пользователя в приветствии, тема письма, сообщение об ошибке, заголовок кнопки, строка в логе — всё это строки. Если числа — это про «сколько», то строки — это про «что написано».

Представь, что ты делаешь форму регистрации. Пользователь ввёл имя \`"  аня  "\` — с лишними пробелами и с маленькой буквы. Ты не можешь показать это в интерфейсе как есть: нужно убрать пробелы, сделать первую букву большой, вставить имя в приветствие. Всё это — пять строчек кода, если ты знаешь базовые операции со строками. И ноль строчек, если не знаешь — программа просто не заработает.

## 📚 Теория

### Строка — это последовательность символов
**Строка (string, \`str\`)** — это текст в кавычках. В Python строку можно записать тремя способами, и все три равноправны:

\`\`\`python
a = "Привет"      # двойные кавычки
b = 'Привет'      # одинарные — то же самое
c = """Привет"""  # тройные — для многострочного текста
\`\`\`

Почему два варианта кавычек? Чтобы удобно вставлять одни внутрь других: \`"Он сказал 'привет'"\` — никаких экранирований. Тройные кавычки нужны, когда текст занимает несколько строк — например, длинное письмо или SQL-запрос.

Строка — это **последовательность**: «П», «р», «и», «в», «е», «т» идут по порядку, и у каждого символа есть позиция. Это важно, потому что отсюда растут индексация и срезы (про них — в следующих уроках).

### Склейка (\`+\`) и повторение (\`*\`)
Две базовые операции, которые ты будешь использовать постоянно.

**Конкатенация** — это склейка двух строк через \`+\`:

\`\`\`python
first = "Python"
second = "101"
print(first + second)        # Python101
print(first + " " + second)  # Python 101 — пробел добавили вручную
\`\`\`

Обрати внимание: \`+\` не ставит пробел между словами. Если нужен пробел — добавь его явно, как отдельную строку \`" "\`.

**Повторение** — это \`*\` с числом. Строка повторяется столько раз, сколько попросили:

\`\`\`python
line = "-" * 20        # двадцать дефисов подряд
laugh = "ха" * 3       # хахаха
\`\`\`

Полезно для разделителей, прогресс-баров, тестовых заглушек.

### f-строки — главный инструмент
А теперь — самое важное. Склеивать строки через \`+\` можно, но когда переменных много, код превращается в кашу:

\`\`\`python
name = "Аня"
age = 25
# Старый способ — больно читать:
msg = "Меня зовут " + name + ", мне " + str(age) + " лет"
\`\`\`

Обрати внимание на \`str(age)\` — без него Python ругается, потому что \`+\` не умеет складывать строку с числом. Это раздражает.

**f-строка (f-string)** решает обе проблемы сразу. Ставишь букву \`f\` перед кавычками — и внутри можешь вставлять переменные в фигурных скобках \`{...}\`:

\`\`\`python
name = "Аня"
age = 25
msg = f"Меня зовут {name}, мне {age} лет"
print(msg)  # Меня зовут Аня, мне 25 лет
\`\`\`

Что происходит построчно:
- \`f"..."\` — префикс \`f\` говорит Python: «внутри этой строки будут подстановки».
- \`{name}\` — Python берёт значение переменной \`name\` и вставляет его сюда.
- \`{age}\` — число автоматически превращается в текст, \`str()\` не нужен.

Внутри \`{}\` можно писать не только переменные, но и **любые выражения**:

\`\`\`python
a = 7
b = 3
print(f"Сумма: {a + b}")            # Сумма: 10
print(f"Имя большими: {name.upper()}")  # Имя большими: АНЯ
\`\`\`

Это именно то, что мы будем использовать в задании.

### Полезные методы: \`.upper()\`, \`.lower()\`, \`.strip()\`, \`.replace()\`
**Метод** — это функция, которая «живёт» на объекте и вызывается через точку: \`text.upper()\`. У строк методов десятки, но четыре ты будешь использовать чаще всего.

| Метод | Что делает | Пример |
|---|---|---|
| \`.upper()\` | ВСЁ В ВЕРХНИЙ РЕГИСТР | \`"аня".upper()\` → \`"АНЯ"\` |
| \`.lower()\` | всё в нижний регистр | \`"АНЯ".lower()\` → \`"аня"\` |
| \`.strip()\` | убирает пробелы по краям | \`"  hi  ".strip()\` → \`"hi"\` |
| \`.replace(a, b)\` | меняет все \`a\` на \`b\` | \`"кот".replace("к", "р")\` → \`"рот"\` |

\`\`\`python
raw = "  Привет, Мир  "
print(raw.strip())                   # "Привет, Мир" — без крайних пробелов
print(raw.strip().lower())           # "привет, мир" — методы можно цепочкой
print(raw.replace("Мир", "Python"))  # "  Привет, Python  "
\`\`\`

Обрати внимание на цепочку \`raw.strip().lower()\`: сначала убрали пробелы, потом результат перевели в нижний регистр. Так и читается — слева направо.

### Длина строки: \`len()\`
\`len()\` — это встроенная функция, которая возвращает **количество символов** в строке:

\`\`\`python
print(len("Python"))   # 6
print(len(""))         # 0 — пустая строка
print(len("   "))      # 3 — пробелы тоже считаются
\`\`\`

Полезно для проверок: «пароль короче 8 символов — не принимаем», «имя не пустое — можно сохранять».

### Неизменяемость строк
Короткий, но важный момент. Строки в Python **неизменяемы (immutable)**. Это значит: когда ты пишешь \`name.upper()\`, сама переменная \`name\` не меняется — создаётся **новая** строка, и её нужно куда-то присвоить:

\`\`\`python
name = "аня"
name.upper()           # вернёт "АНЯ", но результат выбросится!
print(name)            # всё ещё "аня"

name = name.upper()    # вот теперь name стал "АНЯ"
\`\`\`

Запомни как мантру: **методы строк возвращают новую строку, а не меняют старую**.

## 🧪 Примеры

\`\`\`python
# 1. Простое приветствие через f-строку
name = "Иван"
greeting = f"Привет, {name}!"   # подставили имя внутрь шаблона
print(greeting)                  # Привет, Иван!
\`\`\`

\`\`\`python
# 2. Очистка введённых данных
raw_email = "   Ivan@MAIL.ru  "
email = raw_email.strip().lower()   # убрали пробелы, затем — в нижний регистр
print(email)                         # ivan@mail.ru
print(f"Длина email: {len(email)} символов")  # Длина email: 12 символов
\`\`\`

\`\`\`python
# 3. Формирование карточки пользователя
name = "аня"
city = "москва"
# .upper() — имя большими, city оставили как есть
card = f"Пользователь: {name.upper()} из города {city}"
print(card)   # Пользователь: АНЯ из города москва
\`\`\`

\`\`\`python
# 4. Разделитель и повтор
title = "ОТЧЁТ"
line = "=" * len(title)   # длина разделителя = длине заголовка
print(line)               # =====
print(title)              # ОТЧЁТ
print(line)               # =====
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Забыть префикс \`f\` перед кавычками.** Написал \`"Привет, {name}"\` без \`f\` — Python не подставит переменную, а выведет фигурные скобки буквально: \`Привет, {name}\`. Всегда проверяй: если внутри есть \`{}\` — перед кавычками должна стоять \`f\`.

2. **Ожидать, что метод изменит строку.** Код \`name.upper()\` сам по себе ничего не делает с переменной — он возвращает новую строку. Нужно писать \`name = name.upper()\` или использовать результат сразу в f-строке: \`f"{name.upper()}"\`.

3. **Путать \`+\` для чисел и для строк.** \`"5" + "3"\` даёт \`"53"\` (склейка), а не \`8\`. Если нужно сложить как числа — сначала преобразуй через \`int()\`: \`int("5") + int("3")\` вернёт \`8\`. Python строго различает строку \`"5"\` и число \`5\`.

## 💡 Мини-тест самопроверки

1. Что выведет \`print("abc" * 3)\`? (Ответ: \`abcabcabc\`)
2. Что вернёт \`"  Hello  ".strip().lower()\`? (Ответ: \`"hello"\`)
3. Есть переменная \`name = "bob"\`. Что выведет \`print(f"Hi, {name.upper()}!")\`? (Ответ: \`Hi, BOB!\`)

## 🛠️ Задание
Напиши функцию \`greet(name, city)\`, которая возвращает строку вида \`"Hello, ALICE, welcome to Moscow!"\` — т.е. приветствие, где имя переведено в верхний регистр через \`.upper()\`, а город оставлен как есть. Используй f-строку. Возвращаемое значение должно быть строкой (\`str\`).`,

  starterCode: `def greet(name, city):
    # Используй метод .upper() на имени, чтобы получить имя заглавными
    # Город оставь как есть, без преобразований
    # Собери строку через f-строку: f"Hello, {...}, welcome to {...}!"
    # Верни результат через return
    pass`,

  solution: `def greet(name, city):
    return f"Hello, {name.upper()}, welcome to {city}!"`,

  tests: [
    { input: 'greet("Alice", "Moscow")',      expected: "Hello, ALICE, welcome to Moscow!",     description: "Обычный случай — имя становится заглавным" },
    { input: 'greet("BOB", "Paris")',         expected: "Hello, BOB, welcome to Paris!",        description: "Имя уже заглавное — остаётся таким же" },
    { input: 'greet("charlie", "New York")',  expected: "Hello, CHARLIE, welcome to New York!", description: "Строчное имя и город из двух слов" },
    { input: 'greet("", "Tokyo")',            expected: "Hello, , welcome to Tokyo!",           description: "Граничный случай — пустое имя" },
  ],

  hints: [
    "Подумай: тебе нужно собрать строку по шаблону, подставив в неё имя и город. Имя должно быть в верхнем регистре — для этого у строк есть метод .upper(). Город оставь как есть. Удобнее всего использовать f-строку.",
    "F-строка позволяет вызывать методы прямо внутри фигурных скобок. Ключевая часть решения выглядит так: f\"Hello, {name.upper()}, welcome to {city}!\" — и этот результат нужно вернуть через return.",
    "Полное решение:\n```python\ndef greet(name, city):\n    return f\"Hello, {name.upper()}, welcome to {city}!\"\n```",
  ],

  isAvailable: true,
};

export default lesson;
