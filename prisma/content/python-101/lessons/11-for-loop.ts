import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Цикл for: перебор последовательностей",
  order: 11,
  type: "code",
  language: "python",
  xpReward: 140,
  content: `# Цикл \`for\`: перебор последовательностей

## 🎯 Что научишься делать
- Использовать \`for\` для перебора любой последовательности
- Работать с \`range()\` для числовых диапазонов
- Итерировать по строкам и спискам
- Накапливать результат — сумму, максимум, счётчик
- Выбирать между \`for\` и \`while\` под конкретную задачу

## 🔍 Зачем это нужно
Перебор последовательностей — хлеб каждого программиста. Подсчитать сумму чисел, найти максимальный элемент, обработать каждое слово в тексте, проверить каждый товар в заказе — всё это \`for\`. В отличие от \`while\`, цикл \`for\` безопаснее: он не допускает бесконечного выполнения, потому что всегда знает, сколько элементов осталось перебрать.

## 📚 Теория

### Метафора: читать книгу по страницам

Представь, что ты читаешь книгу и хочешь выписать первое слово каждой страницы. Ты открываешь страницу 1, выписываешь слово, переворачиваешь — страница 2, выписываешь, переворачиваешь — и так до конца книги.

Цикл \`for\` работает именно так: берёт последовательность (книгу), берёт по одному элементу (страницы), делает что-то с каждым, переходит к следующему.

### Синтаксис for

\`\`\`python
for переменная in последовательность:
    тело цикла
\`\`\`

\`\`\`python
fruits = ["яблоко", "банан", "вишня"]

for fruit in fruits:    # fruit — текущий элемент списка
    print(fruit)        # выводим каждый

# Вывод:
# яблоко
# банан
# вишня
\`\`\`

Разберём детально:
- \`for\` — ключевое слово, начинает цикл.
- \`fruit\` — переменная, которая принимает значение каждого элемента по очереди.
- \`in fruits\` — последовательность для перебора.
- \`:\` — двоеточие обязательно.
- Тело — строки с отступом 4 пробела.

### range() — генератор числовых последовательностей

Когда нужно пройти по числам — используй \`range()\`:

\`\`\`python
# range(stop) — от 0 до stop-1
for i in range(5):
    print(i)
# Вывод: 0, 1, 2, 3, 4
\`\`\`

\`\`\`python
# range(start, stop) — от start до stop-1
for i in range(1, 6):
    print(i)
# Вывод: 1, 2, 3, 4, 5
\`\`\`

\`\`\`python
# range(start, stop, step) — с шагом
for i in range(0, 10, 2):
    print(i)
# Вывод: 0, 2, 4, 6, 8 (чётные числа)
\`\`\`

Обрати внимание: \`range\` включает \`start\`, но **не включает** \`stop\`. Это поначалу кажется странным, но логика такая: \`range(0, 5)\` даёт ровно 5 элементов (0, 1, 2, 3, 4).

Частая ловушка:
\`\`\`python
# Хочешь числа от 1 до 5 включительно:
for i in range(1, 5):   # НЕПРАВИЛЬНО — даёт 1, 2, 3, 4
    print(i)

for i in range(1, 6):   # ПРАВИЛЬНО — даёт 1, 2, 3, 4, 5
    print(i)
\`\`\`

Чтобы включить верхнюю границу, добавь 1: \`range(start, end + 1)\`.

### Итерация по строке

Строка в Python — это последовательность символов. \`for\` перебирает их по одному:

\`\`\`python
word = "Python"

for char in word:
    print(char)
# Вывод: P, y, t, h, o, n
\`\`\`

Это полезно для анализа текста — подсчёта букв, поиска символов, проверки формата.

### Аккумуляторы: накапливаем результат

Один из самых важных паттернов — аккумулятор. Создаём начальное значение, обновляем его в каждой итерации:

\`\`\`python
# Сумма чисел
numbers = [3, 7, 2, 9, 1]
total = 0               # начальное значение аккумулятора

for n in numbers:
    total = total + n   # добавляем каждый элемент к накопленной сумме

print(total)   # 22
\`\`\`

\`\`\`python
# Подсчёт элементов по условию
text = "Hello, World!"
digit_count = 0

for char in text:
    if char.isdigit():
        digit_count = digit_count + 1

print(digit_count)   # 0 — цифр нет
\`\`\`

### for vs while — когда что выбирать

| Ситуация | Выбор |
|---|---|
| Знаешь заранее, сколько итераций | \`for\` |
| Перебираешь список, строку, диапазон | \`for\` |
| Повторяешь до выполнения условия | \`while\` |
| Не знаешь заранее количество итераций | \`while\` |

Простое правило: если можешь написать \`for\` — пиши \`for\`. Он безопаснее (нет риска бесконечного цикла) и читается понятнее.

## 🧪 Примеры

\`\`\`python
# Вывод таблицы умножения на 3
for i in range(1, 11):
    print(f"3 x {i} = {3 * i}")

# Вывод:
# 3 x 1 = 3
# 3 x 2 = 6
# ...
# 3 x 10 = 30
\`\`\`

\`\`\`python
# Поиск максимального элемента вручную
def find_max(numbers):
    if not numbers:
        return None
    maximum = numbers[0]
    for n in numbers:
        if n > maximum:
            maximum = n
    return maximum

print(find_max([4, 1, 9, 3, 7]))   # 9
\`\`\`

\`\`\`python
# Фильтрация: только положительные числа
def positive_only(numbers):
    result = []
    for n in numbers:
        if n > 0:
            result.append(n)
    return result

print(positive_only([-1, 3, -5, 7, 0, 2]))   # [3, 7, 2]
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Забыть +1 при включении верхней границы.** \`range(1, 10)\` даёт числа 1..9, а не 1..10. Для включения 10 нужно \`range(1, 11)\`. Стандартный паттерн: \`range(start, end + 1)\`.

2. **Изменять переменную цикла внутри тела.** В \`for i in range(5):\` переменная \`i\` обновляется автоматически. Если написать \`i = i + 1\` внутри тела — цикл не пропустит итерации, это просто перезапишет \`i\`, но следующий виток всё равно возьмёт следующий элемент из \`range\`.

3. **Изменять список во время итерации по нему.** Это может привести к пропуску элементов или бесконечному циклу. Если нужно изменить список — итерируй по его копии или строй новый список.

4. **Путать \`range(n)\` и \`range(1, n)\`.** \`range(5)\` — это 0, 1, 2, 3, 4. \`range(1, 5)\` — это 1, 2, 3, 4. Оба дают 5 элементов, но разные.

5. **Не инициализировать аккумулятор.** Если забыть \`total = 0\` перед циклом, Python выдаст \`NameError: name 'total' is not defined\` при первом обращении к \`total\` внутри цикла.

## 💡 Мини-тест самопроверки

1. Что выведет этот код?
\`\`\`python
for i in range(2, 5):
    print(i)
\`\`\`
<details><summary>Ответ</summary>2, 3, 4 — range(2, 5) включает 2, но не включает 5.</details>

2. Как перебрать числа 1, 2, 3, 4, 5?
<details><summary>Ответ</summary>\`for i in range(1, 6):\` — range(1, 6) даёт числа от 1 до 5 включительно.</details>

3. Чем \`for\` лучше \`while\` при переборе списка?
<details><summary>Ответ</summary>С \`for\` нет риска бесконечного цикла — он автоматически останавливается, когда последовательность закончилась. С \`while\` нужно вручную следить за счётчиком.</details>

## 🛠️ Задание

Напиши функцию \`sum_range(start, end)\`, которая возвращает **сумму всех целых чисел** от \`start\` до \`end\` включительно.

Примеры:
- \`sum_range(1, 5)\` → \`15\` (1+2+3+4+5)
- \`sum_range(1, 1)\` → \`1\`
- \`sum_range(5, 10)\` → \`45\` (5+6+7+8+9+10)

Используй цикл \`for\` и \`range()\`. Не забудь, что \`range\` не включает верхнюю границу!`,

  starterCode: `def sum_range(start, end):
    total = 0
    # Используй for с range()
    # Помни: range не включает верхнюю границу, добавь +1
    return total`,

  solution: `def sum_range(start, end):
    total = 0
    for n in range(start, end + 1):
        total = total + n
    return total`,

  tests: [
    { input: "sum_range(1, 5)",   expected: "15",  description: "1+2+3+4+5 = 15" },
    { input: "sum_range(1, 1)",   expected: "1",   description: "Один элемент" },
    { input: "sum_range(5, 10)",  expected: "45",  description: "5+6+7+8+9+10 = 45" },
    { input: "sum_range(10, 10)", expected: "10",  description: "Ровно одно число" },
  ],

  hints: [
    "Чтобы перебрать числа от `start` до `end` включительно, используй `range(start, end + 1)`. Знак +1 обязателен — без него последнее число (`end`) не войдёт в диапазон.",
    "Паттерн аккумулятора: начни с `total = 0`, потом в теле цикла: `total = total + n`. После цикла верни `total`. Полная структура: `for n in range(start, end + 1): total = total + n`.",
    "Полное решение:\n```python\ndef sum_range(start, end):\n    total = 0\n    for n in range(start, end + 1):\n        total = total + n\n    return total\n```",
  ],

  isAvailable: true,
};

export default lesson;
