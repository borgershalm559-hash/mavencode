import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Переменные: именованные коробочки",
  order: 4,
  type: "code",
  language: "python",
  xpReward: 120,
  content: `# Переменные: именованные коробочки

## 🎯 Что научишься делать
- Создавать переменные и сохранять в них данные
- Понимать, что такое присваивание и чем \`=\` отличается от «равно»
- Переименовывать и перезаписывать переменные
- Называть переменные правильно: правила и соглашения
- Собирать строку из нескольких переменных через f-строку

## 🔍 Зачем это нужно
Без переменных программа не помнит ничего. Спросил имя пользователя — забыл. Вычислил цену — потерял. Переменные — это память программы. Каждое реальное приложение держит в переменных имена, счётчики, результаты вычислений, состояния кнопок и ещё сотни вещей. Без этого не написать даже калькулятор.

## 📚 Теория

### Что такое переменная: конверт с наклейкой

Представь почтовый конверт. Ты пишешь на нём имя — «возраст» — и кладёшь внутрь листок с числом \`25\`. Теперь когда тебе нужно знать возраст, ты берёшь конверт с надписью «возраст» и смотришь внутрь.

Переменная работает точно так же:
- **Имя переменной** — это надпись на конверте.
- **Значение** — это то, что лежит внутри.
- **Присваивание** — это момент, когда ты кладёшь что-то в конверт.

\`\`\`python
age = 25        # конверт "age" — внутри число 25
name = "Аня"   # конверт "name" — внутри строка "Аня"
\`\`\`

Теперь в любом месте программы можно написать \`age\` или \`name\` — Python откроет конверт и достанет значение.

### Оператор присваивания \`=\`

Знак \`=\` в Python — это **не проверка равенства**. Это команда: «положи значение справа в переменную слева».

\`\`\`python
city = "Москва"   # кладём строку "Москва" в переменную city
\`\`\`

Читается справа налево: возьми \`"Москва"\`, положи в \`city\`.

Это важно! В математике \`a = 5\` — это утверждение. В Python — это действие. Именно поэтому Python использует \`==\` (два знака равно) для **проверки** равенства, а \`=\` — только для **присваивания**.

### Переприсваивание: заменяем содержимое конверта

Переменная называется «переменной», потому что её значение можно изменить в любой момент. Старое значение просто стирается:

\`\`\`python
score = 0       # счёт в начале игры: 0
score = 10      # игрок заработал очки: теперь 10
score = 42      # заработал ещё: теперь 42
print(score)    # выведет 42 — только последнее присваивание имеет силу
\`\`\`

Конверт остаётся тот же, меняется только листок внутри.

\`\`\`python
mood = "грустный"
print(mood)      # грустный
mood = "весёлый"
print(mood)      # весёлый — предыдущее значение исчезло
\`\`\`

### Правила именования переменных

Python принимает имена переменных не любые. Вот правила:

**Можно:**
- Буквы (латинские или кириллица, но латинские — стандарт), цифры, знак подчёркивания \`_\`
- \`user_age\`, \`first_name\`, \`total_price_usd\` — всё в порядке

**Нельзя:**
- Начинать с цифры: \`1name\` — ошибка, \`name1\` — окей
- Использовать пробелы: \`user age\` — ошибка, \`user_age\` — окей
- Использовать специальные символы: \`user-age\`, \`price$\` — ошибка
- Использовать зарезервированные слова Python: \`for\`, \`if\`, \`else\`, \`while\`, \`True\`, \`False\`, \`class\`, \`def\`, \`return\`, \`print\` и другие — они уже заняты языком

\`\`\`python
# Правильно:
user_name = "Иван"
total_score = 100
is_active = True

# Неправильно (вызовет ошибку):
# 2fast = "слишком быстро"    # начинается с цифры — SyntaxError
# my name = "Дима"            # пробел в имени — SyntaxError
# for = 42                    # зарезервированное слово — SyntaxError
\`\`\`

### Регистр имеет значение

Python **чувствителен к регистру**. \`Name\`, \`name\` и \`NAME\` — это три разные переменные:

\`\`\`python
name = "Катя"
Name = "Вася"
print(name)   # Катя
print(Name)   # Вася — совершенно другая переменная!
\`\`\`

Чтобы не запутаться, следуй единому соглашению.

### Соглашение: snake_case

В Python принят стиль \`snake_case\` (змеиный регистр): все буквы строчные, слова разделены подчёркиванием. Это **не правило языка**, а **соглашение сообщества** — код с таким именованием выглядит «по-питонски» и читается легко.

\`\`\`python
# Хорошо (snake_case):
first_name = "Дмитрий"
user_age = 30
total_price = 1499.90

# Работает, но выглядит странно для Python-разработчика:
FirstName = "Дмитрий"    # это CamelCase, для классов
TOTAL_PRICE = 1499.90    # это UPPER_CASE, для констант
\`\`\`

### Почему имена переменных важны

Хорошее имя — это самодокументирующийся код:

\`\`\`python
# Плохо: зачем x и y?
x = 1999
y = 2024
z = y - x
print(z)    # 25 — но что это значит???

# Хорошо: сразу понятно
birth_year = 1999
current_year = 2024
age = current_year - birth_year
print(age)  # 25 — возраст человека
\`\`\`

Имена \`x\`, \`y\`, \`z\` допустимы только в учебных примерах и математических формулах. В реальном коде называй вещи своими именами.

## 🧪 Примеры

\`\`\`python
# Хранение данных о пользователе
user_name = "Алексей"
user_age = 28
user_city = "Санкт-Петербург"

# Собираем строку через f-строку
profile = f"Пользователь: {user_name}, возраст: {user_age}, город: {user_city}"
print(profile)
# Вывод: Пользователь: Алексей, возраст: 28, город: Санкт-Петербург
\`\`\`

\`\`\`python
# Переприсваивание — счётчик очков
score = 0
print(f"Очков в начале: {score}")   # 0

score = score + 50    # сложить к текущему значению и сохранить
print(f"После уровня: {score}")     # 50

score = score + 30
print(f"После бонуса: {score}")     # 80
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Опечатка в имени переменной.** Написал \`naem = "Аня"\`, а потом обращаешься к \`name\` — получишь \`NameError: name 'name' is not defined\`. Python не исправляет опечатки, он просто не найдёт переменную.

2. **Думать, что \`=\` проверяет равенство.** \`if age = 18:\` — это ошибка синтаксиса. Для проверки используй \`==\`.

3. **Использовать зарезервированные слова.** \`for = 5\`, \`if = "да"\` — Python ответит \`SyntaxError\`. Если среда разработки подчёркивает имя цветом — это тревожный сигнал.

4. **Забыть, что старое значение пропадает при переприсваивании.** Если тебе нужны оба значения — сохрани их в разные переменные.

5. **Назвать переменную \`print\`.** \`print = "текст"\` технически работает (Python позволит), но после этого встроенная функция \`print()\` окажется недоступна — ты перекроешь её своей переменной. Никогда не называй переменные именами встроенных функций.

## 💡 Мини-тест самопроверки

1. Что выведет следующий код?
\`\`\`python
x = 10
x = 20
print(x)
\`\`\`
<details><summary>Ответ</summary>20 — последнее присваивание побеждает.</details>

2. Какое из имён переменных неправильное и почему: \`total_sum\`, \`2nd_player\`, \`player_2\`?
<details><summary>Ответ</summary>\`2nd_player\` — нельзя начинать с цифры, будет SyntaxError. Остальные два правильные.</details>

3. Чем \`=\` отличается от \`==\`?
<details><summary>Ответ</summary>\`=\` — присваивание (кладёт значение в переменную). \`==\` — проверка равенства (сравнивает два значения, возвращает True или False).</details>

## 🛠️ Задание

Создай три переменных:
- \`name\` — твоё имя (строка)
- \`age\` — твой возраст (число)
- \`city\` — твой город (строка)

Затем создай переменную \`message\`, которая содержит строку-приветствие, собранную из этих трёх переменных через f-строку.

Пример ожидаемого результата (с твоими данными):
\`"Привет! Меня зовут Аня, мне 25, я из города Москва."\`

Тест проверит конкретные значения — используй ровно те, что указаны в примере.`,

  starterCode: `# Создай переменные с данными
name = "___"
age = ___
city = "___"

# Собери приветственное сообщение через f-строку
message = f"___"
print(message)
`,

  solution: `name = "Аня"
age = 25
city = "Москва"
message = f"Привет! Меня зовут {name}, мне {age}, я из города {city}."`,

  tests: [
    { input: "name",    expected: "Аня",    description: "Имя задано" },
    { input: "age",     expected: "25",     description: "Возраст задан" },
    { input: "city",    expected: "Москва", description: "Город задан" },
    { input: "message", expected: "Привет! Меня зовут Аня, мне 25, я из города Москва.", description: "Сообщение собрано" },
  ],

  hints: [
    "Переменная создаётся так: `имя = значение`. Строки берутся в кавычки, числа — без. Например: `name = \"Аня\"`, `age = 25`, `city = \"Москва\"`.",
    "F-строка позволяет подставлять переменные прямо в текст: `f\"Привет, {name}!\"`. В задании нужно собрать: `f\"Привет! Меня зовут {name}, мне {age}, я из города {city}.\"`",
    "Полное решение:\n```python\nname = \"Аня\"\nage = 25\ncity = \"Москва\"\nmessage = f\"Привет! Меня зовут {name}, мне {age}, я из города {city}.\"\nprint(message)\n```",
  ],

  isAvailable: true,
};

export default lesson;
