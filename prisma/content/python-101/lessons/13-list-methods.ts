import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Методы списков и сортировка",
  order: 13,
  type: "fill-blanks",
  language: "python",
  xpReward: 140,
  content: `# Методы списков и сортировка

## 🎯 Что научишься делать
- Сортировать список: \`sort()\` (меняет оригинал) и \`sorted()\` (создаёт новый)
- Переворачивать порядок элементов: \`reverse()\` и \`reversed()\`
- Получать длину, минимум, максимум и сумму: \`len()\`, \`min()\`, \`max()\`, \`sum()\`
- Считать вхождения: \`count()\`

## 🔍 Зачем это нужно
Необработанный список чисел — это просто набор данных. Но когда ты умеешь быстро найти максимальное значение, отсортировать список или посчитать сумму — ты превращаешь данные в информацию. Именно это делают программисты каждый день: считают статистику по заказам, ищут топ-пользователей, сортируют результаты поиска.

## 📚 Теория

### Метафора: полка, которая умеет себя переставлять

В прошлом уроке ты узнал, что список — это полка с ящиками. Теперь представь, что у этой полки есть **встроенные команды**: она умеет сама себя отсортировать, перевернуть, ответить на вопросы «сколько ящиков?» или «что в самом тяжёлом ящике?». Это и есть методы и встроенные функции.

### sort() vs sorted(): мутация против копии

Это один из самых важных моментов в работе со списками. Два похожих инструмента ведут себя принципиально по-разному:

\`\`\`python
numbers = [3, 1, 4, 1, 5, 9, 2]

# sort() — меняет сам список, ничего не возвращает (возвращает None)
numbers.sort()
print(numbers)        # [1, 1, 2, 3, 4, 5, 9] — оригинал изменён!

# sorted() — возвращает НОВЫЙ список, оригинал не тронут
original = [3, 1, 4, 1, 5]
new_list = sorted(original)
print(original)       # [3, 1, 4, 1, 5] — не изменился
print(new_list)       # [1, 1, 3, 4, 5] — новый отсортированный список
\`\`\`

**Правило выбора:**
- Нужно **изменить** существующий список → используй \`list.sort()\`
- Нужно **сохранить** оригинал и получить отсортированную копию → используй \`sorted(list)\`

### reverse() и reversed()

Та же история: \`reverse()\` меняет список на месте, \`reversed()\` возвращает итератор (для преобразования в список нужен \`list()\`):

\`\`\`python
letters = ["c", "a", "b"]

# reverse() — переворачивает на месте
letters.reverse()
print(letters)           # ["b", "a", "c"] — оригинал изменён

# reversed() — возвращает итератор, нужно обернуть в list()
original = ["x", "y", "z"]
flipped = list(reversed(original))
print(original)          # ["x", "y", "z"] — не изменился
print(flipped)           # ["z", "y", "x"]
\`\`\`

### len(), min(), max(), sum()

Это **встроенные функции Python** — они работают не только со списками, но и с другими коллекциями:

\`\`\`python
scores = [85, 92, 78, 90, 88]

print(len(scores))    # 5     — количество элементов
print(min(scores))    # 78    — минимальное значение
print(max(scores))    # 92    — максимальное значение
print(sum(scores))    # 433   — сумма всех элементов
\`\`\`

Среднее значение вычисляется через \`sum\` и \`len\`:

\`\`\`python
average = sum(scores) / len(scores)
print(average)        # 86.6
\`\`\`

### count()

Метод \`count(x)\` возвращает, сколько раз элемент \`x\` встречается в списке:

\`\`\`python
votes = ["да", "нет", "да", "да", "нет"]
print(votes.count("да"))    # 3
print(votes.count("нет"))   # 2
\`\`\`

### Проверка наличия: in

Ты уже знаешь оператор \`in\` из прошлого урока, но стоит напомнить: он работает с любой коллекцией и возвращает \`True\` или \`False\`:

\`\`\`python
numbers = [10, 20, 30]
print(20 in numbers)    # True
print(99 in numbers)    # False
\`\`\`

## 🧪 Примеры

\`\`\`python
# Топ-3 результата
scores = [45, 78, 92, 61, 87, 55]
top3 = sorted(scores, reverse=True)[:3]
print(top3)   # [92, 87, 78]
\`\`\`

\`\`\`python
# Статистика по температурам за неделю
temps = [22, 19, 25, 28, 21, 24, 20]
print("Минимум:", min(temps))   # 19
print("Максимум:", max(temps))  # 28
print("Среднее:", sum(temps) / len(temps))  # 22.71...
\`\`\`

\`\`\`python
# Сколько раз встречается число в списке
data = [1, 2, 2, 3, 2, 4, 2]
print(data.count(2))   # 4
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Потерять оригинал при sort().** \`my_list.sort()\` необратимо меняет список. Если оригинал ещё нужен — сначала сделай копию: \`backup = my_list[:]\` или используй \`sorted()\`.

2. **Присвоить None от sort().** \`result = my_list.sort()\` — в \`result\` будет \`None\`, а не отсортированный список. \`sort()\` ничего не возвращает. Используй \`sorted()\` если нужно присваивание.

3. **Вызвать sum() на пустом списке.** \`sum([])\` вернёт \`0\` — это нормально. Но \`min([])\` и \`max([])\` выдадут \`ValueError\`. Всегда проверяй, что список не пуст, перед вызовом \`min\`/\`max\`.

4. **Перепутать in для списков и строк.** \`"ab" in ["ab", "cd"]\` проверяет наличие элемента в списке. \`"a" in "ab"\` проверяет наличие подстроки в строке. Это разные вещи.

## 💡 Мини-тест самопроверки

1. Что будет в \`a\` после \`a = [3, 1, 2]; a.sort()\`?
<details><summary>Ответ</summary>\`a\` станет \`[1, 2, 3]\`. Метод \`sort()\` изменяет список на месте и возвращает \`None\`.</details>

2. Чем \`sorted([3, 1, 2])\` отличается от \`[3, 1, 2].sort()\`?
<details><summary>Ответ</summary>\`sorted()\` возвращает новый отсортированный список, оригинал не меняется. \`sort()\` меняет оригинальный список и возвращает \`None\`.</details>

3. Что вернёт \`max([])\`?
<details><summary>Ответ</summary>Выбросит \`ValueError: max() arg is an empty sequence\` — нельзя найти максимум пустого списка.</details>

## 🛠️ Задание

Заполни пропуски в функции \`stats(numbers)\`, которая возвращает словарь со статистикой по списку чисел. Каждый пропуск \`___\` — это вызов одной встроенной функции Python.`,

  starterCode: `def stats(numbers):
    return {
        "count": ___(numbers),
        "min": ___(numbers),
        "max": ___(numbers),
        "sum": ___(numbers),
        "sorted": ___(numbers),
    }`,

  solution: `def stats(numbers):
    return {
        "count": len(numbers),
        "min": min(numbers),
        "max": max(numbers),
        "sum": sum(numbers),
        "sorted": sorted(numbers),
    }`,

  tests: [
    { input: 'stats([3, 1, 4, 1, 5])["count"]',  expected: "5",               description: "count" },
    { input: 'stats([3, 1, 4, 1, 5])["min"]',    expected: "1",               description: "min" },
    { input: 'stats([3, 1, 4, 1, 5])["max"]',    expected: "5",               description: "max" },
    { input: 'stats([3, 1, 4, 1, 5])["sum"]',    expected: "14",              description: "sum" },
    { input: 'stats([3, 1, 4, 1, 5])["sorted"]', expected: "[1, 1, 3, 4, 5]", description: "sorted" },
  ],

  hints: [
    "Каждый пропуск — это одна встроенная функция Python, которая принимает список. Для «count» нужна функция, возвращающая количество элементов. Какую встроенную функцию ты уже знаешь для этого?",
    "`len(numbers)` считает количество, `min(numbers)` находит минимум, `max(numbers)` — максимум, `sum(numbers)` — сумму. Для «sorted» используй `sorted(numbers)` — она вернёт новый отсортированный список, не изменяя оригинал.",
    "Полное решение:\n```python\ndef stats(numbers):\n    return {\n        \"count\": len(numbers),\n        \"min\": min(numbers),\n        \"max\": max(numbers),\n        \"sum\": sum(numbers),\n        \"sorted\": sorted(numbers),\n    }\n```",
  ],

  isAvailable: true,
};

export default lesson;
