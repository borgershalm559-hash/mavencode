import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Кортежи и множества",
  order: 15,
  type: "code",
  language: "python",
  xpReward: 140,
  content: `# Кортежи и множества

## 🎯 Что научишься делать
- Создавать кортежи и понимать, зачем нужна неизменяемость
- Использовать распаковку (unpacking) для удобного присваивания
- Создавать множества и использовать их для хранения уникальных значений
- Выполнять операции над множествами: объединение, пересечение, разность
- Конвертировать список в множество для удаления дубликатов

## 🔍 Зачем это нужно
Не все данные одинаковы. Иногда тебе нужен список, который **нельзя случайно изменить** — например, координаты точки или RGB-цвет. Иногда нужно быстро проверить, есть ли элемент в наборе из миллиона значений, или убрать дубликаты из списка. Для этого и существуют кортежи и множества — два специализированных инструмента, у каждого своя сильная сторона.

## 📚 Теория

### Кортеж: запечатанный конверт

Представь конверт, в котором что-то лежит. Ты можешь посмотреть содержимое, но не можешь ничего поменять, не разрушив конверт. Это кортеж — неизменяемая последовательность.

\`\`\`python
# Создание кортежа — круглые скобки
point = (10, 20)
color = (255, 128, 0)        # RGB-цвет
coordinates = (55.75, 37.61) # широта, долгота

# Кортеж из одного элемента — нужна запятая!
single = (42,)      # это кортеж
not_tuple = (42)    # это просто число в скобках!
\`\`\`

Доступ по индексу работает так же, как у списков:

\`\`\`python
point = (10, 20)
print(point[0])   # 10
print(point[1])   # 20
print(point[-1])  # 20 — последний элемент
\`\`\`

Но изменить элемент нельзя:

\`\`\`python
point = (10, 20)
point[0] = 99    # TypeError: 'tuple' object does not support item assignment
\`\`\`

### Зачем нужна неизменяемость?

Неизменяемость — это защита от случайных ошибок. Когда ты передаёшь кортеж в функцию, ты знаешь, что функция его не изменит. Кроме того:

- Кортежи чуть **быстрее** списков (Python оптимизирует их хранение)
- Кортежи можно использовать как **ключи словаря** (списки — нельзя)
- Кортежи сигнализируют читателю кода: «эти данные фиксированные»

\`\`\`python
# Кортеж как ключ словаря
locations = {
    (55.75, 37.61): "Moscow",
    (59.93, 30.33): "Saint Petersburg"
}
\`\`\`

### Распаковка (unpacking)

Одна из самых удобных возможностей кортежей — распаковка в несколько переменных:

\`\`\`python
point = (10, 20)
x, y = point      # x = 10, y = 20
print(x)    # 10
print(y)    # 20

# Распаковка работает и с любыми последовательностями
a, b, c = [1, 2, 3]
first, *rest = [10, 20, 30, 40]   # first = 10, rest = [20, 30, 40]
\`\`\`

Распаковка особенно удобна при работе с \`dict.items()\`:

\`\`\`python
contacts = {"Anna": "111", "Ivan": "222"}
for name, phone in contacts.items():
    print(f"{name}: {phone}")
\`\`\`

### Множество: корзина уникальных фруктов

Представь корзину, в которую ты кладёшь фрукты. Особенность: если ты положишь два яблока — в корзине всё равно будет только **одно** яблоко. Дубликаты автоматически игнорируются. Это множество.

\`\`\`python
# Создание множества — фигурные скобки, как у словаря, но без двоеточий
fruits = {"apple", "banana", "cherry"}
numbers = {1, 2, 3, 2, 1}   # дубликаты убираются автоматически
print(numbers)               # {1, 2, 3} — порядок не гарантирован!

# Пустое множество — нельзя использовать {}, это пустой словарь!
empty_set = set()
\`\`\`

**Важно:** множество **не имеет порядка**. Ты не можешь обратиться к элементу по индексу. Множество — это просто «есть или нет».

### Операции с множествами

\`\`\`python
a = {1, 2, 3, 4}
b = {3, 4, 5, 6}

# Объединение — все элементы из обоих
print(a | b)    # {1, 2, 3, 4, 5, 6}

# Пересечение — только общие элементы
print(a & b)    # {3, 4}

# Разность — в a, но не в b
print(a - b)    # {1, 2}

# Симметрическая разность — в одном, но не в обоих
print(a ^ b)    # {1, 2, 5, 6}
\`\`\`

### Основные методы множества

\`\`\`python
colors = {"red", "blue"}

colors.add("green")         # добавить элемент
print("red" in colors)      # True — проверка O(1), быстрее чем в списке!

colors.remove("blue")       # удалить (KeyError если нет)
colors.discard("yellow")    # удалить без ошибки если нет
\`\`\`

### Конвертация: убираем дубликаты из списка

\`\`\`python
with_duplicates = [1, 2, 2, 3, 3, 3, 4]
unique = list(set(with_duplicates))
print(unique)   # [1, 2, 3, 4] (порядок может отличаться!)

# Чтобы сохранить порядок — сортируй после:
unique_sorted = sorted(set(with_duplicates))
print(unique_sorted)   # [1, 2, 3, 4]
\`\`\`

## 🧪 Примеры

\`\`\`python
# Координаты как кортеж — явно неизменяемые данные
moscow = (55.75, 37.61)
lat, lon = moscow
print(f"Latitude: {lat}")   # 55.75
\`\`\`

\`\`\`python
# Общие интересы двух пользователей
user1 = {"Python", "music", "sports", "movies"}
user2 = {"Java", "music", "travel", "movies"}

common = user1 & user2
print(common)   # {"music", "movies"}
\`\`\`

\`\`\`python
# Быстрое удаление дубликатов с сохранением порядка
words = ["the", "cat", "sat", "the", "cat"]
unique_words = sorted(set(words))
print(unique_words)   # ["cat", "sat", "the"]
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Создать пустое множество через \`{}\`.** \`{}\` — это пустой словарь, не множество! Пустое множество создаётся только через \`set()\`.

2. **Ожидать порядок в множестве.** \`{3, 1, 2}\` может выводиться как \`{1, 2, 3}\` или в любом другом порядке. Не полагайся на порядок элементов множества.

3. **Попытаться изменить кортеж.** \`(1, 2, 3)[0] = 9\` — \`TypeError\`. Кортежи неизменяемы по определению. Если нужно изменение — используй список.

4. **Забыть запятую в кортеже из одного элемента.** \`(42)\` — это просто \`42\`. \`(42,)\` — это кортеж. Без запятой Python воспринимает скобки как группировку выражения.

5. **Использовать множество там, где нужен список.** Если важен порядок — используй список. Множество — только для «уникальности» и «принадлежности».

## 💡 Мини-тест самопроверки

1. Как создать пустое множество?
<details><summary>Ответ</summary>\`set()\` — только так. \`{}\` создаёт пустой словарь.</details>

2. Что выведет \`len({1, 2, 2, 3, 3, 3})\`?
<details><summary>Ответ</summary>\`3\` — множество хранит только уникальные элементы.</details>

3. Что произойдёт при \`t = (1, 2); t[0] = 9\`?
<details><summary>Ответ</summary>\`TypeError: 'tuple' object does not support item assignment\` — кортежи неизменяемы.</details>

## 🛠️ Задание

Напиши функцию \`unique_words(text)\`, которая принимает строку и возвращает **отсортированный список** уникальных слов в нижнем регистре.

Например: \`"Hello world hello"\` → \`["hello", "world"]\``,

  starterCode: `def unique_words(text):
    # 1. Приведи текст к нижнему регистру: text.lower()
    # 2. Раздели на слова: .split()
    # 3. Убери дубликаты через set()
    # 4. Верни отсортированный список через sorted()
    pass`,

  solution: `def unique_words(text):
    words = text.lower().split()
    return sorted(set(words))`,

  tests: [
    { input: 'unique_words("Hello world hello")', expected: "['hello', 'world']", description: "Повторы убраны" },
    { input: 'unique_words("a b c a b c")',        expected: "['a', 'b', 'c']",   description: "Много повторов" },
    { input: 'unique_words("")',                   expected: "[]",                description: "Пустая строка" },
    { input: 'unique_words("ONE one One")',         expected: "['one']",           description: "Регистр" },
  ],

  hints: [
    "Разбей задачу на шаги: сначала приведи все слова к нижнему регистру (`text.lower()`), потом раздели строку на слова (`.split()`). Получишь список слов — но с дубликатами.",
    "Чтобы убрать дубликаты — оберни список в `set()`. Чтобы получить отсортированный список из множества — используй `sorted()`. Результат: `sorted(set(words))`.",
    "Полное решение:\n```python\ndef unique_words(text):\n    words = text.lower().split()\n    return sorted(set(words))\n```",
  ],

  isAvailable: true,
};

export default lesson;
