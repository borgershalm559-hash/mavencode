import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Словари: данные по ключам",
  order: 14,
  type: "code",
  language: "python",
  xpReward: 150,
  content: `# Словари: данные по ключам

## 🎯 Что научишься делать
- Создавать словари и получать значения по ключу
- Безопасно обращаться к данным через \`get()\`
- Добавлять, изменять и удалять пары ключ-значение
- Итерироваться по ключам, значениям и парам
- Проверять наличие ключа

## 🔍 Зачем это нужно
Списки отлично хранят упорядоченные наборы: «первый, второй, третий». Но что если тебе нужно хранить имя пользователя, его возраст и город? Обращаться к ним по числовым индексам неудобно — ты должен помнить, что \`user[0]\` — это имя, а \`user[2]\` — город. Словарь решает эту проблему: ты сам называешь каждый «ящик» понятным ключом. Словари — основа любого реального приложения: настройки конфигурации, данные из JSON-API, профили пользователей — всё это словари.

## 📚 Теория

### Метафора: телефонная книжка

Телефонная книжка хранит пары «имя → номер». Тебе не нужно знать, на какой странице запись: ты ищешь по **имени** и сразу получаешь **номер**. Именно так работает словарь в Python: ты ищешь по **ключу** и получаешь **значение**.

\`\`\`
Ключ           Значение
"Anna"    ->   "111-22-33"
"Ivan"    ->   "444-55-66"
"Maria"   ->   "777-88-99"
\`\`\`

### Создание словаря

Словарь создаётся с помощью фигурных скобок. Каждая пара записывается как \`ключ: значение\`, пары разделяются запятыми:

\`\`\`python
# Профиль пользователя
user = {
    "name": "Anna",
    "age": 25,
    "city": "Moscow"
}

# Словарь телефонов
phones = {"Anna": "111", "Ivan": "222"}

# Пустой словарь
empty = {}
\`\`\`

**Ключи** должны быть **неизменяемыми**: строки, числа, кортежи — подходят. Списки в качестве ключей использовать нельзя.

### Получение значения по ключу

\`\`\`python
user = {"name": "Anna", "age": 25, "city": "Moscow"}

print(user["name"])    # Anna
print(user["age"])     # 25
\`\`\`

Если ключа не существует — Python выбросит \`KeyError\`:

\`\`\`python
print(user["email"])   # KeyError: 'email'
\`\`\`

### Безопасный доступ: get()

Метод \`get(ключ, значение_по_умолчанию)\` возвращает значение если ключ есть, или значение по умолчанию если ключа нет — без ошибки:

\`\`\`python
user = {"name": "Anna", "age": 25}

print(user.get("name", "Unknown"))    # Anna — ключ есть
print(user.get("email", "No email")) # No email — ключа нет, берётся default

# Если второй аргумент не указан, возвращается None
print(user.get("phone"))    # None
\`\`\`

На практике \`get()\` используется чаще, чем прямой доступ через \`[]\` — он защищает от неожиданных ошибок.

### Добавление и изменение

Чтобы добавить новую пару или изменить существующую — синтаксис одинаковый:

\`\`\`python
user = {"name": "Anna"}

user["age"] = 25          # добавляем новый ключ "age"
user["name"] = "Anya"     # меняем существующий ключ "name"

print(user)   # {"name": "Anya", "age": 25}
\`\`\`

### Удаление: del

\`\`\`python
user = {"name": "Anna", "age": 25, "city": "Moscow"}

del user["city"]
print(user)   # {"name": "Anna", "age": 25}
\`\`\`

Если ключа нет — \`del\` выбросит \`KeyError\`.

### Итерация: keys(), values(), items()

Три способа пройтись по словарю:

\`\`\`python
contacts = {"Anna": "111", "Ivan": "222", "Maria": "333"}

# Только ключи
for name in contacts.keys():
    print(name)         # Anna, Ivan, Maria

# Только значения
for phone in contacts.values():
    print(phone)        # 111, 222, 333

# Пары ключ-значение
for name, phone in contacts.items():
    print(name, "->", phone)  # Anna -> 111, и т.д.
\`\`\`

Метод \`items()\` особенно удобен, когда нужны и ключ, и значение одновременно.

### Проверка наличия ключа: in

\`\`\`python
contacts = {"Anna": "111", "Ivan": "222"}

print("Anna" in contacts)     # True
print("Petya" in contacts)    # False

# Это проверяет КЛЮЧИ, не значения!
print("111" in contacts)      # False — "111" не ключ, это значение
\`\`\`

## 🧪 Примеры

\`\`\`python
# Подсчёт частоты слов
text = "cat dog cat cat dog parrot"
words = text.split()
frequency = {}

for word in words:
    frequency[word] = frequency.get(word, 0) + 1

print(frequency)   # {"cat": 3, "dog": 2, "parrot": 1}
\`\`\`

\`\`\`python
# Конфигурация приложения
config = {
    "debug": False,
    "max_retries": 3,
    "timeout": 30
}

# Безопасное получение с дефолтом
debug_mode = config.get("debug", False)
log_level = config.get("log_level", "INFO")   # ключа нет — берётся "INFO"
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Использовать \`[]\` вместо \`get()\` там, где ключ может отсутствовать.** Если есть хоть малейшие сомнения — используй \`get()\`. \`KeyError\` в production-коде — частый источник сбоев.

2. **Путать ключи и значения в \`in\`.** \`"значение" in словарь\` проверяет **ключи**, не значения. Чтобы проверить значение, используй \`"значение" in словарь.values()\`.

3. **Использовать изменяемый тип как ключ.** \`{[1, 2]: "value"}\` — это \`TypeError\`, потому что списки нельзя хешировать. Используй кортежи: \`{(1, 2): "value"}\`.

4. **Изменять словарь во время итерации.** Цикл \`for key in dict\` и удаление ключей внутри него — \`RuntimeError\`. Вместо этого итерируйся по копии: \`for key in list(dict.keys())\`.

5. **Забывать, что словари сохраняют порядок (с Python 3.7+).** Элементы хранятся в порядке добавления, но опираться на порядок в бизнес-логике — сомнительная практика.

## 💡 Мини-тест самопроверки

1. Что вернёт \`{"a": 1, "b": 2}.get("c", 0)\`?
<details><summary>Ответ</summary>\`0\` — ключ "c" не существует, возвращается значение по умолчанию.</details>

2. Что произойдёт при \`{"a": 1}["b"]\`?
<details><summary>Ответ</summary>Будет выброшен \`KeyError: 'b'\` — ключ не существует.</details>

3. Что проверяет \`"x" in {"x": 10, "y": 20}\`?
<details><summary>Ответ</summary>Проверяет, является ли "x" ключом словаря. Вернёт \`True\`.</details>

## 🛠️ Задание

Напиши функцию \`lookup_phone(contacts, name)\`, которая принимает словарь контактов (имя → телефон) и имя, и возвращает номер телефона этого человека. Если имя не найдено — вернуть строку \`"Не найдено"\`.`,

  starterCode: `def lookup_phone(contacts, name):
    # Используй метод get() для безопасного поиска
    # Если имя не найдено, вернуть "Не найдено"
    pass`,

  solution: `def lookup_phone(contacts, name):
    return contacts.get(name, "Не найдено")`,

  tests: [
    { input: 'lookup_phone({"Anna": "111", "Ivan": "222"}, "Anna")', expected: "111",         description: "Нашли" },
    { input: 'lookup_phone({"Anna": "111"}, "Bob")',                  expected: "Не найдено", description: "Не нашли" },
    { input: 'lookup_phone({}, "Anna")',                              expected: "Не найдено", description: "Пустой словарь" },
    { input: 'lookup_phone({"Anna": "111", "Ivan": "222"}, "Ivan")', expected: "222",         description: "Второй ключ" },
  ],

  hints: [
    "Тебе нужно найти значение по ключу в словаре, но если ключа нет — вернуть строку \"Не найдено\". Есть ли метод словаря, который делает это в одну строку без проверки `if`?",
    "Метод `get(ключ, значение_по_умолчанию)` делает именно это: возвращает значение если ключ есть, иначе — значение по умолчанию. Попробуй `contacts.get(name, \"Не найдено\")`.",
    "Полное решение:\n```python\ndef lookup_phone(contacts, name):\n    return contacts.get(name, \"Не найдено\")\n```",
  ],

  isAvailable: true,
};

export default lesson;
