import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Условные конструкции: if / elif / else",
  order: 8,
  type: "code",
  language: "python",
  xpReward: 130,
  content: `# Условные конструкции: if / elif / else

## 🎯 Что научишься делать
- Использовать \`if\`, \`elif\` и \`else\` для ветвления программы
- Понимать роль отступов — обязательное правило Python
- Строить цепочки условий с несколькими вариантами
- Возвращать разные значения из функции в зависимости от условия

## 🔍 Зачем это нужно
Любая полезная программа умеет принимать решения. Интернет-магазин показывает скидку только зарегистрированным пользователям. Банковское приложение блокирует карту после трёх неверных PIN-кодов. Игра добавляет бонус за высокий счёт. Всё это — ветвление кода. \`if / elif / else\` — это основа основ: без условных конструкций программа не может реагировать на разные ситуации.

## 📚 Теория

### Метафора: развилка на дороге

Представь, что ты едешь по дороге и видишь знак: «Налево — в город, прямо — на трассу, направо — к озеру». Ты выбираешь направление в зависимости от цели поездки.

Конструкция \`if / elif / else\` работает точно так же. Программа подходит к развилке, проверяет условие, и идёт по нужному пути. Остальные пути игнорируются.

### Простейший if

\`\`\`python
temperature = 35

if temperature > 30:
    print("Очень жарко!")
\`\`\`

Разберём каждую строку:
- \`if temperature > 30:\` — «если температура больше 30...» Двоеточие в конце обязательно — это часть синтаксиса Python.
- \`    print("Очень жарко!")\` — тело условия. Обрати внимание на **4 пробела** слева. Это не украшение — это **отступ**, который сообщает Python: «эта строка выполняется только внутри if».

Если условие ложно (\`temperature = 20\`), строка с \`print\` просто пропускается.

### Ключевое правило Python: отступы

В большинстве языков блоки кода обозначаются фигурными скобками \`{}\`. Python выбрал другой путь — **отступы**. Именно они показывают, какие строки принадлежат блоку.

\`\`\`python
if True:
    print("Я внутри блока")   # отступ 4 пробела — это тело if
    print("Я тоже внутри")    # тоже 4 пробела — тоже внутри
print("Я снаружи")             # нет отступа — выполняется всегда
\`\`\`

Ошибка с отступом — одна из самых частых у новичков. Если отступа нет там, где он нужен, Python выдаст \`IndentationError\` и откажется выполнять код.

Стандарт: **4 пробела**. Большинство редакторов вставляют их автоматически при нажатии Tab.

### Ветка else — «иначе»

\`else\` — это путь, который выбирается, если условие \`if\` ложно:

\`\`\`python
age = 15

if age >= 18:
    print("Добро пожаловать!")
else:
    print("Извините, вход с 18 лет")
\`\`\`

Здесь только два пути: либо \`age >= 18\` — тогда первый блок, либо нет — тогда блок \`else\`. Третьего не дано.

### Цепочка elif — несколько вариантов

Что если вариантов больше двух? Для этого есть \`elif\` (сокращение от «else if» — «иначе если»):

\`\`\`python
score = 75

if score >= 90:
    print("Отлично!")
elif score >= 75:
    print("Хорошо")
elif score >= 60:
    print("Удовлетворительно")
else:
    print("Нужно подтянуться")
\`\`\`

Python проверяет условия сверху вниз и выполняет **первый** блок, условие которого истинно. Как только нашёл — останавливается, остальные не проверяет.

Поэтому порядок важен. Если написать \`elif score >= 60\` перед \`elif score >= 75\`, оценка «Хорошо» никогда не выставится — балл 75 уже попадёт под первое условие.

\`\`\`python
score = 75

# Неправильный порядок — score=75 попадёт в первый elif и не дойдёт до второго
if score >= 90:
    print("Отлично!")
elif score >= 60:     # 75 >= 60 — True! Выполнится здесь и остановится
    print("Удовлетворительно")
elif score >= 75:     # до сюда уже не дойдёт
    print("Хорошо")
\`\`\`

Правило: располагай условия от наиболее строгого (самого высокого порога) к наименее строгому.

### Вложенные if

Иногда нужно проверить второе условие только внутри первого:

\`\`\`python
is_member = True
balance = 500

if is_member:
    if balance >= 1000:
        print("Премиум доступ открыт")
    else:
        print("Пополни баланс до 1000")
else:
    print("Сначала зарегистрируйся")
\`\`\`

Вложенные \`if\` работают, но злоупотреблять ими не стоит — глубокое вложение делает код трудно читаемым. В реальных проектах стараются использовать \`and\` / \`or\` вместо вложений, когда это возможно.

### if в функции: возврат разных значений

Функции часто используют \`if\` для возврата разных результатов. Как только Python встречает \`return\`, выполнение функции завершается — последующие строки не выполняются:

\`\`\`python
def classify_temperature(temp):
    if temp > 35:
        return "жара"
    elif temp > 20:
        return "тепло"
    elif temp > 5:
        return "прохладно"
    else:
        return "холодно"

print(classify_temperature(40))   # жара
print(classify_temperature(22))   # тепло
print(classify_temperature(10))   # прохладно
print(classify_temperature(-5))   # холодно
\`\`\`

Обрати внимание: у каждого \`if / elif / else\` есть свой \`return\`. После первого совпадения функция возвращает значение и сразу выходит.

## 🧪 Примеры

\`\`\`python
# Проверка чётности числа
def is_even(n):
    if n % 2 == 0:
        return "чётное"
    else:
        return "нечётное"

print(is_even(4))   # чётное
print(is_even(7))   # нечётное
\`\`\`

\`\`\`python
# Категория товара по цене
def price_category(price):
    if price >= 10000:
        return "премиум"
    elif price >= 3000:
        return "средний"
    else:
        return "бюджет"

print(price_category(15000))  # премиум
print(price_category(4500))   # средний
print(price_category(800))    # бюджет
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Забыть двоеточие после условия.** \`if x > 0\` — Python выдаст \`SyntaxError\`. Правильно: \`if x > 0:\`. Двоеточие нужно также после \`elif\` и \`else\`.

2. **Неправильный отступ.** \`IndentationError\` — один из самых частых в Python. Убедись, что каждая строка тела блока имеет ровно 4 пробела. Не 3, не 5 — именно 4 (или используй Tab, который IDE обычно переводит в 4 пробела).

3. **Использовать \`=\` вместо \`==\` в условии.** \`if x = 5:\` — это \`SyntaxError\`. Для сравнения всегда \`==\`.

4. **Неправильный порядок elif.** Если ставишь менее строгое условие раньше более строгого, более строгий вариант никогда не выполнится.

5. **Забыть ветку else.** Если ни одно условие не сработало и \`else\` нет, функция вернёт \`None\`. Это может привести к неожиданным ошибкам дальше по коду.

## 💡 Мини-тест самопроверки

1. Что выведет следующий код?
\`\`\`python
x = 85
if x >= 90:
    print("A")
elif x >= 80:
    print("B")
elif x >= 70:
    print("C")
else:
    print("F")
\`\`\`
<details><summary>Ответ</summary>\`B\` — x=85 не проходит первое условие (85 < 90), но проходит второе (85 >= 80), поэтому Python выводит "B" и останавливается.</details>

2. Будет ли ошибка в этом коде?
\`\`\`python
if 5 > 3:
print("да")
\`\`\`
<details><summary>Ответ</summary>Да, \`IndentationError\` — строка \`print\` должна иметь отступ 4 пробела, так как она внутри блока \`if\`.</details>

3. Почему \`if x = 5:\` — это ошибка?
<details><summary>Ответ</summary>Потому что \`=\` — это присваивание, а не сравнение. Для сравнения нужно \`==\`: \`if x == 5:\`.</details>

## 🛠️ Задание

Напиши функцию \`grade(score)\`, которая принимает балл (число от 0 до 100) и возвращает оценку в виде буквы:

- \`score >= 90\` → \`"A"\`
- \`score >= 80\` → \`"B"\`
- \`score >= 70\` → \`"C"\`
- \`score >= 60\` → \`"D"\`
- иначе → \`"F"\`

Функция должна возвращать строку — одну букву без кавычек в выводе.`,

  starterCode: `def grade(score):
    # Используй if / elif / else
    # Проверяй от высшего к низшему
    pass`,

  solution: `def grade(score):
    if score >= 90:
        return "A"
    elif score >= 80:
        return "B"
    elif score >= 70:
        return "C"
    elif score >= 60:
        return "D"
    else:
        return "F"`,

  tests: [
    { input: "grade(95)", expected: "A", description: "Высокий балл" },
    { input: "grade(85)", expected: "B", description: "Средне-высокий" },
    { input: "grade(72)", expected: "C", description: "Средний" },
    { input: "grade(60)", expected: "D", description: "Граничный 60 → D" },
    { input: "grade(45)", expected: "F", description: "Провал" },
  ],

  hints: [
    "Начни с самого высокого порога: `if score >= 90: return \"A\"`. Потом добавь `elif score >= 80: return \"B\"` и так далее по убыванию. Порядок важен — Python проверяет условия сверху вниз.",
    "Структура: `if score >= 90:` → A, `elif score >= 80:` → B, `elif score >= 70:` → C, `elif score >= 60:` → D, `else:` → F. Каждый блок содержит только `return` с нужной буквой.",
    "Полное решение:\n```python\ndef grade(score):\n    if score >= 90:\n        return \"A\"\n    elif score >= 80:\n        return \"B\"\n    elif score >= 70:\n        return \"C\"\n    elif score >= 60:\n        return \"D\"\n    else:\n        return \"F\"\n```",
  ],

  isAvailable: true,
};

export default lesson;
