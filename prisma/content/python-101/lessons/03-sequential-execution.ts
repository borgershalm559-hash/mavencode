import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Как думает программа: инструкции по порядку",
  order: 3,
  type: "fill-blanks",
  language: "python",
  xpReward: 100,
  content: `# Как думает программа: инструкции по порядку

## 🎯 Что научишься делать
- Понять, что Python выполняет код строго сверху вниз
- Объяснить, как порядок инструкций влияет на результат
- Использовать комментарии для пояснения шагов программы
- Заполнять пропуски в готовом коде

## 🔍 Зачем это нужно
Это кажется очевидным — «ну конечно сверху вниз!» — но именно здесь новички делают первые логические ошибки. Программа не думает. Она не видит весь код сразу. Каждая строка выполняется в тот момент, когда до неё доходит очередь. Если поменять порядок строк — изменится результат. Это фундамент, без которого дальше не двинуться.

## 📚 Теория

### Программа — это список дел, а не карта города

Представь, что ты пишешь список дел на утро:

1. Проснуться
2. Выпить кофе
3. Принять душ
4. Поехать на работу

Если ты поменяешь шаги местами — «Поехать на работу, потом проснуться» — это уже другой список с другим результатом. Программа работает точно так же.

**Python читает код строка за строкой, сверху вниз, без исключений.** Это называется **последовательным выполнением** (sequential execution).

\`\`\`python
print("Просыпаюсь")    # выполнится первой
print("Пью кофе")      # выполнится второй
print("Еду на работу") # выполнится третьей
\`\`\`

Вывод:
\`\`\`
Просыпаюсь
Пью кофе
Еду на работу
\`\`\`

Если переставить строки:

\`\`\`python
print("Еду на работу") # теперь первая
print("Просыпаюсь")    # теперь вторая
print("Пью кофе")      # теперь третья
\`\`\`

Вывод:
\`\`\`
Еду на работу
Просыпаюсь
Пью кофе
\`\`\`

Результат изменился, потому что изменился порядок. Python не знал, что «Пью кофе» должно быть вторым — он просто выполнял то, что написано, в том порядке, в котором написано.

### Переменные тоже выполняются по порядку

Порядок важен не только для \`print()\`. Когда ты создаёшь переменную — Python «запоминает» её значение в этот конкретный момент:

\`\`\`python
step1 = "Просыпаюсь"    # Python запоминает step1
step2 = "Пью кофе"      # Python запоминает step2
step3 = "Еду на работу" # Python запоминает step3

print(step1)  # выводит то, что в step1 прямо сейчас
print(step2)
print(step3)
\`\`\`

Если попробовать использовать переменную до её создания:

\`\`\`python
print(step1)            # ОШИБКА: step1 ещё не существует!
step1 = "Просыпаюсь"   # Python создаст её только здесь
\`\`\`

Python выдаст \`NameError: name 'step1' is not defined\` — потому что дошёл до \`print(step1)\` раньше, чем встретил \`step1 = ...\`.

### Комментарии: объясняй порядок шагов

Хорошая практика — комментировать последовательные шаги, особенно когда порядок важен:

\`\`\`python
# Шаг 1: подготовка
name = "Мария"

# Шаг 2: приветствие
greeting = "Привет, " + name

# Шаг 3: вывод результата
print(greeting)
\`\`\`

Комментарий \`# Шаг 1:...\` — это просто текст для читателя. Python его полностью игнорирует. Но другой программист (или ты через три месяца) сразу поймёт логику кода.

### Важный момент: Python — это не таблица Excel

В Excel, если ты меняешь ячейку A1, ячейка B1, которая ссылается на A1, автоматически пересчитывается. Python так **не работает**.

\`\`\`python
a = 5
b = a + 1    # b = 6, потому что a было 5 в этот момент
a = 10       # теперь меняем a
print(b)     # выведет 6, а НЕ 11!
\`\`\`

\`b\` остаётся равным \`6\` — значению, которое было **в момент присваивания**. Python не следит за «связями» между переменными. Он выполняет инструкции по очереди и запоминает результат каждой.

## 🧪 Примеры

\`\`\`python
# Пример 1: утренний план
step1 = "Просыпаюсь"
step2 = "Пью кофе"
step3 = "Еду на работу"
print(step1)
print(step2)
print(step3)
# Вывод:
# Просыпаюсь
# Пью кофе
# Еду на работу

# Пример 2: порядок меняет результат
x = 1
x = 2
x = 3
print(x)   # Выведет 3 — последнее присваивание побеждает
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Использовать переменную до создания.** Если написать \`print(result)\` раньше, чем \`result = ...\` — будет \`NameError\`. Python выполняет код сверху вниз и не «заглядывает вперёд».

2. **Думать, что переменные «реагируют» на изменения.** \`b = a + 1\` вычисляется один раз в момент выполнения. Если потом изменить \`a\` — \`b\` останется прежним.

3. **Случайно перепутать порядок строк при копировании.** Выглядит как мелочь, но результат может быть совершенно другим. Всегда проверяй, что шаги идут в правильном порядке.

4. **Забыть, что пустые строки в коде — это нормально.** Пустые строки Python игнорирует, они только улучшают читаемость. Можешь смело ставить пустую строку между логическими блоками кода.

## 💡 Мини-тест самопроверки

1. Что выведет программа, если \`print("Б")\` стоит перед \`print("А")\`? (Ответ: сначала «Б», потом «А» — Python не сортирует, он выполняет по порядку)
2. Будет ли ошибка в \`print(x)\` если строка \`x = 5\` идёт после? (Ответ: да, \`NameError\` — переменная ещё не создана)
3. Что выведет \`a = 1; a = 99; print(a)\`? (Ответ: \`99\` — последнее присваивание)

## 🛠️ Задание

Заполни пропуски \`"___"\` в утреннем плане, чтобы переменные содержали правильные значения.

Порядок шагов утреннего плана:
1. \`"Просыпаюсь"\`
2. \`"Пью кофе"\`
3. \`"Еду на работу"\`

Тест проверит значения переменных \`step1\`, \`step2\` и \`step3\`.`,

  starterCode: `# Утренний план: заполни пропуски правильными строками
# Порядок важен! Шаги должны идти в правильной последовательности

step1 = "___"  # что ты делаешь первым делом утром
step2 = "___"  # затем
step3 = "___"  # в конце

# Выводим план
print(step1)
print(step2)
print(step3)
`,

  solution: `step1 = "Просыпаюсь"
step2 = "Пью кофе"
step3 = "Еду на работу"`,

  tests: [
    {
      input: "step1",
      expected: "Просыпаюсь",
      description: "Первый шаг — Просыпаюсь",
    },
    {
      input: "step2",
      expected: "Пью кофе",
      description: "Второй шаг — Пью кофе",
    },
    {
      input: "step3",
      expected: "Еду на работу",
      description: "Третий шаг — Еду на работу",
    },
  ],

  hints: [
    "Заменяй каждый `\"___\"` на нужный текст. Первый шаг — это то, что делают сразу после пробуждения. Текст должен совпадать точно: `\"Просыпаюсь\"`, `\"Пью кофе\"`, `\"Еду на работу\"`.",
    "Присваивай значения по одному: `step1 = \"Просыпаюсь\"`, потом `step2 = \"Пью кофе\"`, потом `step3 = \"Еду на работу\"`. Текст строго как написан в условии, включая заглавную букву.",
    "Полное решение:\n```python\nstep1 = \"Просыпаюсь\"\nstep2 = \"Пью кофе\"\nstep3 = \"Еду на работу\"\nprint(step1)\nprint(step2)\nprint(step3)\n```",
  ],

  isAvailable: true,
};

export default lesson;
