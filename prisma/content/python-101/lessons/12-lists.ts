import type { LessonContent } from "../types";

const lesson: LessonContent = {
  title: "Списки: упорядоченные коллекции",
  order: 12,
  type: "code",
  language: "python",
  xpReward: 140,
  content: `# Списки: упорядоченные коллекции

## 🎯 Что научишься делать
- Создавать списки и обращаться к элементам по индексу
- Использовать отрицательные индексы и срезы
- Добавлять и удалять элементы
- Проверять наличие элемента и длину списка

## 🔍 Зачем это нужно
Представь, что тебе нужно хранить оценки студента за семестр, список покупок или набор имён пользователей. Заводить отдельную переменную на каждый элемент — неудобно и нереалистично. Список позволяет хранить сколько угодно значений под одним именем и обращаться к каждому по его номеру. Списки — одна из самых часто используемых структур данных в Python: без них не написать ни один серьёзный проект.

## 📚 Теория

### Метафора: полка с пронумерованными ящиками

Представь книжную полку, где каждый ящик пронумерован. Нумерация начинается с **нуля** — первый ящик имеет номер 0, второй — 1, третий — 2, и так далее. В каждом ящике что-то лежит (элемент), и ты всегда можешь взять нужное, зная его номер.

\`\`\`
полка:   ["яблоко", "банан", "вишня", "груша"]
индекс:       0        1        2        3
\`\`\`

Это и есть список в Python.

### Создание списка

Список создаётся с помощью квадратных скобок. Элементы разделяются запятыми:

\`\`\`python
fruits = ["яблоко", "банан", "вишня"]   # список строк
numbers = [10, 20, 30, 40, 50]          # список чисел
mixed = [1, "привет", 3.14, True]       # список разных типов
empty = []                               # пустой список
\`\`\`

Хотя Python разрешает смешивать типы в одном списке, на практике списки обычно содержат элементы **одного типа** — это делает код понятнее.

### Доступ по индексу

Чтобы получить элемент, укажи его номер в квадратных скобках:

\`\`\`python
fruits = ["яблоко", "банан", "вишня"]

print(fruits[0])   # яблоко  — первый элемент
print(fruits[1])   # банан   — второй элемент
print(fruits[2])   # вишня   — третий элемент
\`\`\`

Если указать индекс, которого нет в списке, Python выдаст ошибку \`IndexError\`:

\`\`\`python
print(fruits[5])   # IndexError: list index out of range
\`\`\`

### Отрицательные индексы

Python умеет считать **с конца**. Индекс \`-1\` — это последний элемент, \`-2\` — предпоследний, и так далее:

\`\`\`python
fruits = ["яблоко", "банан", "вишня", "груша"]

print(fruits[-1])   # груша  — последний
print(fruits[-2])   # вишня  — предпоследний
print(fruits[-4])   # яблоко — четвёртый с конца (= первый)
\`\`\`

Отрицательные индексы очень удобны, когда ты знаешь, что тебе нужен «последний» или «предпоследний» элемент, но не знаешь точную длину списка.

\`\`\`
полка:   ["яблоко", "банан", "вишня", "груша"]
+ индекс:     0        1        2        3
- индекс:    -4       -3       -2       -1
\`\`\`

### Срезы (slicing)

Срез позволяет получить **несколько элементов подряд** — кусок списка. Синтаксис: \`список[начало:конец]\`. Важно: **конец не включается** в результат.

\`\`\`python
numbers = [10, 20, 30, 40, 50]

print(numbers[1:3])   # [20, 30]   — индексы 1 и 2 (3 не включается)
print(numbers[:2])    # [10, 20]   — с начала до индекса 2 (не включая)
print(numbers[2:])    # [30, 40, 50] — с индекса 2 до конца
print(numbers[:])     # [10, 20, 30, 40, 50] — полная копия списка
\`\`\`

Думай о срезе как о ножницах: ты указываешь, **откуда** начать резать и **где** остановиться (не дорезая до конечной точки).

### Основные операции: append, remove, len, in

\`\`\`python
fruits = ["яблоко", "банан"]

# Добавить элемент в конец
fruits.append("вишня")
print(fruits)          # ["яблоко", "банан", "вишня"]

# Удалить первое вхождение элемента
fruits.remove("банан")
print(fruits)          # ["яблоко", "вишня"]

# Длина списка
print(len(fruits))     # 2

# Проверить наличие элемента
print("яблоко" in fruits)    # True
print("манго" in fruits)     # False
\`\`\`

Обрати внимание: \`append\` и \`remove\` **изменяют** сам список (так называемая мутация). Это отличается от строк — строки в Python неизменяемы, а списки изменяемы.

### Изменение элемента

Ты можешь заменить любой элемент, обратившись к нему по индексу:

\`\`\`python
fruits = ["яблоко", "банан", "вишня"]
fruits[1] = "манго"
print(fruits)   # ["яблоко", "манго", "вишня"]
\`\`\`

## 🧪 Примеры

\`\`\`python
# Список оценок студента
grades = [85, 92, 78, 90, 88]

print(grades[0])    # 85 — первая оценка
print(grades[-1])   # 88 — последняя оценка
print(grades[1:4])  # [92, 78, 90] — оценки со второй по четвёртую

# Добавим новую оценку
grades.append(95)
print(len(grades))  # 6 — теперь шесть оценок
\`\`\`

\`\`\`python
# Фильтрация: только числа больше 50
numbers = [10, 70, 30, 90, 20, 60]
big = []
for n in numbers:
    if n > 50:
        big.append(n)
print(big)   # [70, 90, 60]
\`\`\`

## ⚠️ Частые ошибки новичков

1. **Начинать индексацию с 1.** Первый элемент — \`list[0]\`, не \`list[1]\`. Это частое заблуждение для тех, кто впервые знакомится с программированием.

2. **Выйти за границы списка.** \`list[len(list)]\` — это ошибка! Последний индекс — \`len(list) - 1\`. Или используй \`list[-1]\`.

3. **Путать \`remove\` и \`del\`.** \`list.remove(значение)\` удаляет по значению. \`del list[индекс]\` удаляет по индексу. Если элемент встречается несколько раз, \`remove\` удалит только первый.

4. **Забывать, что срез создаёт новый список.** \`a = [1, 2, 3]; b = a[:];\` — это две независимые копии. Изменение \`b\` не затронет \`a\`.

5. **Мутировать список во время итерации по нему.** Удалять или добавлять элементы прямо внутри цикла \`for item in list\` — источник трудноуловимых багов.

## 💡 Мини-тест самопроверки

1. Какой индекс у последнего элемента списка \`[10, 20, 30, 40]\`?
<details><summary>Ответ</summary>Индекс 3 (или -1). Список из 4 элементов имеет индексы 0, 1, 2, 3.</details>

2. Что вернёт \`[10, 20, 30, 40, 50][1:4]\`?
<details><summary>Ответ</summary>\`[20, 30, 40]\` — элементы с индексами 1, 2 и 3 (индекс 4 не включается).</details>

3. Что произойдёт при \`["a", "b", "c"].remove("d")\`?
<details><summary>Ответ</summary>Будет выброшено исключение \`ValueError\`, потому что элемент "d" не найден в списке.</details>

## 🛠️ Задание

Напиши функцию \`filter_even(numbers)\`, которая принимает список чисел и возвращает **новый список**, содержащий только чётные числа. Исходный список изменять нельзя.

Число чётное, если остаток от деления на 2 равен нулю: \`n % 2 == 0\`.`,

  starterCode: `def filter_even(numbers):
    # Создай новый пустой список
    # Пройди по каждому числу в numbers
    # Если число чётное — добавь его в новый список
    # Верни новый список
    pass`,

  solution: `def filter_even(numbers):
    result = []
    for n in numbers:
        if n % 2 == 0:
            result.append(n)
    return result`,

  tests: [
    { input: "filter_even([1, 2, 3, 4, 5, 6])", expected: "[2, 4, 6]",    description: "Смешанный список" },
    { input: "filter_even([1, 3, 5])",           expected: "[]",           description: "Только нечётные" },
    { input: "filter_even([])",                  expected: "[]",           description: "Пустой список" },
    { input: "filter_even([0, -2, -1, 4])",      expected: "[0, -2, 4]",  description: "Отрицательные и ноль" },
  ],

  hints: [
    "Тебе нужен новый список, который ты будешь постепенно заполнять. Начни с `result = []`, затем пройди по каждому элементу `numbers` через `for n in numbers:` и проверяй условие чётности.",
    "Чётное число — это то, у которого остаток от деления на 2 равен нулю: `n % 2 == 0`. Если условие выполняется, добавляй элемент в результат: `result.append(n)`. В конце верни `result`.",
    "Полное решение:\n```python\ndef filter_even(numbers):\n    result = []\n    for n in numbers:\n        if n % 2 == 0:\n            result.append(n)\n    return result\n```",
  ],

  isAvailable: true,
};

export default lesson;
